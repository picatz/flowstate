// Code generated by protoc-gen-go. DO NOT EDIT.
// versions:
// 	protoc-gen-go v1.36.11
// 	protoc        (unknown)
// source: flowstate/v1/flowstate.proto

package flowstatev1

import (
	_ "buf.build/gen/go/bufbuild/protovalidate/protocolbuffers/go/buf/validate"
	_ "google.golang.org/genproto/googleapis/api/annotations"
	v1alpha1 "google.golang.org/genproto/googleapis/api/expr/v1alpha1"
	protoreflect "google.golang.org/protobuf/reflect/protoreflect"
	protoimpl "google.golang.org/protobuf/runtime/protoimpl"
	durationpb "google.golang.org/protobuf/types/known/durationpb"
	timestamppb "google.golang.org/protobuf/types/known/timestamppb"
	reflect "reflect"
	sync "sync"
	unsafe "unsafe"
)

const (
	// Verify that this generated code is sufficiently up-to-date.
	_ = protoimpl.EnforceVersion(20 - protoimpl.MinVersion)
	// Verify that runtime/protoimpl is sufficiently up-to-date.
	_ = protoimpl.EnforceVersion(protoimpl.MaxVersion - 20)
)

// Type is the shape a value must have to be accepted.
//
// An enum rather than a type *expression* like [TaskField.type], and the
// difference between those two fields is the difference between what they are
// for. A TaskField is documentation: it is rendered for a person or an agent
// reading the catalog, and `list[string]` is the right answer there because it
// is what an author would say. This one is *enforced*, at submit, against a
// value some caller chose — so it has to be a closed set the schema can
// constrain and every consumer can switch on. Spelling it as a string would put
// a parser for the same small type language into the CLI, the server, the
// language server and the compiler, which is the "one value written down four
// times" failure this repository keeps finding in other clothes.
//
// These are the six of [Value.Type] that describe *data*, with the same names
// and the same numbers. The two left out are TYPE_EXPR and TYPE_ERROR, and they
// are precisely the two that are not data: an expression is something a run
// evaluates, and a caller does not get to send one. Sharing numbers does not
// make the enums interchangeable — the sets differ, so a conversion between
// them is a switch somebody wrote and somebody reviewed, never a cast.
//
// A parameterized type — a list *of* what — is not expressible here yet, on
// purpose. An element type that nothing checks is decoration, which is the rule
// that put this whole surface in Phase 2 with its checker; and an added enum
// value or an added field is free later, where a wrong one is not.
type InputDeclaration_Type int32

const (
	InputDeclaration_TYPE_UNSPECIFIED InputDeclaration_Type = 0
	InputDeclaration_TYPE_STRING      InputDeclaration_Type = 1
	InputDeclaration_TYPE_INT         InputDeclaration_Type = 2
	InputDeclaration_TYPE_FLOAT       InputDeclaration_Type = 3
	InputDeclaration_TYPE_BOOL        InputDeclaration_Type = 4
	InputDeclaration_TYPE_STRUCT      InputDeclaration_Type = 5
	InputDeclaration_TYPE_LIST        InputDeclaration_Type = 6
)

// Enum value maps for InputDeclaration_Type.
var (
	InputDeclaration_Type_name = map[int32]string{
		0: "TYPE_UNSPECIFIED",
		1: "TYPE_STRING",
		2: "TYPE_INT",
		3: "TYPE_FLOAT",
		4: "TYPE_BOOL",
		5: "TYPE_STRUCT",
		6: "TYPE_LIST",
	}
	InputDeclaration_Type_value = map[string]int32{
		"TYPE_UNSPECIFIED": 0,
		"TYPE_STRING":      1,
		"TYPE_INT":         2,
		"TYPE_FLOAT":       3,
		"TYPE_BOOL":        4,
		"TYPE_STRUCT":      5,
		"TYPE_LIST":        6,
	}
)

func (x InputDeclaration_Type) Enum() *InputDeclaration_Type {
	p := new(InputDeclaration_Type)
	*p = x
	return p
}

func (x InputDeclaration_Type) String() string {
	return protoimpl.X.EnumStringOf(x.Descriptor(), protoreflect.EnumNumber(x))
}

func (InputDeclaration_Type) Descriptor() protoreflect.EnumDescriptor {
	return file_flowstate_v1_flowstate_proto_enumTypes[0].Descriptor()
}

func (InputDeclaration_Type) Type() protoreflect.EnumType {
	return &file_flowstate_v1_flowstate_proto_enumTypes[0]
}

func (x InputDeclaration_Type) Number() protoreflect.EnumNumber {
	return protoreflect.EnumNumber(x)
}

// Deprecated: Use InputDeclaration_Type.Descriptor instead.
func (InputDeclaration_Type) EnumDescriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{3, 0}
}

// Overlap says what happens when a firing comes due while the previous run is
// still going.
//
// Mirrors Temporal's `ScheduleOverlapPolicy` value for value. Named as a
// Flowstate enum rather than importing Temporal's because this is the schema a
// Flowfile compiles to and a plugin compiles against, and neither should
// acquire a dependency on the execution substrate's API to read a workflow —
// the same reasoning that keeps [RetryPolicy] a Flowstate message.
type ScheduleTrigger_Overlap int32

const (
	// Unspecified takes Temporal's own default, which is SKIP.
	//
	// Left as a real arm rather than defaulted here so that "the author said
	// nothing" and "the author wrote skip" stay distinguishable in the
	// specification. They behave identically; only one of them changes if
	// Temporal's default ever does, and that is the correct one to move.
	ScheduleTrigger_OVERLAP_UNSPECIFIED ScheduleTrigger_Overlap = 0
	// Skip drops the new firing entirely. The safe default: a nightly job that
	// takes longer than a day does not pile up.
	ScheduleTrigger_OVERLAP_SKIP ScheduleTrigger_Overlap = 1
	// BufferOne runs the new firing after the current one finishes, keeping at
	// most one waiting.
	ScheduleTrigger_OVERLAP_BUFFER_ONE ScheduleTrigger_Overlap = 2
	// BufferAll queues every firing, running them one after another. A backlog
	// is bounded only by how long the runs take, so this is the arm to think
	// twice about.
	ScheduleTrigger_OVERLAP_BUFFER_ALL ScheduleTrigger_Overlap = 3
	// CancelOther cancels the running one and starts the new one once it has
	// stopped, letting the old run clean up on the way out.
	ScheduleTrigger_OVERLAP_CANCEL_OTHER ScheduleTrigger_Overlap = 4
	// TerminateOther stops the running one immediately and starts the new one.
	// Nothing the old run would have done on its way out is done.
	ScheduleTrigger_OVERLAP_TERMINATE_OTHER ScheduleTrigger_Overlap = 5
	// AllowAll starts the new firing regardless, so several runs of the workload
	// are in flight at once.
	ScheduleTrigger_OVERLAP_ALLOW_ALL ScheduleTrigger_Overlap = 6
)

// Enum value maps for ScheduleTrigger_Overlap.
var (
	ScheduleTrigger_Overlap_name = map[int32]string{
		0: "OVERLAP_UNSPECIFIED",
		1: "OVERLAP_SKIP",
		2: "OVERLAP_BUFFER_ONE",
		3: "OVERLAP_BUFFER_ALL",
		4: "OVERLAP_CANCEL_OTHER",
		5: "OVERLAP_TERMINATE_OTHER",
		6: "OVERLAP_ALLOW_ALL",
	}
	ScheduleTrigger_Overlap_value = map[string]int32{
		"OVERLAP_UNSPECIFIED":     0,
		"OVERLAP_SKIP":            1,
		"OVERLAP_BUFFER_ONE":      2,
		"OVERLAP_BUFFER_ALL":      3,
		"OVERLAP_CANCEL_OTHER":    4,
		"OVERLAP_TERMINATE_OTHER": 5,
		"OVERLAP_ALLOW_ALL":       6,
	}
)

func (x ScheduleTrigger_Overlap) Enum() *ScheduleTrigger_Overlap {
	p := new(ScheduleTrigger_Overlap)
	*p = x
	return p
}

func (x ScheduleTrigger_Overlap) String() string {
	return protoimpl.X.EnumStringOf(x.Descriptor(), protoreflect.EnumNumber(x))
}

func (ScheduleTrigger_Overlap) Descriptor() protoreflect.EnumDescriptor {
	return file_flowstate_v1_flowstate_proto_enumTypes[1].Descriptor()
}

func (ScheduleTrigger_Overlap) Type() protoreflect.EnumType {
	return &file_flowstate_v1_flowstate_proto_enumTypes[1]
}

func (x ScheduleTrigger_Overlap) Number() protoreflect.EnumNumber {
	return protoreflect.EnumNumber(x)
}

// Deprecated: Use ScheduleTrigger_Overlap.Descriptor instead.
func (ScheduleTrigger_Overlap) EnumDescriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{7, 0}
}

type Value_Type int32

const (
	Value_TYPE_UNSPECIFIED Value_Type = 0
	Value_TYPE_STRING      Value_Type = 1
	Value_TYPE_INT         Value_Type = 2
	Value_TYPE_FLOAT       Value_Type = 3
	Value_TYPE_BOOL        Value_Type = 4
	Value_TYPE_STRUCT      Value_Type = 5
	Value_TYPE_LIST        Value_Type = 6
	Value_TYPE_EXPR        Value_Type = 7
	Value_TYPE_ERROR       Value_Type = 8
)

// Enum value maps for Value_Type.
var (
	Value_Type_name = map[int32]string{
		0: "TYPE_UNSPECIFIED",
		1: "TYPE_STRING",
		2: "TYPE_INT",
		3: "TYPE_FLOAT",
		4: "TYPE_BOOL",
		5: "TYPE_STRUCT",
		6: "TYPE_LIST",
		7: "TYPE_EXPR",
		8: "TYPE_ERROR",
	}
	Value_Type_value = map[string]int32{
		"TYPE_UNSPECIFIED": 0,
		"TYPE_STRING":      1,
		"TYPE_INT":         2,
		"TYPE_FLOAT":       3,
		"TYPE_BOOL":        4,
		"TYPE_STRUCT":      5,
		"TYPE_LIST":        6,
		"TYPE_EXPR":        7,
		"TYPE_ERROR":       8,
	}
)

func (x Value_Type) Enum() *Value_Type {
	p := new(Value_Type)
	*p = x
	return p
}

func (x Value_Type) String() string {
	return protoimpl.X.EnumStringOf(x.Descriptor(), protoreflect.EnumNumber(x))
}

func (Value_Type) Descriptor() protoreflect.EnumDescriptor {
	return file_flowstate_v1_flowstate_proto_enumTypes[2].Descriptor()
}

func (Value_Type) Type() protoreflect.EnumType {
	return &file_flowstate_v1_flowstate_proto_enumTypes[2]
}

func (x Value_Type) Number() protoreflect.EnumNumber {
	return protoreflect.EnumNumber(x)
}

// Deprecated: Use Value_Type.Descriptor instead.
func (Value_Type) EnumDescriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{24, 0}
}

type Value_Error_Code int32

const (
	Value_Error_CODE_UNSPECIFIED       Value_Error_Code = 0
	Value_Error_CODE_INVALID_ARGUMENT  Value_Error_Code = 1
	Value_Error_CODE_NOT_FOUND         Value_Error_Code = 2
	Value_Error_CODE_PERMISSION_DENIED Value_Error_Code = 3
	Value_Error_CODE_UNAUTHENTICATED   Value_Error_Code = 4
	Value_Error_CODE_INTERNAL          Value_Error_Code = 5
	Value_Error_CODE_UNAVAILABLE       Value_Error_Code = 6
)

// Enum value maps for Value_Error_Code.
var (
	Value_Error_Code_name = map[int32]string{
		0: "CODE_UNSPECIFIED",
		1: "CODE_INVALID_ARGUMENT",
		2: "CODE_NOT_FOUND",
		3: "CODE_PERMISSION_DENIED",
		4: "CODE_UNAUTHENTICATED",
		5: "CODE_INTERNAL",
		6: "CODE_UNAVAILABLE",
	}
	Value_Error_Code_value = map[string]int32{
		"CODE_UNSPECIFIED":       0,
		"CODE_INVALID_ARGUMENT":  1,
		"CODE_NOT_FOUND":         2,
		"CODE_PERMISSION_DENIED": 3,
		"CODE_UNAUTHENTICATED":   4,
		"CODE_INTERNAL":          5,
		"CODE_UNAVAILABLE":       6,
	}
)

func (x Value_Error_Code) Enum() *Value_Error_Code {
	p := new(Value_Error_Code)
	*p = x
	return p
}

func (x Value_Error_Code) String() string {
	return protoimpl.X.EnumStringOf(x.Descriptor(), protoreflect.EnumNumber(x))
}

func (Value_Error_Code) Descriptor() protoreflect.EnumDescriptor {
	return file_flowstate_v1_flowstate_proto_enumTypes[3].Descriptor()
}

func (Value_Error_Code) Type() protoreflect.EnumType {
	return &file_flowstate_v1_flowstate_proto_enumTypes[3]
}

func (x Value_Error_Code) Number() protoreflect.EnumNumber {
	return protoreflect.EnumNumber(x)
}

// Deprecated: Use Value_Error_Code.Descriptor instead.
func (Value_Error_Code) EnumDescriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{24, 0, 0}
}

// Level is how much a message matters.
//
// Three, and no more without a reason: `debug` is a *deployment* concern rather
// than an authoring one — a workflow author cannot know what a given operator
// wants filtered — and `fatal` is a claim about control flow that a task has no
// way to honour, since emitting a line does not stop the run.
type Task_Log_Level int32

const (
	// Unspecified reads as LEVEL_INFO, so a `log:` that says nothing about level
	// is the ordinary case rather than an error.
	Task_Log_LEVEL_UNSPECIFIED Task_Log_Level = 0
	Task_Log_LEVEL_INFO        Task_Log_Level = 1
	Task_Log_LEVEL_WARN        Task_Log_Level = 2
	Task_Log_LEVEL_ERROR       Task_Log_Level = 3
)

// Enum value maps for Task_Log_Level.
var (
	Task_Log_Level_name = map[int32]string{
		0: "LEVEL_UNSPECIFIED",
		1: "LEVEL_INFO",
		2: "LEVEL_WARN",
		3: "LEVEL_ERROR",
	}
	Task_Log_Level_value = map[string]int32{
		"LEVEL_UNSPECIFIED": 0,
		"LEVEL_INFO":        1,
		"LEVEL_WARN":        2,
		"LEVEL_ERROR":       3,
	}
)

func (x Task_Log_Level) Enum() *Task_Log_Level {
	p := new(Task_Log_Level)
	*p = x
	return p
}

func (x Task_Log_Level) String() string {
	return protoimpl.X.EnumStringOf(x.Descriptor(), protoreflect.EnumNumber(x))
}

func (Task_Log_Level) Descriptor() protoreflect.EnumDescriptor {
	return file_flowstate_v1_flowstate_proto_enumTypes[4].Descriptor()
}

func (Task_Log_Level) Type() protoreflect.EnumType {
	return &file_flowstate_v1_flowstate_proto_enumTypes[4]
}

func (x Task_Log_Level) Number() protoreflect.EnumNumber {
	return protoreflect.EnumNumber(x)
}

// Deprecated: Use Task_Log_Level.Descriptor instead.
func (Task_Log_Level) EnumDescriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{31, 0, 0}
}

type RunResponse_Status int32

const (
	RunResponse_STATUS_UNSPECIFIED RunResponse_Status = 0
	RunResponse_STATUS_RUNNING     RunResponse_Status = 1
	RunResponse_STATUS_COMPLETED   RunResponse_Status = 2
	RunResponse_STATUS_FAILED      RunResponse_Status = 3
	RunResponse_STATUS_CANCELED    RunResponse_Status = 4
	RunResponse_STATUS_TERMINATED  RunResponse_Status = 5
	RunResponse_STATUS_TIMED_OUT   RunResponse_Status = 6
)

// Enum value maps for RunResponse_Status.
var (
	RunResponse_Status_name = map[int32]string{
		0: "STATUS_UNSPECIFIED",
		1: "STATUS_RUNNING",
		2: "STATUS_COMPLETED",
		3: "STATUS_FAILED",
		4: "STATUS_CANCELED",
		5: "STATUS_TERMINATED",
		6: "STATUS_TIMED_OUT",
	}
	RunResponse_Status_value = map[string]int32{
		"STATUS_UNSPECIFIED": 0,
		"STATUS_RUNNING":     1,
		"STATUS_COMPLETED":   2,
		"STATUS_FAILED":      3,
		"STATUS_CANCELED":    4,
		"STATUS_TERMINATED":  5,
		"STATUS_TIMED_OUT":   6,
	}
)

func (x RunResponse_Status) Enum() *RunResponse_Status {
	p := new(RunResponse_Status)
	*p = x
	return p
}

func (x RunResponse_Status) String() string {
	return protoimpl.X.EnumStringOf(x.Descriptor(), protoreflect.EnumNumber(x))
}

func (RunResponse_Status) Descriptor() protoreflect.EnumDescriptor {
	return file_flowstate_v1_flowstate_proto_enumTypes[5].Descriptor()
}

func (RunResponse_Status) Type() protoreflect.EnumType {
	return &file_flowstate_v1_flowstate_proto_enumTypes[5]
}

func (x RunResponse_Status) Number() protoreflect.EnumNumber {
	return protoreflect.EnumNumber(x)
}

// Deprecated: Use RunResponse_Status.Descriptor instead.
func (RunResponse_Status) EnumDescriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{33, 0}
}

// Workflows in Flowstate is a collection of steps that can be executed in a
// specific order. Each step within a workflow can have inputs and outputs,
// and can be a task that performs some action. The workflow can be run and the
// outputs of each step are collected in a structured way that allows for subsequent
// steps to access the outputs of previous steps.
type Workflow struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Workflow is identified by a unique name.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Workflows can have an optional description that provides additional context or
	// information about the workflow itself that may be useful for users or developers.
	Description *string `protobuf:"bytes,2,opt,name=description,proto3,oneof" json:"description,omitempty"`
	// Steps are the individual tasks or operations that make up the workflow.
	// Each step is represented by a Node, which can be a task or another type of
	// operation. The steps are executed in the order they are defined in the workflow.
	// Each step can have its own inputs and outputs, allowing for complex workflows
	// that can perform a variety of operations.
	Steps []*Node `protobuf:"bytes,3,rep,name=steps,proto3" json:"steps,omitempty"`
	// Labels are key-value pairs that can be used to organize and categorize workflows.
	Labels map[string]string `protobuf:"bytes,5,rep,name=labels,proto3" json:"labels,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Profile names the vocabulary every expression in this workflow was compiled
	// against — which CEL extension libraries are in scope.
	//
	// Set by the compiler, not written by an author. There is deliberately no
	// `profile:` key: an author choosing a vocabulary per file is how a file came to
	// have two of them, which is what one profile removes.
	//
	// Recorded rather than resolved at execution because a run has to keep meaning
	// what it meant. A future build that adds a library must not change how an
	// expression already stored in `RunState` evaluates — invariant 10 — and
	// "whatever this worker has" is a set that changes underfoot. A profile name has
	// frozen membership, so a worker resolves the name rather than asking what it
	// ships with, and refuses a name it does not know.
	//
	// A different axis from `edition:`, and the distinction is load-bearing. An
	// edition is a property of a *file*: read when it compiles, gone afterwards, and
	// retiring one affects nothing already executing. A profile is a property of a
	// *run*, and travels.
	//
	// Empty means a spec compiled before this field existed, which resolves to the
	// first profile — a compatibility arm rather than a guess, since nothing has been
	// released and such a spec can only have come from a build whose one vocabulary
	// was that profile's.
	Profile string `protobuf:"bytes,6,opt,name=profile,proto3" json:"profile,omitempty"`
	// Vars are named expressions evaluated once at the start of the run, visible to
	// every step as `vars.<name>`.
	//
	// They exist because the alternative is repetition: a value derived once and used
	// in six places is written six times today, and the six copies drift. A `cel` step
	// can compute one, but then it is a step — it occupies the step budget, appears in
	// the run's outputs, and has to be ordered before everything that reads it.
	//
	// # Rooted, not bare
	//
	// `vars.<name>` rather than `<name>`, which is the same decision `steps.<id>` made
	// and for the same reason: rooting makes a collision between a var and a step id
	// *unrepresentable* rather than something a validation rule has to forbid. What
	// stays bare is only what is bound where the expression is written — a `for_each`
	// iterator, `now` inside `wait_until:`, the names a task resolves against its own
	// scope. Those are bare bindings; see [Scope.vars].
	//
	// # Two sites, and only this one is rooted
	//
	// `vars:` is also accepted on a step, where it means something deliberately
	// different: a step's vars are *bare* within that step (`${modified}`), because
	// they are author-chosen and lexically local — the same standing as a loop
	// binding. Only the workflow-level ones are ambient, so only these are rooted.
	// See [Scope.vars] for where a step's land.
	//
	// # Shadowing is refused, not resolved
	//
	// A bare name must be unique against every bare name in scope: a step var may not
	// collide with an enclosing loop binding, a loop binding may not collide with an
	// enclosing step var, and neither may be `now`. `flow validate` reports the
	// collision at the inner declaration. Silent shadowing is how `${body}` comes to
	// mean two things eleven lines apart.
	//
	// Nothing needs to be refused *here*, at the workflow level, because these are
	// rooted: `vars.region` cannot collide with anything, which is the whole reason
	// the ambient half of the namespace is rooted at all.
	Vars map[string]*Value `protobuf:"bytes,7,rep,name=vars,proto3" json:"vars,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// DeclaredInputs are the parameters a run may be started with, each declared
	// with a name and a type.
	//
	// A workflow that cannot be parameterized has to be *edited* to be reused: the
	// same file with one URL changed becomes a second file, and the second one
	// drifts from the first. These are what let one file serve both runs, which is
	// also what makes a workflow something an agent or a schedule can invoke rather
	// than something a person forks.
	//
	// # Declarations, not a map
	//
	// Field 4 was `map<string, Value> inputs` and is reserved above, which is most
	// of the argument already: a bare map is a shape held open, and every consumer
	// of it has to invent the same missing half for itself. The consumers that want
	// this want *different* halves — the language server type-checks an expression
	// against the environment a run will actually have, `flow run --input k=v` has
	// to turn a command line's strings into typed values, a schedule binds its
	// arguments once and statically, and a Nexus operation has a signature — and
	// not one of them can be built over a map whose values are whatever arrived.
	// Declaring the shape once, here, is what lets all four read the same answer
	// instead of drifting apart holding four guesses.
	//
	// # Named for what it holds
	//
	// Not `inputs`. That name is reserved above by *name* as well as by number, for
	// a reason that has not expired: there is no custom data converter, so a
	// specification is stored in history through ProtoJSON, by field name, and an
	// `inputs` of a new shape would be read out of an old history as the old one.
	//
	// The name is honest anyway, because these are not the values. What a caller
	// passes is [RunRequest.inputs]; what the specification carries is the
	// declaration of what may be passed. A run carries both, they are different
	// things, and they are wanted at different moments — the declaration when the
	// file is written and type-checked, the values when a run is submitted.
	//
	// # Size (invariant 9)
	//
	// These ride in the specification, so `CheckSpecSize` weighs them at submit and
	// `CheckRunStateSize` weighs them again through [RunState.workflow] at every
	// Continue-As-New. Both call `proto.Size` on the whole message rather than
	// summing the fields they know about, so a field added here is bounded on the
	// day it is added and nobody has to remember to count it.
	DeclaredInputs []*InputDeclaration `protobuf:"bytes,8,rep,name=declared_inputs,json=declaredInputs,proto3" json:"declared_inputs,omitempty"`
	// DeclaredOutputs are the values a finished run reports: named expressions,
	// evaluated once the steps are done.
	//
	// Nothing carries a value out of a run today. `vars:` names a value inside the
	// file, and the retirement edition took away `cel:`, which was the one step
	// whose purpose was to shape a final answer — so a run's result is now the
	// union of whatever its steps happened to produce. That is a transcript, not a
	// result: whoever reads the run has to already know which step's which output
	// was the point, which is exactly the knowledge a caller on the other side of
	// an RPC does not have.
	//
	// # Why the computed values are a different message
	//
	// These are declarations and travel with the specification; what a particular
	// run computed is [RunOutputs] and belongs to that run. Widening one message to
	// carry both would make every reader check which half it was holding, and would
	// put an expression and its result in the same field — the shape that makes it
	// possible to confuse "what this workflow promises" with "what this run
	// returned". Two messages also keep the addition additive on both sides: a
	// field added to either is free, where widening either would be breaking.
	//
	// The expression is evaluated by the run, at the end, in the run's own scope —
	// it is not a value a caller supplies, which is the mirror image of the rule on
	// [RunRequest.inputs] and worth reading beside it. Which names that scope holds
	// (`steps`, `vars`, `inputs`) is the compiler's business, not the schema's.
	DeclaredOutputs []*OutputDeclaration `protobuf:"bytes,9,rep,name=declared_outputs,json=declaredOutputs,proto3" json:"declared_outputs,omitempty"`
	// Triggers are the ways this workload is *meant* to start other than somebody
	// asking for it, declared by the file and acted on by nobody until a person
	// says so.
	//
	// # Why a declaration lives in the specification at all
	//
	// A schedule is arguably a property of a deployment rather than of a file: two
	// environments running the same workflow will want different cadences, and the
	// artifact that records "this exists and fires at 09:00" is a Temporal
	// Schedule, which lives in a cluster and not in a repository. All of that is
	// true, and none of it makes the *intent* a deployment's. "This report is a
	// daily report" is a fact about the workload, written by whoever wrote the
	// steps, and reviewed with them.
	//
	// So the file declares and the operator creates, and the two are different
	// acts. Nothing here starts anything: `Run` ignores this field entirely, and
	// `flow run` does not create a schedule, because a file that begins running on
	// its own the moment it merges is a surprise — and a surprise with a cost, since
	// the first firing is indistinguishable from somebody having deliberately
	// started it. Creating one is `CreateSchedule`, where a human is present to be
	// refused.
	//
	// # Why it travels with the workflow rather than sitting in the request
	//
	// The alternative was a schedule spec on [CreateScheduleRequest] alone, keeping
	// the specification free of anything that does not execute. It was rejected for
	// costing more than it saves: the Flowfile compiles to this message and nothing
	// else, so a `triggers:` block with no field would either be dropped on the
	// floor — the shape `flow fix`'s history says never to build — or would need a
	// second compilation output that only one command reads, and `flow compile`
	// would then answer with less than the file says.
	//
	// Carried into a run's history as part of the specification, which costs a few
	// bytes per run and is weighed by the same `CheckSpecSize` as everything else
	// (invariant 9). That is the price of one compiled artifact, and it is small.
	Triggers *Triggers `protobuf:"bytes,10,opt,name=triggers,proto3" json:"triggers,omitempty"`
	// Signals declares, per signal name, who may deliver it — see
	// [SignalPolicy]. Absent for a name means today's behavior: any
	// authenticated caller who can address the run in its tenant may deliver
	// that signal, exactly as before this field existed. Authorization is
	// therefore opt-in per signal name, deliberately: failing closed on every
	// existing workflow the day this field appears would turn every signal
	// user's next `flow signal` into a denial for a policy nobody wrote.
	//
	// # Enforced where the signal is accepted, not where it is read
	//
	// A workflow can already gate its own logic on `steps.<wait>.sender`, and
	// that is real — but it is a check the workflow performs, so it is a check
	// the workflow's own author can skip: delete the `if:`, and the gate is
	// gone. This field is read by `FlowstateServer.Signal`, before Temporal
	// ever sees the signal, against the caller's authenticated identity — a
	// rejected sender never reaches the workflow at all, and gets
	// `PermissionDenied` synchronously rather than a signal that was silently
	// dropped or a wait that quietly never resolves. See lifecycle.go's
	// `Signal` handler.
	//
	// # Frozen at submit, like the rest of the specification
	//
	// The whole specification is frozen once a run starts (invariant 10) so
	// that a run means the same thing on its last segment as its first; this
	// field is no exception; it is extracted into the run's tenancy memo
	// alongside the namespace at submit and never re-read from the
	// specification afterward, which is also what makes it cheap to enforce —
	// the same `DescribeWorkflowExecution` that already establishes tenancy
	// for every verb in lifecycle.go answers this too, at no extra Temporal
	// round trip.
	//
	// # Per name, not per wait
	//
	// A signal may legitimately arrive before the wait that will consume it is
	// reached — that is what `PendingSignal` carry exists for — so the server
	// knows the signal's *name* when it must decide, never which wait (if any)
	// is currently parked on it. The policy is keyed by name for exactly that
	// reason: it is the only unit the accepting boundary can actually ask
	// about. A `wait_for_signal:`-local spelling would only be a projection of
	// this map back onto one call site, and two waits on the same name with
	// different local policies would be an ambiguity the compiler would have
	// to refuse — better not to have the ambiguity at all.
	//
	// # Bounded (invariant: bound anything that consumes untrusted input)
	//
	// A hand-built specification can reach `Run` directly, so this is
	// untrusted input like the rest of the spec: at most 64 signal names, each
	// with at most 32 alternative rules ([SignalPolicy.allow]), each rule with
	// at most 16 claim entries. `CheckSpecSize`/`CheckRunStateSize` also weigh
	// the whole message via `proto.Size`, so this is bounded twice over — by
	// shape here, and by total bytes there.
	Signals       map[string]*SignalPolicy `protobuf:"bytes,11,rep,name=signals,proto3" json:"signals,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Workflow) Reset() {
	*x = Workflow{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[0]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Workflow) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Workflow) ProtoMessage() {}

func (x *Workflow) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[0]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Workflow.ProtoReflect.Descriptor instead.
func (*Workflow) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{0}
}

func (x *Workflow) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *Workflow) GetDescription() string {
	if x != nil && x.Description != nil {
		return *x.Description
	}
	return ""
}

func (x *Workflow) GetSteps() []*Node {
	if x != nil {
		return x.Steps
	}
	return nil
}

func (x *Workflow) GetLabels() map[string]string {
	if x != nil {
		return x.Labels
	}
	return nil
}

func (x *Workflow) GetProfile() string {
	if x != nil {
		return x.Profile
	}
	return ""
}

func (x *Workflow) GetVars() map[string]*Value {
	if x != nil {
		return x.Vars
	}
	return nil
}

func (x *Workflow) GetDeclaredInputs() []*InputDeclaration {
	if x != nil {
		return x.DeclaredInputs
	}
	return nil
}

func (x *Workflow) GetDeclaredOutputs() []*OutputDeclaration {
	if x != nil {
		return x.DeclaredOutputs
	}
	return nil
}

func (x *Workflow) GetTriggers() *Triggers {
	if x != nil {
		return x.Triggers
	}
	return nil
}

func (x *Workflow) GetSignals() map[string]*SignalPolicy {
	if x != nil {
		return x.Signals
	}
	return nil
}

// SignalPolicy constrains who may deliver one named signal to a run.
//
// A sender is authorized if it satisfies *any* rule in [allow] — the rules
// are alternatives, not requirements every one of them must meet. See
// [SignalPolicyRule] for what one rule may require.
type SignalPolicy struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Allow lists the alternative rules that may authorize a sender. At least
	// one, or the policy authorizes nobody, which is indistinguishable from a
	// typo and is refused by the compiler rather than accepted as (probably
	// unintended) lockout.
	Allow         []*SignalPolicyRule `protobuf:"bytes,1,rep,name=allow,proto3" json:"allow,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *SignalPolicy) Reset() {
	*x = SignalPolicy{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[1]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *SignalPolicy) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*SignalPolicy) ProtoMessage() {}

func (x *SignalPolicy) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[1]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use SignalPolicy.ProtoReflect.Descriptor instead.
func (*SignalPolicy) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{1}
}

func (x *SignalPolicy) GetAllow() []*SignalPolicyRule {
	if x != nil {
		return x.Allow
	}
	return nil
}

// SignalPolicyRule is one admissible sender, checked against the
// [SignalSender] the server itself attested — never against anything the
// signal's payload claims.
//
// Every field set on a rule must match (an AND); a rule with nothing set
// matches every sender, which defeats the point of writing one, so the
// compiler refuses it. Combine subject *and* claims in one rule to express
// "this identity, and it must also carry this claim" — that is one list
// entry, not two policies an author has to keep in sync by hand.
type SignalPolicyRule struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Subject is an issuer-qualified identity, written as
	// "<issuer>#<subject>", matched exactly against the attested sender's
	// `issuer` and `subject` together.
	//
	// Issuer-qualified rather than subject alone, on purpose: a subject is
	// only unique within its issuer (`auth/principal.go`'s own rule, restated
	// by `examples/approval-gate/workflow.yaml`'s self-approval check for the
	// identical reason), so two identity providers can each mint a "runner"
	// subject that must not be treated as the same caller. A rule keyed on
	// subject alone would authorize the wrong runner's signal under the right
	// name — the exact multi-IdP ambiguity #215 found in `run.identity`
	// comparisons, reproduced here if this field accepted a bare subject. Only
	// the qualified spelling is accepted; there is no subject-only form to be
	// ambiguous about.
	Subject string `protobuf:"bytes,1,opt,name=subject,proto3" json:"subject,omitempty"`
	// Namespace restricts to one tenant, matched exactly against the attested
	// sender's `namespace`.
	//
	// Rarely useful alone — every signal is already confined to its run's own
	// tenant by `authorizeRun`, before this policy is even consulted — but
	// combines with `claims` to express "anyone in this tenant carrying this
	// claim" without naming individual subjects.
	Namespace string `protobuf:"bytes,2,opt,name=namespace,proto3" json:"namespace,omitempty"`
	// Claims are exact-match requirements against the attested sender's
	// claims: every key here must be present on the sender with this value.
	// Empty places no claim requirement, so a rule naming only `subject` is
	// ordinary and common.
	Claims        map[string]string `protobuf:"bytes,3,rep,name=claims,proto3" json:"claims,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *SignalPolicyRule) Reset() {
	*x = SignalPolicyRule{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[2]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *SignalPolicyRule) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*SignalPolicyRule) ProtoMessage() {}

func (x *SignalPolicyRule) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[2]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use SignalPolicyRule.ProtoReflect.Descriptor instead.
func (*SignalPolicyRule) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{2}
}

func (x *SignalPolicyRule) GetSubject() string {
	if x != nil {
		return x.Subject
	}
	return ""
}

func (x *SignalPolicyRule) GetNamespace() string {
	if x != nil {
		return x.Namespace
	}
	return ""
}

func (x *SignalPolicyRule) GetClaims() map[string]string {
	if x != nil {
		return x.Claims
	}
	return nil
}

// InputDeclaration is one parameter a run may be started with: what it is
// called, what a value for it must be, whether it may be left out, and what it
// is for.
//
// # What the schema checks, and what the compiler checks
//
// The schema checks what a single declaration can be wrong about on its own: the
// name is a CEL identifier of bounded length, the type is a defined value other
// than unspecified, the description fits. Everything that is a fact about a *set*
// of declarations, or about a name CEL's lexer will not accept, is the
// compiler's — protovalidate rules are per-field and RE2 has no negative
// lookahead, so a rule spelled here that cannot hold is worse than no rule, per
// this file's own history with nineteen of them.
//
// Deferred to the compiler, deliberately and in one list so nobody has to
// rediscover it:
//
//   - Two declarations may not share a name. A repeated field cannot say so.
//   - The names `true`, `false`, `null` and `in` are refused. They are CEL lexer
//     tokens, so `inputs.in` is a syntax error in the grammar itself rather than
//     an unknown field — the same four that are refused as step ids, and for the
//     same reason. The other seventeen CEL reserved words are fine here (see
//     `name` below).
//   - A `default` must be a literal, not an expression and not a secret
//     reference. [Value] is a oneof and protovalidate rules attach to fields, so
//     "which arm" is not something this file can require.
//   - A required input with a default is a contradiction, and one of the two is
//     a mistake worth naming rather than resolving by precedence.
//   - Whether a submitted value matches the declared type, which is the check
//     at submit — see [RunRequest.inputs].
type InputDeclaration struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is what the input is called, and how an expression reaches it:
	// `${inputs.<name>}`.
	//
	// Stricter than a step id's pattern, which takes dashes for historical latitude
	// and then has the compiler refuse the ones CEL cannot read. There is no
	// history here, so the boundary refuses them outright: this pattern *is* a CEL
	// identifier.
	//
	// Rooting under `inputs` is what keeps the rest of the question small, the same
	// way `steps.<id>` did (invariant 2). A name here is a field selection rather
	// than an identifier, so seventeen of CEL's twenty-one reserved words are legal
	// — `namespace`, `loop`, `if` and the rest — and only the four lexer tokens are
	// not. Nothing needs refusing against `steps`, `vars` or `now` either: an input
	// called `steps` is `inputs.steps`, which cannot collide with anything, which is
	// the entire reason ambient namespaces get a root.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Type is what a submitted value must be, checked at submit rather than where
	// the value is first read. A run that is going to be refused for a bad argument
	// should be refused while the caller is still there to be told, not three steps
	// in with two requests already sent.
	Type InputDeclaration_Type `protobuf:"varint,2,opt,name=type,proto3,enum=flowstate.v1.InputDeclaration_Type" json:"type,omitempty"`
	// Required reports that a run cannot start without this input.
	//
	// The default is false, which means optional, which is the safe direction for a
	// field being added to a schema: a declaration written before anyone thought
	// about optionality does not silently start refusing runs. Fail-closed lives at
	// the other end of this surface — an *undeclared* input is refused, and a value
	// of the wrong type is refused — where the thing being denied is something a
	// caller sent rather than something an author forgot.
	Required bool `protobuf:"varint,3,opt,name=required,proto3" json:"required,omitempty"`
	// Default is the value used when the caller passes nothing.
	//
	// A [Value] rather than a string, so that a default for an int input is an int
	// and nobody writes a second coercion path for defaults alongside the one for
	// submitted values. It must be a literal: an expression here would be evaluated
	// somewhere, and the only place with a scope to evaluate it in is the run,
	// which would make a default a different kind of thing from the value it stands
	// in for. The compiler enforces that, per the list on the message above.
	Default *Value `protobuf:"bytes,4,opt,name=default,proto3" json:"default,omitempty"`
	// Description is prose for whoever is supplying the value — a person reading
	// `flow run --help`, an agent choosing arguments, an editor's hover. It is the
	// only part of a declaration written *for the caller* rather than for the
	// machine, which is why it is worth carrying rather than leaving in a comment
	// the compiler throws away.
	Description *string `protobuf:"bytes,5,opt,name=description,proto3,oneof" json:"description,omitempty"`
	// Example is an illustrative value for this input, never applied at runtime.
	//
	// `default` and `example` used to be one field, because `default` was the only
	// way to make a workflow runnable unattended — so a required-feeling input like
	// "who is asking" ended up with a specific person as its default, which is
	// incoherent: there is no sensible absent value for who is asking, only a
	// plausible one to paste. The two are different things. A default is applied
	// when a caller supplies nothing, and is legitimate only when absence itself
	// has a meaning (`region: us-east-1`). An example is what a caller unsure what
	// to send would copy, is legal on a required input precisely because a required
	// input cannot have a default, and is checked against this declaration's own
	// type and constraints when the specification compiles — a stale example that
	// no longer satisfies its own `must:` is a diagnostic, not a value nobody
	// notices went wrong. It is never bound to a run; it exists for an editor's
	// hover, a generated doc, and a machine caller's schema.
	Example *Value `protobuf:"bytes,6,opt,name=example,proto3" json:"example,omitempty"`
	// Sensitive marks a value as private but not secret: a salary in an approval, a
	// customer's email — something a run legitimately computes on and that
	// legitimately rides in history, but that should not be casually pasted into a
	// terminal, a log, or an agent's answer.
	//
	// This is display etiquette and nothing more. It is not containment: the value
	// is an ordinary part of the run's history exactly like any other input, and
	// anyone with access to that history reads it in the clear, the same way they
	// read any other input. A secret reference is different: it keeps the value
	// *out* of history entirely, resolving it only inside the activity that needs
	// it — that is containment, and this is not it. Marking sensitive a value that
	// must never reach an operator's screen at all is a mistake; that value is a
	// secret reference, not this.
	//
	// # What this does on an *input*, precisely
	//
	// Declared *outputs* are redacted everywhere Flowstate renders them; an input
	// is rendered on exactly one surface — a schedule's bound arguments, through
	// `flow schedule describe` and `flow schedule list` — and there a
	// sensitive-declared value is redacted the same way, server-side, before it
	// crosses the wire. The redaction lives on the server rather than the client
	// because the server is the only party that holds both the schedule and the
	// spec that declared the sensitivity; a `ScheduleDescription` on the wire
	// carries the already-redacted value, not the raw one for a client to hide.
	//
	// What is not yet built is a reveal path: `flow get` has `--reveal-sensitive`,
	// and the schedule RPCs have no field to carry that request. An operator who
	// needs the value must read it from wherever it was configured, not from a
	// describe. That is a display-surface gap, not a containment one — this flag
	// was never containment (see above).
	Sensitive bool `protobuf:"varint,7,opt,name=sensitive,proto3" json:"sensitive,omitempty"`
	// Pattern is an RE2 regular expression a `type: string` value must fully
	// match. The compiler refuses it on a declaration of any other type, the same
	// way it refuses a `min_items` on one that is not a list — a rule on a type it
	// cannot apply to is worse than no rule, per this schema's own history with
	// rules that could never hold.
	//
	// Named and shaped to match `buf.validate`'s `string.pattern` one for one, so
	// that a constraint written here and one arriving from an imported `.proto`
	// file's own annotations are the same artifact underneath.
	Pattern *string `protobuf:"bytes,8,opt,name=pattern,proto3,oneof" json:"pattern,omitempty"`
	// MinLen and MaxLen bound a `type: string` value's length, counted in runes.
	// Either may be given alone. Named after `buf.validate`'s `string.min_len` and
	// `string.max_len`, for the reason `pattern` above is.
	MinLen *uint64 `protobuf:"varint,9,opt,name=min_len,json=minLen,proto3,oneof" json:"min_len,omitempty"`
	MaxLen *uint64 `protobuf:"varint,10,opt,name=max_len,json=maxLen,proto3,oneof" json:"max_len,omitempty"`
	// Min and Max bound a `type: int` or `type: float` value. Either may be given
	// alone. One field serves both declared types rather than the four
	// `buf.validate` keeps per width, because this schema's numeric inputs are
	// already the two kinds [InputDeclaration.Type] names and a DSL author writes
	// one number regardless of which.
	Min *float64 `protobuf:"fixed64,11,opt,name=min,proto3,oneof" json:"min,omitempty"`
	Max *float64 `protobuf:"fixed64,12,opt,name=max,proto3,oneof" json:"max,omitempty"`
	// MinItems and MaxItems bound a `type: list` value's length. Either may be
	// given alone. Named after `buf.validate`'s `repeated.min_items` and
	// `repeated.max_items`.
	MinItems *uint64 `protobuf:"varint,13,opt,name=min_items,json=minItems,proto3,oneof" json:"min_items,omitempty"`
	MaxItems *uint64 `protobuf:"varint,14,opt,name=max_items,json=maxItems,proto3,oneof" json:"max_items,omitempty"`
	// Unique reports that a `type: list` value's elements must all differ from one
	// another, compared the way two [Value] literals compare for equality. Named
	// after `buf.validate`'s `repeated.unique`. The compiler refuses it on a
	// declaration of any other type.
	Unique bool `protobuf:"varint,15,opt,name=unique,proto3" json:"unique,omitempty"`
	// Must is a CEL predicate over `this`, the value being checked, for a rule the
	// declarative keys above cannot state — the escape hatch `buf.validate` itself
	// keeps behind its own standard-rule vocabulary, and for the identical reason:
	// making an author spell `size(this) > 0 && size(this) <= 100` for every
	// bounded list is the ergonomic failure the keys above exist to prevent, so
	// this exists only for what they genuinely cannot say.
	//
	// Compiled and type-checked when the specification loads — not when a run
	// submits one — so a bad expression is a defect in the specification itself,
	// caught the same way a mistyped default is. Evaluated under this schema's
	// standard CEL cost bound at every point a value is checked against it, because
	// a specification (and the `must:` inside it) can arrive over the RPC and is
	// therefore untrusted input like any other stored expression here. It may not
	// reference `now` or call anything nondeterministic: a constraint has to answer
	// the same way on every replay and at every one of this declaration's
	// enforcement points, which an expression reading the clock cannot promise.
	Must          *string `protobuf:"bytes,16,opt,name=must,proto3,oneof" json:"must,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *InputDeclaration) Reset() {
	*x = InputDeclaration{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[3]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *InputDeclaration) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*InputDeclaration) ProtoMessage() {}

func (x *InputDeclaration) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[3]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use InputDeclaration.ProtoReflect.Descriptor instead.
func (*InputDeclaration) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{3}
}

func (x *InputDeclaration) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *InputDeclaration) GetType() InputDeclaration_Type {
	if x != nil {
		return x.Type
	}
	return InputDeclaration_TYPE_UNSPECIFIED
}

func (x *InputDeclaration) GetRequired() bool {
	if x != nil {
		return x.Required
	}
	return false
}

func (x *InputDeclaration) GetDefault() *Value {
	if x != nil {
		return x.Default
	}
	return nil
}

func (x *InputDeclaration) GetDescription() string {
	if x != nil && x.Description != nil {
		return *x.Description
	}
	return ""
}

func (x *InputDeclaration) GetExample() *Value {
	if x != nil {
		return x.Example
	}
	return nil
}

func (x *InputDeclaration) GetSensitive() bool {
	if x != nil {
		return x.Sensitive
	}
	return false
}

func (x *InputDeclaration) GetPattern() string {
	if x != nil && x.Pattern != nil {
		return *x.Pattern
	}
	return ""
}

func (x *InputDeclaration) GetMinLen() uint64 {
	if x != nil && x.MinLen != nil {
		return *x.MinLen
	}
	return 0
}

func (x *InputDeclaration) GetMaxLen() uint64 {
	if x != nil && x.MaxLen != nil {
		return *x.MaxLen
	}
	return 0
}

func (x *InputDeclaration) GetMin() float64 {
	if x != nil && x.Min != nil {
		return *x.Min
	}
	return 0
}

func (x *InputDeclaration) GetMax() float64 {
	if x != nil && x.Max != nil {
		return *x.Max
	}
	return 0
}

func (x *InputDeclaration) GetMinItems() uint64 {
	if x != nil && x.MinItems != nil {
		return *x.MinItems
	}
	return 0
}

func (x *InputDeclaration) GetMaxItems() uint64 {
	if x != nil && x.MaxItems != nil {
		return *x.MaxItems
	}
	return 0
}

func (x *InputDeclaration) GetUnique() bool {
	if x != nil {
		return x.Unique
	}
	return false
}

func (x *InputDeclaration) GetMust() string {
	if x != nil && x.Must != nil {
		return *x.Must
	}
	return ""
}

// OutputDeclaration is one value a finished run reports: a name, and the
// expression that produces it.
//
// The expression is evaluated by the run, in the run's own scope, after its steps
// have finished — so this is the one place in the schema where an author's
// expression is deliberately what is stored, as against [RunRequest.inputs] where
// an expression is deliberately what is refused. The two rules point the same
// way: an expression may come from the file, which is reviewed and compiled, and
// never from a caller.
//
// Deferred to the compiler for the same reasons as [InputDeclaration]: that two
// declarations do not share a name, and that the expression's references resolve
// against the scope a finished run actually has.
type OutputDeclaration struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is what the value is called in the run's result.
	//
	// Same pattern as an input's, and for a weaker reason — nothing selects an
	// output through CEL today. It is held to the same shape anyway so that the two
	// halves of one contract cannot disagree about what a name is, and so that a
	// later `${outputs.<name>}` or a cross-file signature check is not a break.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Value is the expression evaluated to produce this output.
	Value *Value `protobuf:"bytes,2,opt,name=value,proto3" json:"value,omitempty"`
	// Description is prose for whoever reads the result.
	Description *string `protobuf:"bytes,3,opt,name=description,proto3,oneof" json:"description,omitempty"`
	// Must is a CEL predicate over `this`, the value this output's own expression
	// produced, checked once that expression has been evaluated — so a workflow
	// cannot report a value that violates its own contract, which is what makes
	// consuming a library workflow's result safe. Same rules as
	// [InputDeclaration.must]: compiled and type-checked when the specification
	// loads, evaluated under the standard CEL cost bound, and refused if it
	// references `now` or calls anything else nondeterministic.
	Must *string `protobuf:"bytes,4,opt,name=must,proto3,oneof" json:"must,omitempty"`
	// Sensitive marks this output's value the way [InputDeclaration.sensitive]
	// marks an input's — display etiquette, never containment. See that field's
	// doc comment for the honesty this comes with; nothing about the two differs
	// beyond which side of a run they describe.
	Sensitive     bool `protobuf:"varint,5,opt,name=sensitive,proto3" json:"sensitive,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *OutputDeclaration) Reset() {
	*x = OutputDeclaration{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[4]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *OutputDeclaration) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*OutputDeclaration) ProtoMessage() {}

func (x *OutputDeclaration) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[4]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use OutputDeclaration.ProtoReflect.Descriptor instead.
func (*OutputDeclaration) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{4}
}

func (x *OutputDeclaration) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *OutputDeclaration) GetValue() *Value {
	if x != nil {
		return x.Value
	}
	return nil
}

func (x *OutputDeclaration) GetDescription() string {
	if x != nil && x.Description != nil {
		return *x.Description
	}
	return ""
}

func (x *OutputDeclaration) GetMust() string {
	if x != nil && x.Must != nil {
		return *x.Must
	}
	return ""
}

func (x *OutputDeclaration) GetSensitive() bool {
	if x != nil {
		return x.Sensitive
	}
	return false
}

// RunOutputs is what one run computed for its declared outputs.
//
// Its own message rather than a widening of [Workflow.StepOutputs], which is a
// different question with a similar shape: step outputs are every value the run
// produced, keyed by the step that produced it, and these are the few values the
// *workflow* said it would report. A caller wanting the answer should not have to
// know the run's internal structure to find it, and a reader of either message
// should never have to ask which of the two it is holding.
//
// Carried in [RunState] as well as returned, because a run that suspends has to
// keep what it has computed; and weighed there by `CheckRunStateSize`, which
// measures the whole message (invariant 9).
type RunOutputs struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Values are the declared outputs, keyed by the name their declaration gave.
	//
	// A map rather than a repeated message: order is a property of the declaration
	// list, which is already ordered, and a result is looked up by name.
	Values        map[string]*Value `protobuf:"bytes,1,rep,name=values,proto3" json:"values,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *RunOutputs) Reset() {
	*x = RunOutputs{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[5]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *RunOutputs) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*RunOutputs) ProtoMessage() {}

func (x *RunOutputs) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[5]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use RunOutputs.ProtoReflect.Descriptor instead.
func (*RunOutputs) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{5}
}

func (x *RunOutputs) GetValues() map[string]*Value {
	if x != nil {
		return x.Values
	}
	return nil
}

// Triggers is what a workflow says about starting itself.
//
// A message with one field per kind of trigger rather than a oneof, because these
// are not alternatives: a workload that runs nightly *and* on a webhook is an
// ordinary thing to want, and a oneof would make expressing it a schema change
// rather than a second key. Adding a kind is then a new field here, which is
// additive on the wire and additive in the grammar — `triggers:` gains a key and
// every existing file still means what it meant.
//
// Its own message rather than fields on [Workflow] for the same reason the DSL
// spells it `triggers: { schedule: ... }`: a reader meets one block that answers
// "how does this start", and a second kind does not scatter a third and fourth
// top-level key across the file.
type Triggers struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Schedule is the cadence this workload is meant to run at.
	//
	// Optional and, when present, still inert until somebody creates it. See
	// [Workflow.triggers] for why the declaration and the act are separate.
	Schedule      *ScheduleTrigger `protobuf:"bytes,1,opt,name=schedule,proto3" json:"schedule,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Triggers) Reset() {
	*x = Triggers{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[6]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Triggers) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Triggers) ProtoMessage() {}

func (x *Triggers) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[6]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Triggers.ProtoReflect.Descriptor instead.
func (*Triggers) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{6}
}

func (x *Triggers) GetSchedule() *ScheduleTrigger {
	if x != nil {
		return x.Schedule
	}
	return nil
}

// ScheduleTrigger says when a workload should run.
//
// # A projection of Temporal's ScheduleSpec, not a reimplementation
//
// Temporal has first-class Schedules, with a spec covering cron expressions,
// intervals, calendars, skips, jitter, time zones, start and end bounds, and
// policy about what to do when a firing overlaps the last one. Every field here
// is passed to that spec unchanged; nothing in Flowstate computes a next firing
// time, and nothing here is a second implementation of a cron parser at run time.
// What this message is for is giving those parameters a *declarable* spelling —
// the surface, not the mechanism.
//
// # What is here, and what is deliberately not yet
//
// Here: cron expressions, a plain interval, a time zone, jitter, and the overlap
// policy. That set was chosen as the smallest one that answers the questions
// anybody asks before running a schedule in production — when does it fire, in
// whose clock, and what happens if the last one has not finished.
//
// Not here, and each additive later with no break: calendar specs and their
// `skip` exceptions (a cron expression says the same thing and is the spelling
// people arrive already knowing), `start_at`/`end_at` bounds, the catchup window,
// pause-on-failure, a limit on the number of firings, and backfill. Their absence
// is a smaller surface rather than a missing capability — an operator wanting one
// today reaches for the `temporal` CLI against the schedule this created, and the
// day one is added it is a field here and a key in `triggers.schedule`.
//
// # Where the arguments are not
//
// There is deliberately no `inputs:` under a schedule. What a scheduled run is
// started with is bound at creation, from the command that creates it — see
// [CreateScheduleRequest.inputs] — because arguments are a deployment's answer
// (which cluster, which account, which region) while the cadence is the
// workload's. Putting them here would also make the same values expressible in
// two places, since an input declaration already carries a default, and a
// precedence rule between a default and a schedule's override is a rule somebody
// has to learn for no benefit.
type ScheduleTrigger struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Cron holds cron expressions, each naming a set of firing times.
	//
	// Repeated because a cadence like "every weekday at 09:00, and at noon on
	// Sunday" is two expressions and cannot be one — the union is what Temporal
	// takes. The DSL accepts a single string as well as a list, so the common case
	// reads as one line.
	//
	// Temporal accepts 5, 6 or 7 fields, the `@daily`-style shorthands, `@every
	// <interval>`, and an optional `CRON_TZ=`/`TZ=` prefix. `flow validate` checks
	// the shape of what is written here and reports a position; the cluster is the
	// authority on the rest, which is the standing rule for anything a deployment
	// decides.
	Cron []string `protobuf:"bytes,1,rep,name=cron,proto3" json:"cron,omitempty"`
	// Every fires on a fixed interval, measured from the epoch rather than from the
	// last firing — so `every: 1h` is every hour on the hour, not an hour after
	// whenever the previous run happened to end.
	//
	// Beside `cron:` rather than instead of it because the two answer different
	// questions. An interval is what somebody means by "every fifteen minutes"; a
	// cron expression is what they mean by "at 09:00 on weekdays", and expressing
	// either in the other's notation is where the mistakes live.
	Every *durationpb.Duration `protobuf:"bytes,2,opt,name=every,proto3" json:"every,omitempty"`
	// TimeZone is the IANA zone a calendar-based expression is read in, for example
	// `America/New_York`. Empty means UTC.
	//
	// Resolved by the Temporal cluster from the zone database it runs with, not
	// here: a zone name is exactly the kind of thing a deployment answers, and a
	// validator running in an editor would be answering from the author's laptop.
	// What is checked here is that it is a plausible name; whether this cluster has
	// it is the cluster's to say.
	TimeZone string `protobuf:"bytes,3,opt,name=time_zone,json=timeZone,proto3" json:"time_zone,omitempty"`
	// Jitter spreads firings over a window, so a fleet of schedules that all say
	// `0 * * * *` does not become a thundering herd on the hour.
	//
	// Each firing is delayed by a random amount between zero and this, capped by
	// the time until the next firing.
	Jitter *durationpb.Duration `protobuf:"bytes,4,opt,name=jitter,proto3" json:"jitter,omitempty"`
	// Overlap is what happens when a firing comes due with the last run still
	// going. Unset takes Temporal's default, which is to skip.
	Overlap       ScheduleTrigger_Overlap `protobuf:"varint,5,opt,name=overlap,proto3,enum=flowstate.v1.ScheduleTrigger_Overlap" json:"overlap,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ScheduleTrigger) Reset() {
	*x = ScheduleTrigger{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[7]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ScheduleTrigger) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ScheduleTrigger) ProtoMessage() {}

func (x *ScheduleTrigger) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[7]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ScheduleTrigger.ProtoReflect.Descriptor instead.
func (*ScheduleTrigger) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{7}
}

func (x *ScheduleTrigger) GetCron() []string {
	if x != nil {
		return x.Cron
	}
	return nil
}

func (x *ScheduleTrigger) GetEvery() *durationpb.Duration {
	if x != nil {
		return x.Every
	}
	return nil
}

func (x *ScheduleTrigger) GetTimeZone() string {
	if x != nil {
		return x.TimeZone
	}
	return ""
}

func (x *ScheduleTrigger) GetJitter() *durationpb.Duration {
	if x != nil {
		return x.Jitter
	}
	return nil
}

func (x *ScheduleTrigger) GetOverlap() ScheduleTrigger_Overlap {
	if x != nil {
		return x.Overlap
	}
	return ScheduleTrigger_OVERLAP_UNSPECIFIED
}

// Node represents a step in the workflow. Each node can be a task or another type of operation.
// Nodes can have outputs that are structured as named values, allowing for complex workflows
// where the outputs of one node can be used as inputs to another node.
type Node struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Each node has a unique identifier within the workflow. This ID is used to reference the
	// node in the workflow's step outputs and can be used to access the outputs of the
	// node after the workflow has been executed.
	Id string `protobuf:"bytes,1,opt,name=id,proto3" json:"id,omitempty"`
	// A node must be a specific kind of operation.
	//
	// Types that are valid to be assigned to Kind:
	//
	//	*Node_Task
	//	*Node_ForEach
	//	*Node_Parallel
	//	*Node_Wait
	//	*Node_Call
	//	*Node_Loop
	Kind isNode_Kind `protobuf_oneof:"kind"`
	// Condition determines whether the step runs.
	//
	// It is evaluated before the step is scheduled and must produce a boolean. A
	// step whose condition is false is skipped and produces no outputs, so a later
	// step referencing it will not resolve — which is the honest outcome, since
	// the value genuinely does not exist.
	//
	// Unset means the step always runs.
	Condition *Value `protobuf:"bytes,3,opt,name=condition,proto3" json:"condition,omitempty"`
	// Policy controls how the step is executed: how long it may take, whether and
	// how it is retried, and whether its failure ends the run.
	//
	// Unset means the engine's defaults apply.
	Policy *StepPolicy `protobuf:"bytes,4,opt,name=policy,proto3" json:"policy,omitempty"`
	// Description is optional prose about what this step is for.
	//
	// It belongs to the step rather than to the task the step runs, which is where
	// it used to live. Once a step names its task directly, the value under `echo:`
	// is that task's inputs, so a `description` written there would be an input of
	// that name; and a `for_each` or a `sleep` is just as worth explaining as a
	// task is. The step is the thing an author is describing either way.
	Description *string `protobuf:"bytes,8,opt,name=description,proto3,oneof" json:"description,omitempty"`
	// Vars are names this step binds for itself, read *bare* inside it: `${modified}`.
	//
	// Bare rather than rooted, which is the opposite of [Workflow.vars] and deliberate.
	// The rule is not about which block a name came from but about where it is bound:
	// an author-chosen name bound lexically, close enough to read the binding and the
	// use together, stays bare — the same standing a `for_each` binding has. A name
	// that is simply in the air everywhere gets a root, because that is the only thing
	// that keeps it from colliding with what an author chose.
	//
	// Evaluated after the step's condition and before its inputs, in the step's own
	// scope: it may read `vars.<name>`, the outputs of steps already run, and any
	// binding it sits inside. So unlike a workflow var it *can* see the run.
	//
	// # Collisions are refused, not resolved
	//
	// Every name here is bare and so can collide with another bare name — an enclosing
	// loop's binding, a step var further out, `now`. That is refused at validation, at
	// the inner declaration, rather than resolved by a precedence rule. Silent
	// shadowing is how `${body}` comes to mean two things eleven lines apart, and a
	// precedence rule is a thing every reader has to know before they can read the
	// second one correctly.
	//
	// By the time a spec reaches the engine there is therefore nothing to decide, which
	// is why [Scope.vars] holds these and a loop's binding in one map.
	Vars map[string]*Value `protobuf:"bytes,9,rep,name=vars,proto3" json:"vars,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Undo is how this step is taken back when a *later* step fails and the run
	// cannot continue — the saga compensation for whatever this step did.
	//
	// It hangs off the step rather than being a handler list at the workflow level
	// because the undo and the do are one decision. The thing to be deleted is named
	// by the outputs of the step that created it, so a list somewhere else would have
	// to reach back across the file for every value it needs, and a reviewer would
	// have to read two places to answer "does this get cleaned up". One place, and
	// one fewer control-flow construct (DSL.md's rule 10).
	//
	// Field 10 rather than an arm of the `kind` oneof, because a compensation is not
	// a kind of work a step does. It is a property of a step that does some other
	// kind, exactly as `policy` and `condition` are.
	Undo          *Compensation `protobuf:"bytes,10,opt,name=undo,proto3" json:"undo,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Node) Reset() {
	*x = Node{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[8]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Node) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Node) ProtoMessage() {}

func (x *Node) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[8]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Node.ProtoReflect.Descriptor instead.
func (*Node) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{8}
}

func (x *Node) GetId() string {
	if x != nil {
		return x.Id
	}
	return ""
}

func (x *Node) GetKind() isNode_Kind {
	if x != nil {
		return x.Kind
	}
	return nil
}

func (x *Node) GetTask() *Task {
	if x != nil {
		if x, ok := x.Kind.(*Node_Task); ok {
			return x.Task
		}
	}
	return nil
}

func (x *Node) GetForEach() *ForEach {
	if x != nil {
		if x, ok := x.Kind.(*Node_ForEach); ok {
			return x.ForEach
		}
	}
	return nil
}

func (x *Node) GetParallel() *Parallel {
	if x != nil {
		if x, ok := x.Kind.(*Node_Parallel); ok {
			return x.Parallel
		}
	}
	return nil
}

func (x *Node) GetWait() *Wait {
	if x != nil {
		if x, ok := x.Kind.(*Node_Wait); ok {
			return x.Wait
		}
	}
	return nil
}

func (x *Node) GetCall() *Call {
	if x != nil {
		if x, ok := x.Kind.(*Node_Call); ok {
			return x.Call
		}
	}
	return nil
}

func (x *Node) GetLoop() *Loop {
	if x != nil {
		if x, ok := x.Kind.(*Node_Loop); ok {
			return x.Loop
		}
	}
	return nil
}

func (x *Node) GetCondition() *Value {
	if x != nil {
		return x.Condition
	}
	return nil
}

func (x *Node) GetPolicy() *StepPolicy {
	if x != nil {
		return x.Policy
	}
	return nil
}

func (x *Node) GetDescription() string {
	if x != nil && x.Description != nil {
		return *x.Description
	}
	return ""
}

func (x *Node) GetVars() map[string]*Value {
	if x != nil {
		return x.Vars
	}
	return nil
}

func (x *Node) GetUndo() *Compensation {
	if x != nil {
		return x.Undo
	}
	return nil
}

type isNode_Kind interface {
	isNode_Kind()
}

type Node_Task struct {
	Task *Task `protobuf:"bytes,2,opt,name=task,proto3,oneof"`
}

type Node_ForEach struct {
	ForEach *ForEach `protobuf:"bytes,5,opt,name=for_each,json=forEach,proto3,oneof"`
}

type Node_Parallel struct {
	Parallel *Parallel `protobuf:"bytes,6,opt,name=parallel,proto3,oneof"`
}

type Node_Wait struct {
	Wait *Wait `protobuf:"bytes,7,opt,name=wait,proto3,oneof"`
}

type Node_Call struct {
	Call *Call `protobuf:"bytes,11,opt,name=call,proto3,oneof"`
}

type Node_Loop struct {
	Loop *Loop `protobuf:"bytes,12,opt,name=loop,proto3,oneof"`
}

func (*Node_Task) isNode_Kind() {}

func (*Node_ForEach) isNode_Kind() {}

func (*Node_Parallel) isNode_Kind() {}

func (*Node_Wait) isNode_Kind() {}

func (*Node_Call) isNode_Kind() {}

func (*Node_Loop) isNode_Kind() {}

// Compensation is what undoes one step.
//
// # A task, not a node
//
// The field is a [Task] and not a [Node], and that is the load-bearing choice in
// this message rather than an economy. Three things follow from it, each of which
// would otherwise be a rule somebody has to enforce:
//
//   - A compensation cannot itself be compensated. There is nowhere to write a
//     nested `undo`, so an infinite compensation loop is not something a
//     specification can express — better than a bound on one.
//   - A compensation cannot loop, branch, or wait. Those are the node kinds, and
//     each of them is a position a run could suspend at; compensation runs in the
//     segment that failed and does not suspend (see [RunState.pending_undo]), so a
//     kind that needs a resume position could not honour that.
//   - A compensation has no `id`, so it can never appear in `steps.<id>` and no
//     expression can depend on one having run.
//
// # No policy field, deliberately
//
// A compensation is retried and bounded exactly as a step with no `retry:` and no
// `timeout:` is — the engine's defaults, from the same constants both drivers read
// (`retrydefaults.go`). A `policy` field here would be a bound nothing reaches
// until an author writes one, and CLAUDE.md's rule about those is that they are
// bounds nothing tests. Adding the field later is additive; guessing its semantics
// now is not.
type Compensation struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Task is the work that undoes the step.
	//
	// Its inputs are resolved at the moment the step *succeeds*, not at the moment
	// compensation runs — see [PendingUndo.task] for why that is the whole design.
	Task          *Task `protobuf:"bytes,1,opt,name=task,proto3" json:"task,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Compensation) Reset() {
	*x = Compensation{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[9]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Compensation) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Compensation) ProtoMessage() {}

func (x *Compensation) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[9]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Compensation.ProtoReflect.Descriptor instead.
func (*Compensation) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{9}
}

func (x *Compensation) GetTask() *Task {
	if x != nil {
		return x.Task
	}
	return nil
}

// PendingUndo is one compensation a run has registered and not yet run.
//
// # Resolved on registration, not on failure
//
// The task here carries *values*, not expressions over a scope that may no longer
// exist. A step's `undo:` is resolved the instant that step succeeds, in that
// step's own scope with its own outputs added, and what is stored is the result.
//
// Three problems disappear at once, and each of them is real:
//
//   - Compaction. A run that continues as new carries only the step outputs its
//     *remaining* steps can still reference (`compactOutputsForRemainingSteps`).
//     A deferred compensation expression would be a fifth reference site that walk
//     does not know about, and the failure mode is the one that walk has already
//     had twice: an output pruned, and a run that fails after a handover on a
//     specification that never changed. A resolved value references nothing.
//   - Determinism. Running a compensation schedules an activity and evaluates
//     nothing, so nothing about compensating is new workflow-side work
//     (ARCHITECTURE.md invariant 4).
//   - Meaning. `${steps.provision.id}` inside an undo means the id that step
//     produced, at the moment it produced it. Resolving later would let it mean
//     whatever the scope held after some other step overwrote things.
//
// The cost is stated rather than hidden: what a compensation would have computed
// from the *failure* — which step failed, what it said — is not available to it,
// because at registration time the failure has not happened. That is a deliberate
// narrowing and DSL.md argues it as one.
//
// # Size (ARCHITECTURE.md invariant 9)
//
// These ride in [RunState], so `CheckRunStateSize` weighs them at every
// Continue-As-New along with everything else — it calls `proto.Size` on the whole
// message rather than summing the fields it knows about, so this field was bounded
// on the day it was added and nobody has to remember to count it. A run with an
// undo on every step carries one resolved task per succeeded step, and
// `Workflow.steps` is capped at 100.
type PendingUndo struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// StepId is the step this undoes, for the sentence a failed run reports and for
	// nothing else — nothing dispatches on it.
	StepId string `protobuf:"bytes,1,opt,name=step_id,json=stepId,proto3" json:"step_id,omitempty"`
	// Task is the compensation with its inputs already resolved.
	Task          *Task `protobuf:"bytes,2,opt,name=task,proto3" json:"task,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *PendingUndo) Reset() {
	*x = PendingUndo{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[10]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *PendingUndo) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*PendingUndo) ProtoMessage() {}

func (x *PendingUndo) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[10]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use PendingUndo.ProtoReflect.Descriptor instead.
func (*PendingUndo) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{10}
}

func (x *PendingUndo) GetStepId() string {
	if x != nil {
		return x.StepId
	}
	return ""
}

func (x *PendingUndo) GetTask() *Task {
	if x != nil {
		return x.Task
	}
	return nil
}

// SecretRef names a secret without containing it.
//
// A workflow specification is persisted, replayed, and readable by anyone who can
// read workflow history, so it must never carry a credential. It carries this
// instead: enough to find the value at the moment it is needed, and nothing more.
//
// The scheme and name are separate fields rather than one joined string so that
// the schema itself rejects a malformed reference at the boundary, before any
// worker tries to resolve it.
//
// This message must never gain a field holding a value. Adding one would make
// every workflow that used it leak a credential into history, and the mistake
// would be invisible until someone read the history.
type SecretRef struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Scheme selects the provider that resolves the reference, such as "env",
	// "file", "vault", or "keychain".
	//
	// A scheme is how a secrets backend plugs in: a deployment registers providers
	// for the schemes it permits, and a reference to an unregistered scheme is
	// refused rather than guessed at. A deployment that permits no secrets at all
	// registers none, and every reference fails.
	Scheme string `protobuf:"bytes,1,opt,name=scheme,proto3" json:"scheme,omitempty"`
	// Name locates the secret within that provider: an environment variable
	// suffix, a path under a mounted directory, a vault path. Its meaning belongs
	// to the provider, including how it expresses versions.
	//
	// The pattern here cannot express everything that matters — control characters
	// are also rejected, in code, because a reference reaching a log or history
	// must not be able to forge lines in it.
	Name          string `protobuf:"bytes,2,opt,name=name,proto3" json:"name,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *SecretRef) Reset() {
	*x = SecretRef{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[11]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *SecretRef) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*SecretRef) ProtoMessage() {}

func (x *SecretRef) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[11]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use SecretRef.ProtoReflect.Descriptor instead.
func (*SecretRef) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{11}
}

func (x *SecretRef) GetScheme() string {
	if x != nil {
		return x.Scheme
	}
	return ""
}

func (x *SecretRef) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

// Wait suspends a workload until time passes, a condition holds, or a signal
// arrives.
//
// This is the primitive a CI system fundamentally cannot copy. A run that waits
// a week for a human approval and survives every worker restart in between is
// not a long-running process — the process may not exist for most of that week.
// The wait is durable state on the execution substrate, so a worker that is
// redeployed, crashes, or moves machines resumes exactly where it was.
//
// It is a node kind rather than a task, because waiting schedules no activity
// and touches nothing outside the workflow: no worker executes it, no
// credentials are needed, and nothing can fail except the wait itself. Making it
// a task would put an evaluator and an activity round trip behind something that
// is a timer.
type Wait struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// What is being waited for.
	//
	// Types that are valid to be assigned to Kind:
	//
	//	*Wait_Duration
	//	*Wait_Until
	//	*Wait_Signal
	Kind isWait_Kind `protobuf_oneof:"kind"`
	// Timeout bounds how long the wait may last, for a wait that might otherwise
	// never end.
	//
	// It is meaningful for `until` and `signal`, and meaningless for `duration`,
	// where the duration is already the bound. A wait that times out is not a
	// failure: it produces its outputs with `timed_out` true, so an author decides
	// what a lapsed approval means with an ordinary `if:` rather than having to
	// treat a normal outcome as an error.
	//
	// Unset means the wait may last as long as the run does. That is a deliberate
	// choice for an approval gate that genuinely should block until someone acts,
	// and a mistake everywhere else.
	Timeout       *durationpb.Duration `protobuf:"bytes,4,opt,name=timeout,proto3" json:"timeout,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Wait) Reset() {
	*x = Wait{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[12]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Wait) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Wait) ProtoMessage() {}

func (x *Wait) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[12]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Wait.ProtoReflect.Descriptor instead.
func (*Wait) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{12}
}

func (x *Wait) GetKind() isWait_Kind {
	if x != nil {
		return x.Kind
	}
	return nil
}

func (x *Wait) GetDuration() *durationpb.Duration {
	if x != nil {
		if x, ok := x.Kind.(*Wait_Duration); ok {
			return x.Duration
		}
	}
	return nil
}

func (x *Wait) GetUntil() *Value {
	if x != nil {
		if x, ok := x.Kind.(*Wait_Until); ok {
			return x.Until
		}
	}
	return nil
}

func (x *Wait) GetSignal() *Signal {
	if x != nil {
		if x, ok := x.Kind.(*Wait_Signal); ok {
			return x.Signal
		}
	}
	return nil
}

func (x *Wait) GetTimeout() *durationpb.Duration {
	if x != nil {
		return x.Timeout
	}
	return nil
}

type isWait_Kind interface {
	isWait_Kind()
}

type Wait_Duration struct {
	// Duration waits for time to pass, as `sleep: 30s`.
	//
	// It is a durable timer, so the workload is not holding a worker or a thread
	// while it elapses. A week is as reasonable as a second.
	Duration *durationpb.Duration `protobuf:"bytes,1,opt,name=duration,proto3,oneof"`
}

type Wait_Until struct {
	// Until waits for a *moment*, as `wait_until: <expr>`.
	//
	// The expression is evaluated once, against the outputs of steps that have
	// already run, and must produce a time; a boolean is refused rather than
	// polled. Inside it `now` names the moment the wait is evaluated — taken from
	// the driver's clock, so it replays to the same instant — and the duration
	// units make a relative deadline readable: `${now + days(3)}`.
	//
	// It is a moment and not a condition because a condition here could only ever
	// spell "wait forever". A blocked workload runs no step, so nothing it can
	// see changes while it waits: a condition over step outputs is either already
	// true or never going to be. Waiting on something outside the workload is
	// what a signal is for.
	//
	// A moment already past releases the wait immediately, which is what lets a
	// workload resumed after an outage reach an open window and catch up instead
	// of failing for being late.
	Until *Value `protobuf:"bytes,2,opt,name=until,proto3,oneof"`
}

type Wait_Signal struct {
	// Signal waits for something outside the workload to say it may proceed,
	// which is what makes a human approval gate expressible.
	Signal *Signal `protobuf:"bytes,3,opt,name=signal,proto3,oneof"`
}

func (*Wait_Duration) isWait_Kind() {}

func (*Wait_Until) isWait_Kind() {}

func (*Wait_Signal) isWait_Kind() {}

// Signal names something a workload waits to be told.
//
// The payload a signal carries becomes the waiting step's outputs, which is what
// makes a gate compose with everything else: `${approval.approved}` resolves the
// way any other step reference does, so it works in an `if:`, in a task input,
// and in a later expression, with no special form for signals.
type Signal struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is what a sender addresses. It is part of the workload's contract with
	// whatever approves it, so it is written by the author rather than generated.
	Name          string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Signal) Reset() {
	*x = Signal{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[13]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Signal) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Signal) ProtoMessage() {}

func (x *Signal) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[13]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Signal.ProtoReflect.Descriptor instead.
func (*Signal) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{13}
}

func (x *Signal) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

// SignalSender is who the server attests sent a signal — never what the
// sender's own payload claims.
//
// Two separate things travel with every signal, and they must never merge into
// one map: `payload` is what the sender chose to send, unchanged and untrusted;
// `sender` is what the server itself established about the caller before it
// ever looked at the payload. A workflow author writes conditions against
// `sender` — `sender.identity.subject != inputs.requested_by` for separation of
// duties — and treats `payload` as evidence rather than identity.
//
// Identity reuses [WorkloadIdentity] rather than a parallel type: a signal's
// sender and a run's own caller are the same kind of fact — someone acting,
// established by authentication rather than self-report — and a run's identity
// is already this message. Two messages for one shape is exactly the drift
// invariant 1 (proto-first) exists to prevent.
//
// Absent entirely on a signal that predates this field (identity unset,
// accepted_at unset, local false), which reads as unattested — correct,
// because no signal recorded before this field existed could have carried one.
// There is no compatibility arm: an old signal simply has nothing here.
type SignalSender struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Identity is the authenticated caller the server established for this
	// signal, by the same path a run's own identity is established
	// (`FlowstateServer.identityFor`, via `authorizeRun`). Never taken from the
	// request — a caller who could name their own identity could name anyone
	// else's, which is the exact defect this message exists to close.
	Identity *WorkloadIdentity `protobuf:"bytes,1,opt,name=identity,proto3" json:"identity,omitempty"`
	// AcceptedAt is when the server accepted the signal, from the server's own
	// clock — never the sender's. A self-reported time is exactly the kind of
	// claim this field replaces with something attested.
	AcceptedAt *timestamppb.Timestamp `protobuf:"bytes,2,opt,name=accepted_at,json=acceptedAt,proto3" json:"accepted_at,omitempty"`
	// Local marks a signal delivered to a local run (`flow run local --signal`
	// or `flow test`'s scripted signals), which has no authenticated caller at
	// all — there is no server in front of it to attest anything. Set only by
	// [LocalSignalSender]; the durable driver never sets it true.
	//
	// A wait's rendered `sender.local` output (see wait.go's signalSenderValue)
	// is `true` for this case *and* for every other case where nothing was
	// attested at all — this message absent entirely, which covers a signal
	// that predates this field, a timed-out wait with nothing pending, and the
	// engine's own compatibility fallback for a signal that arrived in the
	// pre-#194 wire shape (engine/signal_compat.go). All of those are the same
	// fact from a workflow author's point of view — nobody attested this — and
	// must read the same way, distinct from an attested-but-anonymous caller
	// (this message present, Local false, Identity.Subject merely empty because
	// no identity provider is configured). This field's own wire value only
	// ever means "the local driver," never "nothing was attested"; the broader
	// reading exists solely in the rendered output, precisely so a workflow
	// author can trust `!sender.local` as "the server accepted this."
	Local         bool `protobuf:"varint,3,opt,name=local,proto3" json:"local,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *SignalSender) Reset() {
	*x = SignalSender{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[14]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *SignalSender) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*SignalSender) ProtoMessage() {}

func (x *SignalSender) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[14]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use SignalSender.ProtoReflect.Descriptor instead.
func (*SignalSender) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{14}
}

func (x *SignalSender) GetIdentity() *WorkloadIdentity {
	if x != nil {
		return x.Identity
	}
	return nil
}

func (x *SignalSender) GetAcceptedAt() *timestamppb.Timestamp {
	if x != nil {
		return x.AcceptedAt
	}
	return nil
}

func (x *SignalSender) GetLocal() bool {
	if x != nil {
		return x.Local
	}
	return false
}

// PendingSignal is a signal that arrived before the step waiting for it was
// reached, carried across Continue-As-New.
//
// It exists because Temporal delivers a signal to the run that is current when it
// arrives, and a run that suspends drops anything still buffered on a channel it
// has not read. Without carrying them, approving a gate before the run reaches it
// — which is ordinary behavior, not misuse — would silently lose the approval and
// leave the run waiting forever.
type PendingSignal struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is the signal that arrived.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Payload is what it carried, which becomes the waiting step's outputs.
	Payload *Node_Outputs `protobuf:"bytes,2,opt,name=payload,proto3" json:"payload,omitempty"`
	// Sender is who the server attested for this signal, carried alongside the
	// payload for the same reason payload is carried: a signal drained before
	// its step is reached must not lose either half when carried across
	// Continue-As-New. Absent for a pending signal carried from before this
	// field existed, which reads as unattested.
	Sender        *SignalSender `protobuf:"bytes,3,opt,name=sender,proto3" json:"sender,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *PendingSignal) Reset() {
	*x = PendingSignal{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[15]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *PendingSignal) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*PendingSignal) ProtoMessage() {}

func (x *PendingSignal) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[15]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use PendingSignal.ProtoReflect.Descriptor instead.
func (*PendingSignal) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{15}
}

func (x *PendingSignal) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *PendingSignal) GetPayload() *Node_Outputs {
	if x != nil {
		return x.Payload
	}
	return nil
}

func (x *PendingSignal) GetSender() *SignalSender {
	if x != nil {
		return x.Sender
	}
	return nil
}

// SignalDelivery is what the server hands a waiting workflow over the signal
// channel: the sender's own payload, paired with the sender the server
// attested for it. It is not persisted directly — [PendingSignal] is the form
// that survives Continue-As-New — but the two carry the same pair for the same
// reason: a signal must not lose its sender any more than it may lose its
// payload.
type SignalDelivery struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Payload is what the sender sent, unchanged and untrusted — see
	// [SignalSender] for why it never carries identity.
	Payload *Node_Outputs `protobuf:"bytes,1,opt,name=payload,proto3" json:"payload,omitempty"`
	// Sender is what the server established about the caller before forwarding
	// the payload. Never absent on a signal a current server sends; absent only
	// when reading a delivery recorded before this field existed.
	Sender        *SignalSender `protobuf:"bytes,2,opt,name=sender,proto3" json:"sender,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *SignalDelivery) Reset() {
	*x = SignalDelivery{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[16]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *SignalDelivery) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*SignalDelivery) ProtoMessage() {}

func (x *SignalDelivery) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[16]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use SignalDelivery.ProtoReflect.Descriptor instead.
func (*SignalDelivery) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{16}
}

func (x *SignalDelivery) GetPayload() *Node_Outputs {
	if x != nil {
		return x.Payload
	}
	return nil
}

func (x *SignalDelivery) GetSender() *SignalSender {
	if x != nil {
		return x.Sender
	}
	return nil
}

// Scope is what an expression is evaluated against.
//
// It holds the outputs of steps that have already run, plus any variables bound
// by enclosing control flow, such as a loop's current item.
//
// This is part of the schema rather than an internal Go type because it crosses
// the wire: a task that evaluates its own expressions — the cel task's `expr`, the
// http task's `outputs` — does so on a worker, so the scope has to travel with it.
// Without that, a loop body could not use its own iterator in those inputs.
//
// Variables are Values, the same representation step outputs use, so anything a
// step can produce can also be bound as a variable.
type Scope struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Outputs holds the outputs of steps that have already run.
	Outputs *Workflow_StepOutputs `protobuf:"bytes,1,opt,name=outputs,proto3" json:"outputs,omitempty"`
	// Profile is the workflow's language profile, carried here because this is what
	// reaches the worker that evaluates a task's own expressions.
	//
	// Without it that worker would evaluate against whatever profile its own build
	// calls current, which is the same run computing against two vocabularies — the
	// failure one profile exists to remove, reintroduced at the point where the work
	// actually happens.
	Profile string `protobuf:"bytes,3,opt,name=profile,proto3" json:"profile,omitempty"`
	// Vars holds the names bound *where an expression is written*, resolved bare: a
	// `for_each` binding, a step's own `vars:`, `now` inside a wait.
	//
	// The name is a historical one and is kept deliberately. This field has carried
	// bare bindings since before there was a rooted namespace, and an activity
	// scheduled before an upgrade holds that shape in history — a retry after the
	// upgrade replays the same payload. Redefining the field would make a new worker
	// read an old loop's `item` as an ambient var and leave the bare name unbound, so
	// the rooted namespace went to [Scope.ambient_vars] instead and this one did not
	// move. A field that crosses a version boundary means what it has always meant.
	//
	// Because every name here is bare they can collide, and that is refused at
	// validation rather than resolved: a step var that shadows an enclosing loop
	// binding, or either shadowing `now`, is reported at the inner declaration. By the
	// time a spec reaches the engine there is nothing left to decide.
	Vars map[string]*Value `protobuf:"bytes,2,rep,name=vars,proto3" json:"vars,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// AmbientVars holds the rooted `vars.<name>` namespace, which is the workflow's
	// declared vars.
	//
	// Only the workflow level declares into here. A step's `vars:` are bare and land in
	// [Scope.vars] instead, because they are author-chosen and lexically local rather
	// than ambient — so there is exactly one source for this map and no shadowing to
	// resolve within it.
	//
	// A new field rather than a new meaning for an old one: see [Scope.vars].
	AmbientVars map[string]*Value `protobuf:"bytes,4,rep,name=ambient_vars,json=ambientVars,proto3" json:"ambient_vars,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Inputs holds the rooted `inputs.<name>` namespace: the arguments this run was
	// started with, checked against [Workflow.declared_inputs] and defaulted at
	// submit.
	//
	// Carried on the scope rather than looked up from the specification where an
	// expression is evaluated, because the specification is not everywhere the scope
	// is: a task declaring `needs_prev_outputs` evaluates its own expressions inside
	// an activity, on a worker holding this message and nothing else. Without the
	// field, `${inputs.region}` would resolve in a step's `if:` and be unbound in the
	// http task's `outputs:` two lines below it — one spelling with two behaviours,
	// decided by a property of the task an author has no reason to know. That is the
	// failure `now` is placed to avoid; see [Scope.ambient_vars], which was added and
	// then omitted from the compacted copy the executor ships, with exactly that
	// result.
	//
	// Values only, never expressions: what a caller sends is data (see
	// [RunRequest.inputs]), and a default is required to be a literal by the
	// compiler.
	//
	// A new field rather than folding these into [Scope.ambient_vars]: a var and an
	// input are different things with different roots, and merging them would make
	// `${vars.region}` resolve an argument nobody declared as a var.
	Inputs map[string]*Value `protobuf:"bytes,5,rep,name=inputs,proto3" json:"inputs,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Identity is the attested identity of whoever started this run, exposed to
	// expressions under the `run` root — `run.identity.subject`,
	// `run.identity.issuer`, `run.identity.namespace`, `run.identity.claims` —
	// so a rule can compare the caller who requested the run against the
	// caller who satisfies a gate later. See [RunState.identity], which this is
	// copied from and never anything else: a workflow author cannot set this,
	// and a signal's own sender (see [SignalSender]) cannot either, because
	// both are attested at a boundary the workflow does not control.
	//
	// Deliberately narrower than [WorkloadIdentity] itself: `deployment` is
	// left off, because it answers "which installation ran this" rather than
	// "who is this caller", and that is exactly the question CLAUDE.md's
	// diagnostics rule already answers for a different surface — a fact a
	// deployment decides, not a fact about the file, does not belong in a
	// workflow's own logic. An outbound authorization policy already sees it as
	// `workload.deployment` (see [WorkloadIdentity.deployment]); a workflow
	// gating on it would be asking a question that belongs one layer down.
	//
	// Absent for a run that predates this field, and for every local run — both
	// read as an identity with every field empty, and [Scope.local] is what
	// tells those two cases apart from a run the server genuinely attested with
	// an anonymous identity.
	Identity *WorkloadIdentity `protobuf:"bytes,6,opt,name=identity,proto3" json:"identity,omitempty"`
	// Local marks a scope built by the local driver (`flow run local`, `flow
	// test`), which has no authenticated caller at all — read under
	// `run.local`. Mirrors [SignalSender.local] for the identical reason: an
	// empty `run.identity.subject` must never be silently confused with a real
	// caller who merely authenticated anonymously (no identity provider
	// configured), so the local driver states outright that nothing here was
	// attested, rather than leaving an author to infer it from an empty string.
	//
	// Never true on any scope the durable driver builds, even when
	// [Scope.identity] is empty because the run was started without
	// authentication — that is only possible in development, and it is still a
	// server attesting an anonymous caller rather than no caller at all.
	Local         bool `protobuf:"varint,7,opt,name=local,proto3" json:"local,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Scope) Reset() {
	*x = Scope{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[17]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Scope) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Scope) ProtoMessage() {}

func (x *Scope) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[17]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Scope.ProtoReflect.Descriptor instead.
func (*Scope) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{17}
}

func (x *Scope) GetOutputs() *Workflow_StepOutputs {
	if x != nil {
		return x.Outputs
	}
	return nil
}

func (x *Scope) GetProfile() string {
	if x != nil {
		return x.Profile
	}
	return ""
}

func (x *Scope) GetVars() map[string]*Value {
	if x != nil {
		return x.Vars
	}
	return nil
}

func (x *Scope) GetAmbientVars() map[string]*Value {
	if x != nil {
		return x.AmbientVars
	}
	return nil
}

func (x *Scope) GetInputs() map[string]*Value {
	if x != nil {
		return x.Inputs
	}
	return nil
}

func (x *Scope) GetIdentity() *WorkloadIdentity {
	if x != nil {
		return x.Identity
	}
	return nil
}

func (x *Scope) GetLocal() bool {
	if x != nil {
		return x.Local
	}
	return false
}

// ForEach runs a body of steps once per item of a list.
//
// This is how a workload expresses "do this to each of these" without the author
// unrolling it by hand, which they cannot do anyway when the list is computed at
// run time.
//
// The body sees the current item under the iterator's name, and body steps can
// reference each other within an iteration. Body step outputs do not escape the
// iteration; the loop as a whole reports every iteration's outputs through its own
// `results` output, so that what a later step sees does not depend on which
// iteration happened to run last.
type ForEach struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Items is an expression producing the list to iterate. An empty list runs the
	// body zero times, which is a normal outcome rather than an error.
	Items *Value `protobuf:"bytes,1,opt,name=items,proto3" json:"items,omitempty"`
	// Iterator names the variable bound to the current item inside the body.
	// Defaults to "item". It must not collide with a step id, since both are
	// resolved from the same namespace.
	Iterator string `protobuf:"bytes,2,opt,name=iterator,proto3" json:"iterator,omitempty"`
	// Body is the steps to run per item.
	Body []*Node `protobuf:"bytes,3,rep,name=body,proto3" json:"body,omitempty"`
	// MaxParallel bounds how many iterations run concurrently. Zero or one runs
	// them one at a time, which is the safe default: concurrency is worth
	// requesting explicitly, because it changes both the load placed on whatever
	// the body touches and the order in which side effects happen.
	MaxParallel   int32 `protobuf:"varint,4,opt,name=max_parallel,json=maxParallel,proto3" json:"max_parallel,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ForEach) Reset() {
	*x = ForEach{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[18]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ForEach) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ForEach) ProtoMessage() {}

func (x *ForEach) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[18]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ForEach.ProtoReflect.Descriptor instead.
func (*ForEach) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{18}
}

func (x *ForEach) GetItems() *Value {
	if x != nil {
		return x.Items
	}
	return nil
}

func (x *ForEach) GetIterator() string {
	if x != nil {
		return x.Iterator
	}
	return ""
}

func (x *ForEach) GetBody() []*Node {
	if x != nil {
		return x.Body
	}
	return nil
}

func (x *ForEach) GetMaxParallel() int32 {
	if x != nil {
		return x.MaxParallel
	}
	return 0
}

// Parallel runs several branches of steps concurrently and waits for all of them.
//
// Branches are for work that is independent by construction — checking three
// services, publishing to two destinations — where doing it sequentially wastes
// time for no benefit. Steps within a branch still run in order.
//
// Branches must not depend on each other's outputs. There is no ordering between
// them, so such a dependency would be a race; each branch sees the outputs that
// existed before the parallel node began, plus its own.
type Parallel struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Branches      []*Parallel_Branch     `protobuf:"bytes,1,rep,name=branches,proto3" json:"branches,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Parallel) Reset() {
	*x = Parallel{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[19]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Parallel) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Parallel) ProtoMessage() {}

func (x *Parallel) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[19]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Parallel.ProtoReflect.Descriptor instead.
func (*Parallel) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{19}
}

func (x *Parallel) GetBranches() []*Parallel_Branch {
	if x != nil {
		return x.Branches
	}
	return nil
}

// Loop repeats a body of steps, carrying a value from one iteration to the next,
// until a condition holds or a hard iteration ceiling is reached.
//
// It is the primitive `for_each` is not. `for_each` maps a body over a list it
// already holds; nothing in it threads a value from iteration N into N+1, and its
// length is fixed before the first iteration runs. A loop is what expresses "keep
// going until you are done, and each step decides where the next one starts" — the
// shape a cursor-paged API has, where you cannot know how many pages there are
// until a page tells you it was the last one.
//
// # Do-while, deliberately
//
// The body runs, and *then* [until] is evaluated — so a loop always runs its body
// at least once, and [until] may read that body's own outputs. That is not an
// arbitrary choice between while and do-while: the whole point is that the stop
// condition is something a body step *produces* (a page reporting it was not
// truncated), which a pre-body check could not see, because on the first iteration
// no body step has run and nothing it could read exists yet.
//
// # The carried value, and why it is one
//
// [state] names a single value the body reads under that bare name — the same
// standing a `for_each` binding has, an author-chosen name bound where the body's
// expressions are written (docs/DSL.md principle 5). [initial] is what it holds on
// the first iteration; [update] computes what it holds on the next, from the
// current value and the body's outputs. All three are optional together: a bounded
// retry-until loop that carries nothing writes none of them.
//
// One carried value rather than a block of several, and that is the smaller
// language on purpose. A named block of state entries would immediately need an
// answer to whether one entry may read another — the exact `vars:`-sibling
// ordering question a protobuf map cannot answer and this repo already refused
// once ([Workflow.vars]). A single value sidesteps it: when several fields are
// wanted, [initial] and [update] are CEL maps, and the ordering question never
// arises because there is one expression, evaluated once per iteration.
//
// # Bounded, because the author does not control the trip count
//
// [until] is a promise the loop cannot keep on its own — a cursor that never
// reports exhaustion, an update that never satisfies the condition, is an infinite
// loop, and the resource an author does not fully control is the iteration count.
// So a loop is bounded by [max_iterations], read through the one function both
// drivers call ([LoopMaxIterations]) so the ceiling cannot disagree with itself,
// and hitting it is a *distinct* failure — the loop reports that it exhausted its
// budget without [until] ever holding, rather than stopping silently as though it
// had finished. See [LoopIterationLimitError], the one sentence both drivers
// report it with.
//
// # Results
//
// A loop reports its body's per-iteration outputs through `results`, exactly as a
// `for_each` does — `results[0]` is the first iteration's body outputs, and so on
// — which is what lets a pagination loop gather every page. When the loop carries
// state, its final value is also reported as `state`, because an accumulate-until
// loop's answer *is* that value and it is reachable nowhere else once the loop
// ends.
//
// # What is deferred
//
// This is slice 1: a *finite* loop, provable by the bound. An unbounded loop that
// runs forever compacting its carried state across Continue-As-New — the entity
// pattern — is a separate, larger slice, because it needs a byte bound on [state]
// and a suspend cadence driven from history size, neither of which a finite loop
// needs. Nested loops and a concurrent loop are deferred too; see docs/DSL.md.
type Loop struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Body is the steps to run each iteration.
	//
	// Spelled `steps:` in the DSL, the same key a `for_each` body and a `parallel`
	// branch use, because it is the same thing: a list of steps that runs as a unit.
	// Body step outputs do not escape one iteration into the next any more than a
	// `for_each`'s do — each iteration starts from the outputs visible before the
	// loop, plus the carried [state] — so the only value threaded between iterations
	// is the one [update] computes.
	Body []*Node `protobuf:"bytes,1,rep,name=body,proto3" json:"body,omitempty"`
	// Until is the stop condition, evaluated after the body each iteration and
	// required to produce a boolean. When it is true the loop stops, reporting normal
	// completion.
	//
	// Evaluated in the scope the body finished in — the body's own step outputs are
	// visible, and so is [state] under its bare name — which is what makes
	// `${!steps.page.truncated}` mean "stop once the last page said it was not
	// truncated". A non-boolean is an error rather than being coerced, the same rule
	// a step's `if:` follows.
	Until *Value `protobuf:"bytes,2,opt,name=until,proto3" json:"until,omitempty"`
	// MaxIterations is the author's hard ceiling on how many times the body runs.
	//
	// Zero (or unset) takes [LoopMaxIterations]'s default, which both drivers read
	// from one constant so the ceiling cannot differ between a local rehearsal and a
	// durable run. A value above the absolute cap this field allows is refused by the
	// schema, so the bound the engine enforces is never larger than the one an author
	// can see written down.
	//
	// Reaching it is not a silent stop: the loop fails with [LoopIterationLimitError],
	// because a loop that ran its whole budget without [until] holding did not do what
	// it was asked, and reporting success would hide exactly the runaway the bound
	// exists to catch.
	MaxIterations int32 `protobuf:"varint,3,opt,name=max_iterations,json=maxIterations,proto3" json:"max_iterations,omitempty"`
	// State names the value the body carries from one iteration to the next, read
	// *bare* inside the body, [until] and [update]: `${cursor}`.
	//
	// Bare rather than rooted, the same standing as a `for_each` binding and for the
	// same reason (docs/DSL.md principle 5): it is an author-chosen name bound
	// lexically where the expressions that read it are written. `flow validate`
	// refuses a name that collides with an enclosing bare binding, with `now`, or
	// with a declaration root, exactly as it refuses a colliding loop iterator or
	// step var — a bare name may mean one thing at a time.
	//
	// Optional. A loop that carries nothing — a bounded retry-until — leaves this,
	// [initial] and [update] all unset. Setting one without the others is refused by
	// the compiler: a name with no initial value, or a value that never changes, is a
	// mistake worth naming rather than a shape to run.
	//
	// The pattern is ignored when unset, because unset is the whole stateless mode: an
	// empty string is the *absence* of a name, not a malformed one, and applying an
	// identifier pattern to it would refuse the loop that carries nothing — the mode
	// examples/loop-poll-until ships.
	State string `protobuf:"bytes,4,opt,name=state,proto3" json:"state,omitempty"`
	// Initial is the value [state] holds on the first iteration.
	//
	// Evaluated once, before the loop begins, against the scope the loop node sits in
	// — so it may read `vars.*`, `inputs.*`, and the outputs of steps that ran before
	// the loop, but not [state] itself, which does not exist yet. A literal is the
	// common case (`init: ${”}`); an expression is evaluated like any other.
	Initial *Value `protobuf:"bytes,5,opt,name=initial,proto3" json:"initial,omitempty"`
	// Update computes the value [state] holds on the *next* iteration, from the value
	// it holds now and the body's outputs.
	//
	// Evaluated after the body each iteration, in the same scope [until] sees — the
	// body's step outputs and the current [state] — which is what lets it say
	// `${steps.page.next_cursor}` (take the next value the body produced) or
	// `${state + steps.tick.amount}` (fold the body's output into an accumulator).
	// Required when [state] is set and refused when it is not.
	Update        *Value `protobuf:"bytes,6,opt,name=update,proto3" json:"update,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Loop) Reset() {
	*x = Loop{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[20]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Loop) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Loop) ProtoMessage() {}

func (x *Loop) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[20]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Loop.ProtoReflect.Descriptor instead.
func (*Loop) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{20}
}

func (x *Loop) GetBody() []*Node {
	if x != nil {
		return x.Body
	}
	return nil
}

func (x *Loop) GetUntil() *Value {
	if x != nil {
		return x.Until
	}
	return nil
}

func (x *Loop) GetMaxIterations() int32 {
	if x != nil {
		return x.MaxIterations
	}
	return 0
}

func (x *Loop) GetState() string {
	if x != nil {
		return x.State
	}
	return ""
}

func (x *Loop) GetInitial() *Value {
	if x != nil {
		return x.Initial
	}
	return nil
}

func (x *Loop) GetUpdate() *Value {
	if x != nil {
		return x.Update
	}
	return nil
}

// Call runs another workflow as a step.
//
// This is how a Flowfile stops being a single file. Before it, an organisation's
// second workflow copied its first: there was no way to factor a sequence out, no
// way to version one, and no way to test one apart from the thing that used it.
//
// # Resolved when the file is compiled, carried whole
//
// A `Call` holds the callee's *specification*, not a reference to it. The
// resolution — reading `./provision.yaml`, compiling it, checking its arguments —
// happens once, in the client, when a Flowfile becomes a Workflow.
//
// That is not an optimisation, it is invariant 10. A run's specification is frozen
// when it is submitted and carried across every Continue-As-New in its chain; a
// call resolved by name at execution time would make a durable run depend on a
// definition someone can edit while it is in flight, so a workload could mean one
// thing in its first segment and another in its last. It would also put a
// filesystem read inside a worker, which is the second reason: the server and the
// worker never resolve a path, so there is no traversal surface there to defend.
// The client that reads the author's files is the one place that can.
//
// The cost is honest and worth stating: a specification carrying its callees is
// bigger than one naming them, and it counts against [CheckSpecSize] like
// everything else. A workflow too large to submit is refused with the same
// message it would have been refused with if somebody had pasted the same steps
// in by hand — which is the right answer, because that is what this is.
//
// # A nested run, not an inlining
//
// The callee's steps are kept as a workflow rather than spliced into the caller's
// list, and each of the three reasons is something inlining loses.
//
// A position stays a path. `deploy > provision > network` is where a run is, and
// a flattened list would report `provision-network` — a name nobody wrote, in a
// file nobody can open. Diagnostics and failures name the callee the same way.
//
// The isolation below is real rather than achieved by renaming. And a call remains
// a *place* the engine can decide to execute differently: running one as a Temporal
// child workflow, with its own history and its own versioning, is additive from
// here and impossible after inlining.
//
// # What a callee can see
//
// Its `inputs:`, and nothing else. Not the caller's steps, not the caller's
// `vars:`, not a loop binding it happens to sit inside.
//
// That is the whole point rather than a restriction. A workflow that can read its
// caller's scope is not a unit — it cannot be understood, tested, or reused apart
// from the file that calls it, which is what a call was meant to make possible. It
// is also a security property once workflows are shared: a library workflow cannot
// read the values its caller resolved, including any it resolved from a secret.
//
// What comes back is what the callee `outputs:` declares, under the step's id. A
// callee that declares no outputs is a step that produces none — legal, and the
// right shape for a workflow called for its effects.
type Call struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Workflow is the callee, already compiled.
	//
	// Whole rather than referenced, for the reasons on the message above. It carries
	// its own `inputs:` declarations, which is what `arguments` below are checked
	// against — so a call type-checks in an editor, before anything runs.
	Workflow *Workflow `protobuf:"bytes,1,opt,name=workflow,proto3" json:"workflow,omitempty"`
	// Arguments bind the callee's declared inputs, by name.
	//
	// Values, never expressions evaluated by the callee. They are resolved in the
	// *caller's* scope — where `${steps.build.digest}` means something — and what
	// crosses the boundary is the result. A callee evaluating an expression handed
	// to it would be reading a scope it is not allowed to see, through a string.
	//
	// Every declared input the callee requires must be bound, and binding one it
	// does not declare is refused. Both are decided when the file is compiled, with
	// a position, rather than when the run reaches the step.
	Arguments map[string]*Value `protobuf:"bytes,2,rep,name=arguments,proto3" json:"arguments,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Source is where the callee was read from, as written in the file.
	//
	// Carried for diagnostics and for `flow compile` output, so a reader of a
	// compiled specification can see which file a nested workflow came from. It is
	// never resolved again — nothing reads a path from here — which is why it is a
	// plain string with no validation beyond a length: it is a record of what
	// happened, not an instruction.
	Source string `protobuf:"bytes,3,opt,name=source,proto3" json:"source,omitempty"`
	// SourceDigest is a content digest of the callee file's bytes, taken at the
	// same moment the resolver reads them, formatted `sha256:<hex>`.
	//
	// Recorded rather than verified, exactly as `source` is: neither is
	// resolved again by anything that reads a compiled specification. What it
	// buys is provenance an operator can audit after the fact — "this run
	// called provision-tenant.yaml at sha256:…" is answerable from a run record
	// alone, without the file that produced it necessarily being reachable, or
	// unchanged, by the time anyone asks. A platform sharing modules across
	// tenants is exactly the setting where "which bytes actually ran" needs an
	// answer that outlives the file.
	SourceDigest  string `protobuf:"bytes,4,opt,name=source_digest,json=sourceDigest,proto3" json:"source_digest,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Call) Reset() {
	*x = Call{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[21]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Call) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Call) ProtoMessage() {}

func (x *Call) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[21]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Call.ProtoReflect.Descriptor instead.
func (*Call) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{21}
}

func (x *Call) GetWorkflow() *Workflow {
	if x != nil {
		return x.Workflow
	}
	return nil
}

func (x *Call) GetArguments() map[string]*Value {
	if x != nil {
		return x.Arguments
	}
	return nil
}

func (x *Call) GetSource() string {
	if x != nil {
		return x.Source
	}
	return ""
}

func (x *Call) GetSourceDigest() string {
	if x != nil {
		return x.SourceDigest
	}
	return ""
}

// StepPolicy controls the execution of a single step.
//
// These settings exist because one policy cannot fit every step. Reading a
// configuration file and provisioning a cluster differ by orders of magnitude in
// how long they take and how safe they are to repeat, and a workload engine that
// applies the same timeout and the same retry count to both is wrong for at least
// one of them.
type StepPolicy struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Timeout bounds a single attempt at the step. A step exceeding it fails that
	// attempt, which may then be retried according to Retry.
	Timeout *durationpb.Duration `protobuf:"bytes,1,opt,name=timeout,proto3" json:"timeout,omitempty"`
	// Retry describes how a failed attempt is retried. Only failures that could
	// plausibly succeed on another attempt are retried; a step that failed because
	// its inputs are invalid is not retried regardless of this setting.
	Retry *RetryPolicy `protobuf:"bytes,2,opt,name=retry,proto3" json:"retry,omitempty"`
	// ContinueOnError lets the run proceed when this step fails.
	//
	// The step records its failure instead of ending the run, which is what makes
	// best-effort work expressible: a notification that could not be delivered
	// should not abandon the workload that succeeded.
	ContinueOnError bool `protobuf:"varint,3,opt,name=continue_on_error,json=continueOnError,proto3" json:"continue_on_error,omitempty"`
	unknownFields   protoimpl.UnknownFields
	sizeCache       protoimpl.SizeCache
}

func (x *StepPolicy) Reset() {
	*x = StepPolicy{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[22]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *StepPolicy) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*StepPolicy) ProtoMessage() {}

func (x *StepPolicy) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[22]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use StepPolicy.ProtoReflect.Descriptor instead.
func (*StepPolicy) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{22}
}

func (x *StepPolicy) GetTimeout() *durationpb.Duration {
	if x != nil {
		return x.Timeout
	}
	return nil
}

func (x *StepPolicy) GetRetry() *RetryPolicy {
	if x != nil {
		return x.Retry
	}
	return nil
}

func (x *StepPolicy) GetContinueOnError() bool {
	if x != nil {
		return x.ContinueOnError
	}
	return false
}

// RetryPolicy describes how a failed step attempt is retried.
type RetryPolicy struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// MaxAttempts bounds total attempts, including the first. Zero uses the
	// engine's default. One disables retrying, which is the right choice for an
	// operation that is not safe to repeat.
	MaxAttempts int32 `protobuf:"varint,1,opt,name=max_attempts,json=maxAttempts,proto3" json:"max_attempts,omitempty"`
	// InitialInterval is the delay before the second attempt.
	InitialInterval *durationpb.Duration `protobuf:"bytes,2,opt,name=initial_interval,json=initialInterval,proto3" json:"initial_interval,omitempty"`
	// BackoffCoefficient multiplies the delay after each attempt. Values below one
	// are rejected, since a shrinking delay defeats the purpose of backing off.
	BackoffCoefficient float64 `protobuf:"fixed64,3,opt,name=backoff_coefficient,json=backoffCoefficient,proto3" json:"backoff_coefficient,omitempty"`
	// MaxInterval caps the delay between attempts, so exponential growth does not
	// produce an unbounded wait.
	MaxInterval   *durationpb.Duration `protobuf:"bytes,4,opt,name=max_interval,json=maxInterval,proto3" json:"max_interval,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *RetryPolicy) Reset() {
	*x = RetryPolicy{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[23]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *RetryPolicy) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*RetryPolicy) ProtoMessage() {}

func (x *RetryPolicy) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[23]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use RetryPolicy.ProtoReflect.Descriptor instead.
func (*RetryPolicy) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{23}
}

func (x *RetryPolicy) GetMaxAttempts() int32 {
	if x != nil {
		return x.MaxAttempts
	}
	return 0
}

func (x *RetryPolicy) GetInitialInterval() *durationpb.Duration {
	if x != nil {
		return x.InitialInterval
	}
	return nil
}

func (x *RetryPolicy) GetBackoffCoefficient() float64 {
	if x != nil {
		return x.BackoffCoefficient
	}
	return 0
}

func (x *RetryPolicy) GetMaxInterval() *durationpb.Duration {
	if x != nil {
		return x.MaxInterval
	}
	return nil
}

// Value represents a value that can be used in expressions or as inputs/outputs in tasks.
// It can be a primitive type (like string, int, float, or bool), a structured type
// (like a struct or list), an expression (ParsedExpr), or an error. The Value type is used
// to encapsulate different kinds of data that can be used within the workflow.
type Value struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Types that are valid to be assigned to Kind:
	//
	//	*Value_Expr
	//	*Value_Literal
	//	*Value_Error_
	//	*Value_SecretRef
	//	*Value_Structure_
	Kind          isValue_Kind `protobuf_oneof:"kind"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Value) Reset() {
	*x = Value{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[24]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Value) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Value) ProtoMessage() {}

func (x *Value) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[24]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Value.ProtoReflect.Descriptor instead.
func (*Value) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{24}
}

func (x *Value) GetKind() isValue_Kind {
	if x != nil {
		return x.Kind
	}
	return nil
}

func (x *Value) GetExpr() *v1alpha1.ParsedExpr {
	if x != nil {
		if x, ok := x.Kind.(*Value_Expr); ok {
			return x.Expr
		}
	}
	return nil
}

func (x *Value) GetLiteral() *v1alpha1.Value {
	if x != nil {
		if x, ok := x.Kind.(*Value_Literal); ok {
			return x.Literal
		}
	}
	return nil
}

func (x *Value) GetError() *Value_Error {
	if x != nil {
		if x, ok := x.Kind.(*Value_Error_); ok {
			return x.Error
		}
	}
	return nil
}

func (x *Value) GetSecretRef() *SecretRef {
	if x != nil {
		if x, ok := x.Kind.(*Value_SecretRef); ok {
			return x.SecretRef
		}
	}
	return nil
}

func (x *Value) GetStructure() *Value_Structure {
	if x != nil {
		if x, ok := x.Kind.(*Value_Structure_); ok {
			return x.Structure
		}
	}
	return nil
}

type isValue_Kind interface {
	isValue_Kind()
}

type Value_Expr struct {
	Expr *v1alpha1.ParsedExpr `protobuf:"bytes,1,opt,name=expr,proto3,oneof"`
}

type Value_Literal struct {
	Literal *v1alpha1.Value `protobuf:"bytes,2,opt,name=literal,proto3,oneof"`
}

type Value_Error_ struct {
	Error *Value_Error `protobuf:"bytes,3,opt,name=error,proto3,oneof"`
}

type Value_SecretRef struct {
	// A reference to a secret, resolved only where the value is needed.
	//
	// This kind is inert everywhere except the activity that uses it. The
	// compiler produces it, the control plane transports it, and workflow-side
	// evaluation passes it through untouched — it is never resolved into a value
	// and never copied into step outputs, because workflow history is durable and
	// broadly readable. Only the worker executing the task resolves it, and the
	// value exists solely for that call.
	SecretRef *SecretRef `protobuf:"bytes,4,opt,name=secret_ref,json=secretRef,proto3,oneof"`
}

type Value_Structure_ struct {
	// A list or a mapping whose entries are values, which is the only shape
	// that can hold a [SecretRef] somewhere other than at the top.
	Structure *Value_Structure `protobuf:"bytes,5,opt,name=structure,proto3,oneof"`
}

func (*Value_Expr) isValue_Kind() {}

func (*Value_Literal) isValue_Kind() {}

func (*Value_Error_) isValue_Kind() {}

func (*Value_SecretRef) isValue_Kind() {}

func (*Value_Structure_) isValue_Kind() {}

// Task represents a specific operation that can be performed within a workflow as a node step.
// TaskCatalog is what this build can execute, described so that something other
// than a person can read it.
//
// The task registry is the single source of truth for capability — the engine
// dispatches from it, the validator checks against it, the editor completes from
// it — and until this existed the only way out of the process was a table meant
// for a terminal. An agent driving the CLI as a tool, a generator producing
// documentation, or an editor that is not this project's language server all had
// to parse columns, which is not a contract.
//
// It is a schema type rather than a shape invented in the CLI because it
// describes the system, which is what this file is for: a field added to it
// appears in every consumer without any of them changing, and it is already
// versioned and wire-stable — which is what let GetCatalog return it unchanged
// the day that RPC arrived.
type TaskCatalog struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Tasks is every task this build can run, sorted by name.
	Tasks []*TaskDescription `protobuf:"bytes,1,rep,name=tasks,proto3" json:"tasks,omitempty"`
	// CelLibraries are the CEL extension libraries every expression in a workflow
	// reaches. Not opt-in: a workflow speaks one dialect, so this is the vocabulary
	// of `if:`, `items:`, `wait_until:`, a task input and a `cel` step alike.
	CelLibraries []string `protobuf:"bytes,2,rep,name=cel_libraries,json=celLibraries,proto3" json:"cel_libraries,omitempty"`
	// DurationUnits are the duration constructors available to *every* expression
	// — `days(3)`, `minutes(30)` — rather than only where a library is enabled.
	// Listed separately for that reason: somebody reading cel_libraries to find out
	// what an expression can say would otherwise conclude these are unavailable.
	DurationUnits []string `protobuf:"bytes,3,rep,name=duration_units,json=durationUnits,proto3" json:"duration_units,omitempty"`
	// NowIdentifier is the name bound to the evaluation moment inside a
	// `wait_until` expression, and nowhere else. It is the one identifier whose
	// availability depends on position, so a reader cannot infer it.
	NowIdentifier string `protobuf:"bytes,4,opt,name=now_identifier,json=nowIdentifier,proto3" json:"now_identifier,omitempty"`
	// ValueRoots are the names an expression reaches a value through: `vars` for a
	// value the file names, `steps` for what a step produced.
	//
	// Here for the same reason NowIdentifier is — a reader cannot infer it from the
	// task list — and the retirement edition made that sharper rather than softer. A
	// consumer reading this catalog now sees two tasks, one of which produces nothing
	// at all, and could reasonably conclude the language has almost no way to compute
	// anything. It has one: an expression, bound under `vars:` at the workflow or on a
	// step, read back through these roots.
	//
	// Roots rather than prose because this message is the machine-readable half. What
	// the roots *mean* is `flow tasks` output and docs/DSL.md; what an agent needs in
	// order to write a reference that resolves is the words themselves.
	ValueRoots []string `protobuf:"bytes,5,rep,name=value_roots,json=valueRoots,proto3" json:"value_roots,omitempty"`
	// CelFunctions is every name the profile's libraries add, sorted by library and
	// then by name.
	//
	// cel_libraries above says which libraries are switched on and nothing about what
	// is in any of them, which is the difference between a fact and an answer: a
	// reader who wants to sort a list learns that `lists` is enabled and still has to
	// go and find cel-go's documentation to discover `sortBy`. That was survivable
	// while the answer was "everything cel-go has"; it stopped being survivable when a
	// profile became a *membership*, because a subset nobody can enumerate is a subset
	// nobody can write against.
	//
	// Both fields, rather than replacing one with the other. A library name is what
	// groups these and what docs/DSL.md and the profile definition talk in, so a
	// consumer that only wants to know the shape of the dialect should not have to
	// derive it from ninety names.
	CelFunctions  []*CELFunction `protobuf:"bytes,6,rep,name=cel_functions,json=celFunctions,proto3" json:"cel_functions,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TaskCatalog) Reset() {
	*x = TaskCatalog{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[25]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TaskCatalog) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TaskCatalog) ProtoMessage() {}

func (x *TaskCatalog) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[25]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TaskCatalog.ProtoReflect.Descriptor instead.
func (*TaskCatalog) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{25}
}

func (x *TaskCatalog) GetTasks() []*TaskDescription {
	if x != nil {
		return x.Tasks
	}
	return nil
}

func (x *TaskCatalog) GetCelLibraries() []string {
	if x != nil {
		return x.CelLibraries
	}
	return nil
}

func (x *TaskCatalog) GetDurationUnits() []string {
	if x != nil {
		return x.DurationUnits
	}
	return nil
}

func (x *TaskCatalog) GetNowIdentifier() string {
	if x != nil {
		return x.NowIdentifier
	}
	return ""
}

func (x *TaskCatalog) GetValueRoots() []string {
	if x != nil {
		return x.ValueRoots
	}
	return nil
}

func (x *TaskCatalog) GetCelFunctions() []*CELFunction {
	if x != nil {
		return x.CelFunctions
	}
	return nil
}

// CELFunction is one name an expression may call.
type CELFunction struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is written as it is called: `math.abs`, `upperAscii`.
	//
	// With one qualification, for a macro. cel-go identifies a macro by the name after
	// the dot, so `math.greatest(1, 2)` is reported as `greatest` and
	// `[3,1,2].sortBy(v, v)` as `sortBy` — a receiver style whose receiver is a
	// namespace in the first case and a value in the second, which its API does not
	// distinguish. So for a macro this is the name and not the whole call form, and a
	// consumer must not render it as one.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Library is the profile library that declares it.
	//
	// One library per name even where two declare it: this build's `regex` entry pulls
	// in cel-go's optional types, so an undeduplicated listing repeats all thirteen of
	// `optional` beneath it. A profile is one membership rather than a set of
	// switches, so which library a name arrived through is a fact about wiring, and
	// printing it twice reads as two functions.
	Library string `protobuf:"bytes,2,opt,name=library,proto3" json:"library,omitempty"`
	// Macro says the parser expands this rather than the evaluator calling it.
	//
	// Visible to an author in one place, which is why it is carried rather than
	// levelled away: a macro is settled when the file compiles, so its meaning is
	// frozen into the compiled workflow, where a function is looked up by whichever
	// worker evaluates the run.
	Macro bool `protobuf:"varint,3,opt,name=macro,proto3" json:"macro,omitempty"`
	// Example is a complete expression calling this, set only where `name` is not
	// enough to construct one — which is exactly the macros.
	//
	// A function's name *is* its call form: `upperAscii` is written `x.upperAscii()`
	// or `regex.replace(a, b, c)` as the name says. A macro's is not, and cel-go's
	// parser API is why: `Macro` exposes `Function`, `ArgCount`, `IsReceiverStyle`
	// and `MacroKey`, and none of them names the *receiver*. So `greatest` is
	// reported for something written `math.greatest(1, 2)` and `sortBy` for
	// something written `[3,1,2].sortBy(v, v)` — a namespace in one case and a value
	// in the other, indistinguishable from the outside.
	//
	// Which means this cannot be derived, and a consumer told "do not render `name`
	// as a call" and given nothing else has been handed a catalogue of things it
	// cannot use. It is a written table, kept honest the way this repo keeps the
	// things that cannot be derived: a test asks the environment for its macros and
	// fails on any that has no example, and every example is *evaluated* rather than
	// inspected, so an entry that stops working stops passing.
	//
	// Deriving it was tried and rejected rather than skipped. A library's macros can
	// be paired with the internal `@` names it declares — `math.@max` alongside
	// `greatest` gives `math` — and that is right for `math` and `bindings` and
	// silently wrong for `comprehensions`, which declares `cel.@mapInsert` while
	// `transformList` is written on a value. A derivation that is wrong for one
	// library in five is worse than a table somebody has to keep, because nothing
	// about it says which one.
	Example       string `protobuf:"bytes,4,opt,name=example,proto3" json:"example,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *CELFunction) Reset() {
	*x = CELFunction{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[26]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *CELFunction) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*CELFunction) ProtoMessage() {}

func (x *CELFunction) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[26]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use CELFunction.ProtoReflect.Descriptor instead.
func (*CELFunction) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{26}
}

func (x *CELFunction) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *CELFunction) GetLibrary() string {
	if x != nil {
		return x.Library
	}
	return ""
}

func (x *CELFunction) GetMacro() bool {
	if x != nil {
		return x.Macro
	}
	return false
}

func (x *CELFunction) GetExample() string {
	if x != nil {
		return x.Example
	}
	return ""
}

// TaskDescription is one task's name and shape.
type TaskDescription struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	Name  string                 `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Summary is a single line, the same one `flow tasks` prints.
	Summary       string       `protobuf:"bytes,2,opt,name=summary,proto3" json:"summary,omitempty"`
	Inputs        []*TaskField `protobuf:"bytes,3,rep,name=inputs,proto3" json:"inputs,omitempty"`
	Outputs       []*TaskField `protobuf:"bytes,4,rep,name=outputs,proto3" json:"outputs,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TaskDescription) Reset() {
	*x = TaskDescription{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[27]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TaskDescription) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TaskDescription) ProtoMessage() {}

func (x *TaskDescription) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[27]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TaskDescription.ProtoReflect.Descriptor instead.
func (*TaskDescription) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{27}
}

func (x *TaskDescription) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *TaskDescription) GetSummary() string {
	if x != nil {
		return x.Summary
	}
	return ""
}

func (x *TaskDescription) GetInputs() []*TaskField {
	if x != nil {
		return x.Inputs
	}
	return nil
}

func (x *TaskDescription) GetOutputs() []*TaskField {
	if x != nil {
		return x.Outputs
	}
	return nil
}

// PluginCatalog is what a deployment's plugins add to this build, described so
// that something other than a person can read it.
//
// The same argument as [TaskCatalog], applied one process boundary out. A plugin
// is code the engine does not ship, so "which tasks can this worker run" stops
// being a property of the binary and becomes a property of a deployment — and a
// question nobody can ask from outside is one every operator answers by reading
// logs.
//
// A plugin's task is described by [TaskDescription], the same message a built-in
// one is. That is not a convenience: a plugin ships protobuf descriptors for its
// inputs and outputs, so there is genuinely nothing left over to say about it,
// and giving it a parallel message would have been a second definition of the
// same thing. Where the two differ is *provenance*, which is the field this
// message adds and the reason it exists — a reviewer needs to know that a step
// leaves the engine's code.
//
// Wire-stable, on the same terms as [TaskCatalog], and for the same reason: a
// worker-introspection RPC that wants to return one should not have to invent a
// shape the CLI already prints.
type PluginCatalog struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Plugins is every plugin the worker brought up, sorted by name.
	Plugins []*PluginDescription `protobuf:"bytes,1,rep,name=plugins,proto3" json:"plugins,omitempty"`
	// SearchPath is the directories the worker was configured to discover plugins
	// in, in precedence order.
	//
	// Carried because an empty plugin list has two meanings that matter to tell
	// apart — nothing installed, or nowhere to look — and only one of them is a
	// configuration mistake.
	SearchPath    []string `protobuf:"bytes,2,rep,name=search_path,json=searchPath,proto3" json:"search_path,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *PluginCatalog) Reset() {
	*x = PluginCatalog{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[28]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *PluginCatalog) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*PluginCatalog) ProtoMessage() {}

func (x *PluginCatalog) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[28]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use PluginCatalog.ProtoReflect.Descriptor instead.
func (*PluginCatalog) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{28}
}

func (x *PluginCatalog) GetPlugins() []*PluginDescription {
	if x != nil {
		return x.Plugins
	}
	return nil
}

func (x *PluginCatalog) GetSearchPath() []string {
	if x != nil {
		return x.SearchPath
	}
	return nil
}

// PluginDescription is one plugin's identity and what it advertises.
type PluginDescription struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is the segment after `flowstate-plugin-` in the executable's name, and
	// is how discovery establishes it. It is not self-reported.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Version is what the plugin says it is. Informational: compatibility is
	// decided by the protocol version, not by this.
	Version string `protobuf:"bytes,2,opt,name=version,proto3" json:"version,omitempty"`
	// Description is the plugin's own one-line summary.
	Description string `protobuf:"bytes,3,opt,name=description,proto3" json:"description,omitempty"`
	// Path is the executable this worker launched, which is the fact an operator
	// wants when two directories on the search path hold the same name.
	Path string `protobuf:"bytes,4,opt,name=path,proto3" json:"path,omitempty"`
	// SecretSchemes are the secret reference schemes this plugin resolves.
	SecretSchemes []string `protobuf:"bytes,5,rep,name=secret_schemes,json=secretSchemes,proto3" json:"secret_schemes,omitempty"`
	// Tasks are the tasks this plugin provides, described exactly as a built-in
	// task is.
	Tasks         []*TaskDescription `protobuf:"bytes,6,rep,name=tasks,proto3" json:"tasks,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *PluginDescription) Reset() {
	*x = PluginDescription{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[29]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *PluginDescription) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*PluginDescription) ProtoMessage() {}

func (x *PluginDescription) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[29]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use PluginDescription.ProtoReflect.Descriptor instead.
func (*PluginDescription) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{29}
}

func (x *PluginDescription) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *PluginDescription) GetVersion() string {
	if x != nil {
		return x.Version
	}
	return ""
}

func (x *PluginDescription) GetDescription() string {
	if x != nil {
		return x.Description
	}
	return ""
}

func (x *PluginDescription) GetPath() string {
	if x != nil {
		return x.Path
	}
	return ""
}

func (x *PluginDescription) GetSecretSchemes() []string {
	if x != nil {
		return x.SecretSchemes
	}
	return nil
}

func (x *PluginDescription) GetTasks() []*TaskDescription {
	if x != nil {
		return x.Tasks
	}
	return nil
}

// TaskField is one input or output, described in the DSL's vocabulary.
type TaskField struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	Name  string                 `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Type is what an author would call it — `string`, `list[string]`,
	// `map[string, string]`, or `any` where the shape is whatever an expression
	// produced. Deliberately not the Protobuf type name, which is accurate about
	// the schema and useless about the file being written.
	Type string `protobuf:"bytes,2,opt,name=type,proto3" json:"type,omitempty"`
	// Required reports whether the task cannot run without this input. Never set on
	// an output, where the question does not arise.
	Required bool `protobuf:"varint,3,opt,name=required,proto3" json:"required,omitempty"`
	// Deferred reports that the task evaluates this input's expression itself,
	// against a scope the workflow does not have.
	//
	// It changes what may legally be written here, which is why it is a field
	// rather than prose: `http`'s `outputs` may name `status_code` because the task
	// evaluates it after the response arrives, and an ordinary input may not,
	// because the engine resolves it before the step is even scheduled.
	Deferred      bool `protobuf:"varint,4,opt,name=deferred,proto3" json:"deferred,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TaskField) Reset() {
	*x = TaskField{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[30]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TaskField) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TaskField) ProtoMessage() {}

func (x *TaskField) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[30]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TaskField.ProtoReflect.Descriptor instead.
func (*TaskField) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{30}
}

func (x *TaskField) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *TaskField) GetType() string {
	if x != nil {
		return x.Type
	}
	return ""
}

func (x *TaskField) GetRequired() bool {
	if x != nil {
		return x.Required
	}
	return false
}

func (x *TaskField) GetDeferred() bool {
	if x != nil {
		return x.Deferred
	}
	return false
}

type Task struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Tasks have specific names that identify the type of task being performed,
	// such as "log" or "http". The name is used to determine which task
	// implementation to execute when the workflow is run.
	//
	// # A dot means a plugin, and its absence means the engine
	//
	// A built-in task is one bare word. A plugin's task is two, joined by a dot —
	// `slack.post` is the `post` task of the plugin discovered as
	// `flowstate-plugin-slack` — and the dot is provenance rather than decoration:
	// it tells a reviewer that this line leaves the engine's code and enters code
	// somebody installed, at the step, where that fact is needed. It also makes
	// collision unrepresentable in both directions. A plugin task cannot shadow a
	// built-in because a plugin's tasks all carry a dot and no built-in does; two
	// plugins cannot fight over one name because each one's tasks are prefixed
	// with a name discovery established from the binary, not self-reported.
	//
	// At most one dot, by the pattern below. The first segment is a plugin name
	// (`^[a-z0-9][a-z0-9-]*$`, from the executable's own name) and the second a
	// task name (`^[a-z][a-z0-9_]*$`, from its manifest); the pattern here is the
	// permissive union rather than that concatenation, because this field also
	// spells built-in names with their historical uppercase-and-dash latitude, and
	// the registry enforces the sharper per-segment rules at registration.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Inputs are the parameters that the task requires to perform its operation.
	Inputs        map[string]*Value `protobuf:"bytes,3,rep,name=inputs,proto3" json:"inputs,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Task) Reset() {
	*x = Task{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[31]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Task) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Task) ProtoMessage() {}

func (x *Task) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[31]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Task.ProtoReflect.Descriptor instead.
func (*Task) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{31}
}

func (x *Task) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *Task) GetInputs() map[string]*Value {
	if x != nil {
		return x.Inputs
	}
	return nil
}

// RunRequest is the request message for running a workflow.
type RunRequest struct {
	state    protoimpl.MessageState `protogen:"open.v1"`
	Workflow *Workflow              `protobuf:"bytes,1,opt,name=workflow,proto3" json:"workflow,omitempty"`
	// Inputs are the arguments this run is started with, keyed by the name an
	// [InputDeclaration] on the workflow gave.
	//
	// # Values, never expressions
	//
	// Every [Value] here must be a literal. Not a convention — the security posture
	// of the whole surface. These arrive from whoever can call Run, which is the
	// definition of an untrusted party, and an expression accepted from there is
	// code the server would evaluate on its own behalf, in a scope holding the
	// run's own values. An expression is something a file says, and a file is
	// reviewed and compiled; this is data a caller sends. `secret_ref` is refused
	// for the neighbouring reason: a caller naming a secret would be choosing which
	// credential the run resolves, which is a decision that belongs to the
	// specification and its policy.
	//
	// The oneof cannot say which arm is allowed — protovalidate rules attach to
	// fields — so the server enforces it at submit, together with the rest of the
	// check against the declarations: every required input present, no undeclared
	// name, every value of the declared type, defaults filled in for what was left
	// out. Fail closed on each: a run that would be wrong is refused while the
	// caller is still there to be told.
	//
	// # Size (invariant 9)
	//
	// These become part of the run's state, so they are weighed by
	// `CheckRunStateSize` at every Continue-As-New along with everything else the
	// run carries — `proto.Size` on the whole message, so nothing has to be added
	// to a tally by hand. The submit-time check is `CheckSpecSize` on the workflow;
	// a caller who could push a run past the blob limit with arguments alone would
	// have found the hang that invariant exists to convert into an answer.
	Inputs        map[string]*Value `protobuf:"bytes,2,rep,name=inputs,proto3" json:"inputs,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *RunRequest) Reset() {
	*x = RunRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[32]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *RunRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*RunRequest) ProtoMessage() {}

func (x *RunRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[32]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use RunRequest.ProtoReflect.Descriptor instead.
func (*RunRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{32}
}

func (x *RunRequest) GetWorkflow() *Workflow {
	if x != nil {
		return x.Workflow
	}
	return nil
}

func (x *RunRequest) GetInputs() map[string]*Value {
	if x != nil {
		return x.Inputs
	}
	return nil
}

// RunResponse is the response message for a workflow run.
type RunResponse struct {
	state      protoimpl.MessageState `protogen:"open.v1"`
	WorkflowId string                 `protobuf:"bytes,1,opt,name=workflow_id,json=workflowId,proto3" json:"workflow_id,omitempty"`
	RunId      string                 `protobuf:"bytes,2,opt,name=run_id,json=runId,proto3" json:"run_id,omitempty"`
	Status     RunResponse_Status     `protobuf:"varint,3,opt,name=status,proto3,enum=flowstate.v1.RunResponse_Status" json:"status,omitempty"`
	// Types that are valid to be assigned to Kind:
	//
	//	*RunResponse_Error_
	//	*RunResponse_Outputs
	Kind          isRunResponse_Kind `protobuf_oneof:"kind"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *RunResponse) Reset() {
	*x = RunResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[33]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *RunResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*RunResponse) ProtoMessage() {}

func (x *RunResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[33]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use RunResponse.ProtoReflect.Descriptor instead.
func (*RunResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{33}
}

func (x *RunResponse) GetWorkflowId() string {
	if x != nil {
		return x.WorkflowId
	}
	return ""
}

func (x *RunResponse) GetRunId() string {
	if x != nil {
		return x.RunId
	}
	return ""
}

func (x *RunResponse) GetStatus() RunResponse_Status {
	if x != nil {
		return x.Status
	}
	return RunResponse_STATUS_UNSPECIFIED
}

func (x *RunResponse) GetKind() isRunResponse_Kind {
	if x != nil {
		return x.Kind
	}
	return nil
}

func (x *RunResponse) GetError() *RunResponse_Error {
	if x != nil {
		if x, ok := x.Kind.(*RunResponse_Error_); ok {
			return x.Error
		}
	}
	return nil
}

func (x *RunResponse) GetOutputs() *Workflow_StepOutputs {
	if x != nil {
		if x, ok := x.Kind.(*RunResponse_Outputs); ok {
			return x.Outputs
		}
	}
	return nil
}

type isRunResponse_Kind interface {
	isRunResponse_Kind()
}

type RunResponse_Error_ struct {
	Error *RunResponse_Error `protobuf:"bytes,4,opt,name=error,proto3,oneof"`
}

type RunResponse_Outputs struct {
	Outputs *Workflow_StepOutputs `protobuf:"bytes,5,opt,name=outputs,proto3,oneof"`
}

func (*RunResponse_Error_) isRunResponse_Kind() {}

func (*RunResponse_Outputs) isRunResponse_Kind() {}

// GetRequest is the request message for getting a workflow run.
type GetRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	WorkflowId    string                 `protobuf:"bytes,1,opt,name=workflow_id,json=workflowId,proto3" json:"workflow_id,omitempty"`
	RunId         *string                `protobuf:"bytes,2,opt,name=run_id,json=runId,proto3,oneof" json:"run_id,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *GetRequest) Reset() {
	*x = GetRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[34]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *GetRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*GetRequest) ProtoMessage() {}

func (x *GetRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[34]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use GetRequest.ProtoReflect.Descriptor instead.
func (*GetRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{34}
}

func (x *GetRequest) GetWorkflowId() string {
	if x != nil {
		return x.WorkflowId
	}
	return ""
}

func (x *GetRequest) GetRunId() string {
	if x != nil && x.RunId != nil {
		return *x.RunId
	}
	return ""
}

// GetResponse is the response message for getting a workflow run.
type GetResponse struct {
	state      protoimpl.MessageState `protogen:"open.v1"`
	WorkflowId string                 `protobuf:"bytes,1,opt,name=workflow_id,json=workflowId,proto3" json:"workflow_id,omitempty"`
	RunId      string                 `protobuf:"bytes,2,opt,name=run_id,json=runId,proto3" json:"run_id,omitempty"`
	Status     RunResponse_Status     `protobuf:"varint,3,opt,name=status,proto3,enum=flowstate.v1.RunResponse_Status" json:"status,omitempty"`
	// Types that are valid to be assigned to Kind:
	//
	//	*GetResponse_Error
	//	*GetResponse_Outputs
	Kind isGetResponse_Kind `protobuf_oneof:"kind"`
	// StartTime is when the workload began, and CloseTime when it finished — unset
	// while it is still running, so "has not finished" and "finished at the epoch"
	// stay distinct.
	//
	// Same two fields a listing reports, with the same names and the same meaning,
	// because a listing already answered them and a Get did not: `flow list` could
	// tell you a run had been going for an hour and `flow get <id>` on the same run
	// could only tell you it was running. One verb knowing less about a run than the
	// verb that enumerates it is the wrong way round — Get is the one a person
	// reaches for when they care about a *particular* run.
	//
	// Both come off the same DescribeWorkflowExecution response the status does, so
	// this costs nothing but the field.
	StartTime *timestamppb.Timestamp `protobuf:"bytes,6,opt,name=start_time,json=startTime,proto3" json:"start_time,omitempty"`
	CloseTime *timestamppb.Timestamp `protobuf:"bytes,7,opt,name=close_time,json=closeTime,proto3" json:"close_time,omitempty"`
	// Progress is where a running run has got to. Unset for any other status, since a
	// finished run's position is its outputs.
	//
	// Unset does not mean "at the beginning". It also means no worker answered, or
	// that the run is on an interpreter built before this existed — facts about the
	// deployment rather than about the workload. A reader that rendered an unanswered
	// query as "step 1 of 5" would be inventing one.
	Progress *RunProgress `protobuf:"bytes,8,opt,name=progress,proto3" json:"progress,omitempty"`
	// PendingActivities are the steps Temporal is retrying right now, with why the
	// last attempt failed and when the next one is due.
	//
	// This is the answer to "why has this been RUNNING for six hours", and until
	// it was carried here that answer was unobtainable through Flowstate at all:
	// Temporal's Describe response held it, the server read only the status beside
	// it, and an operator had to leave the tenancy boundary this service enforces
	// and ask the temporal CLI directly. Set only for a RUNNING run, on the same
	// reasoning progress is; empty for a running run means nothing is mid-retry,
	// which is itself the answer — the run is waiting or between steps, not stuck.
	PendingActivities []*PendingActivity `protobuf:"bytes,9,rep,name=pending_activities,json=pendingActivities,proto3" json:"pending_activities,omitempty"`
	// Outputs are the values the workflow declared it would report, computed by
	// this run — the answer, as against the transcript `outputs` above holds.
	//
	// A new field beside the existing oneof rather than a third arm in it: the
	// oneof answers "did it fail", which is a different question from "what did it
	// produce", and a run can perfectly well have both a status and a result. Unset
	// means the workflow declared no outputs, or the run has not finished, or the
	// run was started before this existed — all three of which are honestly
	// "nothing to report" rather than "an empty result", which is why there is no
	// empty message to distinguish them.
	RunOutputs    *RunOutputs `protobuf:"bytes,10,opt,name=run_outputs,json=runOutputs,proto3" json:"run_outputs,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *GetResponse) Reset() {
	*x = GetResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[35]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *GetResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*GetResponse) ProtoMessage() {}

func (x *GetResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[35]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use GetResponse.ProtoReflect.Descriptor instead.
func (*GetResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{35}
}

func (x *GetResponse) GetWorkflowId() string {
	if x != nil {
		return x.WorkflowId
	}
	return ""
}

func (x *GetResponse) GetRunId() string {
	if x != nil {
		return x.RunId
	}
	return ""
}

func (x *GetResponse) GetStatus() RunResponse_Status {
	if x != nil {
		return x.Status
	}
	return RunResponse_STATUS_UNSPECIFIED
}

func (x *GetResponse) GetKind() isGetResponse_Kind {
	if x != nil {
		return x.Kind
	}
	return nil
}

func (x *GetResponse) GetError() *RunResponse_Error {
	if x != nil {
		if x, ok := x.Kind.(*GetResponse_Error); ok {
			return x.Error
		}
	}
	return nil
}

func (x *GetResponse) GetOutputs() *Workflow_StepOutputs {
	if x != nil {
		if x, ok := x.Kind.(*GetResponse_Outputs); ok {
			return x.Outputs
		}
	}
	return nil
}

func (x *GetResponse) GetStartTime() *timestamppb.Timestamp {
	if x != nil {
		return x.StartTime
	}
	return nil
}

func (x *GetResponse) GetCloseTime() *timestamppb.Timestamp {
	if x != nil {
		return x.CloseTime
	}
	return nil
}

func (x *GetResponse) GetProgress() *RunProgress {
	if x != nil {
		return x.Progress
	}
	return nil
}

func (x *GetResponse) GetPendingActivities() []*PendingActivity {
	if x != nil {
		return x.PendingActivities
	}
	return nil
}

func (x *GetResponse) GetRunOutputs() *RunOutputs {
	if x != nil {
		return x.RunOutputs
	}
	return nil
}

type isGetResponse_Kind interface {
	isGetResponse_Kind()
}

type GetResponse_Error struct {
	Error *RunResponse_Error `protobuf:"bytes,4,opt,name=error,proto3,oneof"`
}

type GetResponse_Outputs struct {
	Outputs *Workflow_StepOutputs `protobuf:"bytes,5,opt,name=outputs,proto3,oneof"`
}

func (*GetResponse_Error) isGetResponse_Kind() {}

func (*GetResponse_Outputs) isGetResponse_Kind() {}

// PendingActivity is one activity Temporal is retrying, projected from its
// Describe response into the vocabulary this schema speaks.
//
// Deliberately not named by step. Temporal identifies a pending activity by an
// id it generated and a function name the engine registered, and neither is a
// name an author chose; the engine cannot stamp the step's id onto the activity
// either, because a loop running iterations in parallel schedules one step id
// several times at once and activity ids must be unique. Which step a running
// run is on is the progress query's answer, one field over — the two are read
// together, and a field here that claimed the step would be guessing.
type PendingActivity struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Attempt is the attempt about to run or running, starting at 1. An attempt
	// count climbing on a RUNNING run is the signature of a workload that is
	// stuck rather than working.
	Attempt int32 `protobuf:"varint,1,opt,name=attempt,proto3" json:"attempt,omitempty"`
	// LastFailure is the message of the most recent failed attempt, which is the
	// sentence that usually contains the whole diagnosis.
	LastFailure string `protobuf:"bytes,2,opt,name=last_failure,json=lastFailure,proto3" json:"last_failure,omitempty"`
	// NextAttemptScheduledTime is when the next attempt is due. Unset when an
	// attempt is running right now.
	NextAttemptScheduledTime *timestamppb.Timestamp `protobuf:"bytes,3,opt,name=next_attempt_scheduled_time,json=nextAttemptScheduledTime,proto3" json:"next_attempt_scheduled_time,omitempty"`
	// Phase is what the running attempt last said it was doing — `requesting`
	// while a request is out, `reading the response` once a peer has answered,
	// `calling the plugin` around a plugin's RPC.
	//
	// This is the field that separates "this step has been running for four
	// minutes" from knowing which end of it is slow, and the two phases an http
	// request moves through fail differently: stuck requesting is a peer that has
	// said nothing, stuck reading is a peer that answered and then stopped
	// talking.
	//
	// Unset in three situations that a reader must not confuse with a fourth.
	// The attempt may be between its start and its first report; it may be
	// waiting to be retried, in which case nothing is running to have a phase at
	// all; or the worker may be older than this field. None of those means "the
	// step is doing nothing", and a renderer that printed a blank phase as an
	// absence of work would be inventing one.
	//
	// The vocabulary is closed on purpose, and that is a security property rather
	// than a style. This is projected from an activity heartbeat, heartbeats are
	// written into workflow history, and history is durable and broadly readable —
	// so a phase built from a task's inputs would be invariant 7's exact failure.
	// The worker enforces it with a type that has no constructor; see
	// `v1.Phase` and the AST check beside it.
	Phase         string `protobuf:"bytes,4,opt,name=phase,proto3" json:"phase,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *PendingActivity) Reset() {
	*x = PendingActivity{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[36]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *PendingActivity) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*PendingActivity) ProtoMessage() {}

func (x *PendingActivity) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[36]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use PendingActivity.ProtoReflect.Descriptor instead.
func (*PendingActivity) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{36}
}

func (x *PendingActivity) GetAttempt() int32 {
	if x != nil {
		return x.Attempt
	}
	return 0
}

func (x *PendingActivity) GetLastFailure() string {
	if x != nil {
		return x.LastFailure
	}
	return ""
}

func (x *PendingActivity) GetNextAttemptScheduledTime() *timestamppb.Timestamp {
	if x != nil {
		return x.NextAttemptScheduledTime
	}
	return nil
}

func (x *PendingActivity) GetPhase() string {
	if x != nil {
		return x.Phase
	}
	return ""
}

// RunProgress is where a run has got to, answered by the running workflow itself.
//
// Not derivable from outside. A listing and a Describe both know a run is RUNNING and
// neither knows what it is running, because the position lives in the interpreter's
// own stack rather than in anything the service records. So it comes from a Temporal
// query, which is the one way to ask a workflow a question without changing it: a
// query runs against the live state, writes no history event, and cannot make the run
// do anything.
type RunProgress struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// StepId is the top-level step the run is on.
	//
	// Top-level always, which is a floor rather than a limitation. A run inside a
	// parallel block or a concurrent loop is on several steps at once and has no single
	// position — the same reason the engine refuses to suspend there — so the answer
	// that is always true is the outermost one.
	StepId string `protobuf:"bytes,1,opt,name=step_id,json=stepId,proto3" json:"step_id,omitempty"`
	// Path is the position from that step inward, where there is one: the loop and the
	// body step within it, or the branch. Empty where the run is on a top-level step,
	// and it stops at the boundary of concurrent work for the reason above.
	Path []string `protobuf:"bytes,2,rep,name=path,proto3" json:"path,omitempty"`
	// CompletedSteps is how many steps have finished in this segment of the run.
	//
	// Per segment rather than per run, and the distinction has to survive contact with
	// whoever renders it: a run that continued as new starts a new count, because the
	// budget it is measured against is per segment too. It is progress against a
	// suspend, not against the workload.
	CompletedSteps int32 `protobuf:"varint,3,opt,name=completed_steps,json=completedSteps,proto3" json:"completed_steps,omitempty"`
	unknownFields  protoimpl.UnknownFields
	sizeCache      protoimpl.SizeCache
}

func (x *RunProgress) Reset() {
	*x = RunProgress{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[37]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *RunProgress) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*RunProgress) ProtoMessage() {}

func (x *RunProgress) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[37]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use RunProgress.ProtoReflect.Descriptor instead.
func (*RunProgress) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{37}
}

func (x *RunProgress) GetStepId() string {
	if x != nil {
		return x.StepId
	}
	return ""
}

func (x *RunProgress) GetPath() []string {
	if x != nil {
		return x.Path
	}
	return nil
}

func (x *RunProgress) GetCompletedSteps() int32 {
	if x != nil {
		return x.CompletedSteps
	}
	return 0
}

// Diagnostic is one problem found in a Flowfile, positioned in its source.
//
// A schema type rather than a shape the CLI invents, because it already has three
// readers and they must agree: `flow validate --output json` hands it to whatever is
// driving the CLI, the language server converts it into an editor's own diagnostic,
// and the Validate RPC returns it unchanged. Three hand-written
// projections of one idea is how the editor and the command line come to disagree
// about what is wrong with a file.
//
// It is also what keeps `--output json` meaning one thing. On `get` and `list` that
// flag is documented as carrying the server's own schema, so a field is addressable by
// name; a `validate` that answered with an object the CLI made up would give one flag
// two meanings in one binary.
//
// # Positions are 1-based, and zero means unknown
//
// Zero rather than an omitted field, because a diagnostic with no position is a real
// answer — some problems are about a file rather than about a line in one — and a
// consumer that saw `line: 1` for those would send a reader to the wrong place with
// confidence.
type Diagnostic struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Line is the 1-based source line, or zero when the position is unknown.
	Line uint32 `protobuf:"varint,1,opt,name=line,proto3" json:"line,omitempty"`
	// Column is the 1-based column within Line, counting characters rather than
	// bytes, or zero when only the line is known.
	Column uint32 `protobuf:"varint,2,opt,name=column,proto3" json:"column,omitempty"`
	// Message states the problem and, where possible, what to do instead.
	Message string `protobuf:"bytes,3,opt,name=message,proto3" json:"message,omitempty"`
	// Step is the id of the step the problem was found in, when there is one.
	Step string `protobuf:"bytes,4,opt,name=step,proto3" json:"step,omitempty"`
	// Field names the input or property at fault, when there is one.
	Field string `protobuf:"bytes,5,opt,name=field,proto3" json:"field,omitempty"`
	// Kind names the step's kind key — the task's name, or `for_each`, `sleep` — when
	// the problem is with that key rather than with anything under it.
	//
	// Separate from Field because the two read differently and are fixed differently:
	// one sends a reader to a value they wrote under a task, the other to the word
	// naming the work itself.
	Kind string `protobuf:"bytes,6,opt,name=kind,proto3" json:"kind,omitempty"`
	// Value is the literal at fault *inside* Field, when the field holds a list and one
	// element of it is the problem.
	Value         string `protobuf:"bytes,7,opt,name=value,proto3" json:"value,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Diagnostic) Reset() {
	*x = Diagnostic{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[38]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Diagnostic) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Diagnostic) ProtoMessage() {}

func (x *Diagnostic) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[38]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Diagnostic.ProtoReflect.Descriptor instead.
func (*Diagnostic) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{38}
}

func (x *Diagnostic) GetLine() uint32 {
	if x != nil {
		return x.Line
	}
	return 0
}

func (x *Diagnostic) GetColumn() uint32 {
	if x != nil {
		return x.Column
	}
	return 0
}

func (x *Diagnostic) GetMessage() string {
	if x != nil {
		return x.Message
	}
	return ""
}

func (x *Diagnostic) GetStep() string {
	if x != nil {
		return x.Step
	}
	return ""
}

func (x *Diagnostic) GetField() string {
	if x != nil {
		return x.Field
	}
	return ""
}

func (x *Diagnostic) GetKind() string {
	if x != nil {
		return x.Kind
	}
	return ""
}

func (x *Diagnostic) GetValue() string {
	if x != nil {
		return x.Value
	}
	return ""
}

// DiagnosticReport is what one file's check produced.
//
// Per file rather than per invocation, because `flow validate a.yaml b.yaml` is two
// answers and a consumer wants to know which file each belongs to. It is also what
// makes the JSONL form useful: one report per line, one file per report.
//
// A file with no diagnostics still gets a report. "Checked and clean" and "not checked"
// are different facts, and a consumer that saw only failures could not tell them apart.
type DiagnosticReport struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// File is the path as it was given on the command line, so a reader can find it
	// again without resolving anything.
	File string `protobuf:"bytes,1,opt,name=file,proto3" json:"file,omitempty"`
	// Diagnostics is every problem found, in source order. Empty means the file is
	// valid.
	Diagnostics   []*Diagnostic `protobuf:"bytes,2,rep,name=diagnostics,proto3" json:"diagnostics,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *DiagnosticReport) Reset() {
	*x = DiagnosticReport{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[39]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *DiagnosticReport) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*DiagnosticReport) ProtoMessage() {}

func (x *DiagnosticReport) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[39]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use DiagnosticReport.ProtoReflect.Descriptor instead.
func (*DiagnosticReport) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{39}
}

func (x *DiagnosticReport) GetFile() string {
	if x != nil {
		return x.File
	}
	return ""
}

func (x *DiagnosticReport) GetDiagnostics() []*Diagnostic {
	if x != nil {
		return x.Diagnostics
	}
	return nil
}

// ValidationReport is what one check produced, across every file it was given.
//
// It exists because `json` and `jsonl` are different shapes rather than different
// spellings, and this CLI is consistent about which: `json` is one document per
// invocation, `jsonl` is one per line. Checking three files produces three
// [DiagnosticReport]s, which is three lines of `jsonl` and — without something to hold
// them — three documents where `json` promises one. A bare JSON array would do it and
// is not a proto message, so the thing that holds them is a message.
type ValidationReport struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Files is one report per file checked, in the order they were given, including the
	// files that were clean.
	Files         []*DiagnosticReport `protobuf:"bytes,1,rep,name=files,proto3" json:"files,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ValidationReport) Reset() {
	*x = ValidationReport{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[40]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ValidationReport) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ValidationReport) ProtoMessage() {}

func (x *ValidationReport) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[40]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ValidationReport.ProtoReflect.Descriptor instead.
func (*ValidationReport) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{40}
}

func (x *ValidationReport) GetFiles() []*DiagnosticReport {
	if x != nil {
		return x.Files
	}
	return nil
}

// FixChange is one edit `flow fix` made, or would make under `--check`.
//
// A schema type for the same reason [Diagnostic] is: `flow fix --output json`
// hands this to whatever is driving the CLI, and a hand-invented shape would give
// `--output json` two meanings on one binary.
type FixChange struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Line is the 1-based line the change was made at.
	Line uint32 `protobuf:"varint,1,opt,name=line,proto3" json:"line,omitempty"`
	// Message says what changed, in the terms an author wrote the file in.
	Message       string `protobuf:"bytes,2,opt,name=message,proto3" json:"message,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *FixChange) Reset() {
	*x = FixChange{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[41]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *FixChange) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*FixChange) ProtoMessage() {}

func (x *FixChange) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[41]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use FixChange.ProtoReflect.Descriptor instead.
func (*FixChange) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{41}
}

func (x *FixChange) GetLine() uint32 {
	if x != nil {
		return x.Line
	}
	return 0
}

func (x *FixChange) GetMessage() string {
	if x != nil {
		return x.Message
	}
	return ""
}

// FixReport is what `flow fix` (or `flow fix --check`) did to one file.
//
// Per file rather than per invocation, on the same reasoning as [DiagnosticReport]:
// `flow fix a.yaml b.yaml` touches two files and a consumer wants to know which
// report belongs to which. `Changed` answers "would git see a diff" on its own,
// because a file with nothing to change produces neither a change nor a refusal
// and would otherwise look indistinguishable from one that was never reached.
type FixReport struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// File is the path as it was given on the command line, so a reader can find it
	// again without resolving anything.
	File string `protobuf:"bytes,1,opt,name=file,proto3" json:"file,omitempty"`
	// Changed reports that the file was rewritten, or would be under `--check`.
	Changed bool `protobuf:"varint,2,opt,name=changed,proto3" json:"changed,omitempty"`
	// Changes is every edit made, in source order. Empty when nothing changed.
	Changes []*FixChange `protobuf:"bytes,3,rep,name=changes,proto3" json:"changes,omitempty"`
	// Refusals are the places the rewriter could not act on safely, positioned where
	// a position exists. A file with refusals is not finished whatever `Changed`
	// says, because the parts that were not refused may still have been rewritten.
	Refusals []*Diagnostic `protobuf:"bytes,4,rep,name=refusals,proto3" json:"refusals,omitempty"`
	// Notes are places worth a human's eye that are not problems and do not affect
	// whether the file is finished — a comment mentioning a step that has moved, for
	// instance. Distinct from Refusals for the reason `flow fix` draws that line
	// itself: a caller must not fail a build on one of these.
	Notes         []*Diagnostic `protobuf:"bytes,5,rep,name=notes,proto3" json:"notes,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *FixReport) Reset() {
	*x = FixReport{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[42]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *FixReport) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*FixReport) ProtoMessage() {}

func (x *FixReport) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[42]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use FixReport.ProtoReflect.Descriptor instead.
func (*FixReport) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{42}
}

func (x *FixReport) GetFile() string {
	if x != nil {
		return x.File
	}
	return ""
}

func (x *FixReport) GetChanged() bool {
	if x != nil {
		return x.Changed
	}
	return false
}

func (x *FixReport) GetChanges() []*FixChange {
	if x != nil {
		return x.Changes
	}
	return nil
}

func (x *FixReport) GetRefusals() []*Diagnostic {
	if x != nil {
		return x.Refusals
	}
	return nil
}

func (x *FixReport) GetNotes() []*Diagnostic {
	if x != nil {
		return x.Notes
	}
	return nil
}

// FmtReport is what `flow fmt` (or `flow fmt --check`) did to one file.
//
// `flow fmt` is not comment- or position-preserving the way `flow fix` is — it
// reads a file into a workflow and renders that back out from nothing — so a
// refusal here is "this file did not parse (or could not be rendered back out)"
// rather than one shape among several, and rarely carries a position of its own
// beyond what the parser's own error already gives.
type FmtReport struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// File is the path as it was given on the command line.
	File string `protobuf:"bytes,1,opt,name=file,proto3" json:"file,omitempty"`
	// Changed reports that the file was reformatted, or would be under `--check`.
	Changed bool `protobuf:"varint,2,opt,name=changed,proto3" json:"changed,omitempty"`
	// Refusals are the reasons the file was left untouched: it did not parse, or the
	// workflow it parsed into cannot be rendered back out. Positioned where the
	// parser could position it, unpositioned where the failure is only a message.
	Refusals      []*Diagnostic `protobuf:"bytes,3,rep,name=refusals,proto3" json:"refusals,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *FmtReport) Reset() {
	*x = FmtReport{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[43]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *FmtReport) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*FmtReport) ProtoMessage() {}

func (x *FmtReport) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[43]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use FmtReport.ProtoReflect.Descriptor instead.
func (*FmtReport) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{43}
}

func (x *FmtReport) GetFile() string {
	if x != nil {
		return x.File
	}
	return ""
}

func (x *FmtReport) GetChanged() bool {
	if x != nil {
		return x.Changed
	}
	return false
}

func (x *FmtReport) GetRefusals() []*Diagnostic {
	if x != nil {
		return x.Refusals
	}
	return nil
}

// FixReports is what `flow fix` produced, across every file it was given.
//
// The same reason [ValidationReport] exists beside [DiagnosticReport]: `json` is
// one document per invocation and `jsonl` is one per line, and fixing three files
// produces three [FixReport]s — which is three lines of `jsonl` and, without
// something to hold them, three documents where `json` promises one.
type FixReports struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Files is one report per file given, in the order they were given.
	Files         []*FixReport `protobuf:"bytes,1,rep,name=files,proto3" json:"files,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *FixReports) Reset() {
	*x = FixReports{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[44]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *FixReports) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*FixReports) ProtoMessage() {}

func (x *FixReports) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[44]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use FixReports.ProtoReflect.Descriptor instead.
func (*FixReports) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{44}
}

func (x *FixReports) GetFiles() []*FixReport {
	if x != nil {
		return x.Files
	}
	return nil
}

// FmtReports is what `flow fmt` produced, across every file it was given, on the
// same reasoning as [FixReports].
type FmtReports struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Files is one report per file given, in the order they were given.
	Files         []*FmtReport `protobuf:"bytes,1,rep,name=files,proto3" json:"files,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *FmtReports) Reset() {
	*x = FmtReports{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[45]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *FmtReports) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*FmtReports) ProtoMessage() {}

func (x *FmtReports) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[45]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use FmtReports.ProtoReflect.Descriptor instead.
func (*FmtReports) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{45}
}

func (x *FmtReports) GetFiles() []*FmtReport {
	if x != nil {
		return x.Files
	}
	return nil
}

// TestCase is what one test in a `*.test.yaml` file produced.
//
// Per case rather than per file, on the same reasoning as [FixReport]: a test
// file usually holds more than one `tests:` entry, and a reader wants to know
// which one failed without re-deriving it from an index.
type TestCase struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is the test's own `name:`, so a report reads the same words the
	// author wrote.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Passed is true when every expectation in the case held.
	Passed bool `protobuf:"varint,2,opt,name=passed,proto3" json:"passed,omitempty"`
	// Failures are the expectations that did not hold, positioned to the test
	// file where an expectation's own position is known — a diagnostic in the
	// same shape `flow validate` and `flow fix` already report, so a consumer of
	// `-o json` learns one schema for "something is wrong" across every verb.
	Failures []*Diagnostic `protobuf:"bytes,3,rep,name=failures,proto3" json:"failures,omitempty"`
	// Error is set instead of Failures when the case could not run to a verdict
	// at all — the workflow it names failed to compile, a stub described a task
	// the registry does not have, or the run failed in a way the case did not
	// declare with `expect.failed` — as distinct from an expectation that ran
	// and did not hold.
	Error string `protobuf:"bytes,4,opt,name=error,proto3" json:"error,omitempty"`
	// Duration is how long the case took to run, in wall-clock time. Never a
	// reading of virtual time: it is what a person deciding whether the suite
	// is still fast wants to know, and virtual time inside the run answers a
	// different question.
	Duration      *durationpb.Duration `protobuf:"bytes,5,opt,name=duration,proto3" json:"duration,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TestCase) Reset() {
	*x = TestCase{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[46]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TestCase) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TestCase) ProtoMessage() {}

func (x *TestCase) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[46]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TestCase.ProtoReflect.Descriptor instead.
func (*TestCase) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{46}
}

func (x *TestCase) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *TestCase) GetPassed() bool {
	if x != nil {
		return x.Passed
	}
	return false
}

func (x *TestCase) GetFailures() []*Diagnostic {
	if x != nil {
		return x.Failures
	}
	return nil
}

func (x *TestCase) GetError() string {
	if x != nil {
		return x.Error
	}
	return ""
}

func (x *TestCase) GetDuration() *durationpb.Duration {
	if x != nil {
		return x.Duration
	}
	return nil
}

// TestReport is what `flow test` produced for one `*.test.yaml` file.
//
// Per file rather than per invocation, on the same reasoning as
// [DiagnosticReport]: `flow test examples/` discovers more than one file and a
// consumer wants to know which report belongs to which.
type TestReport struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// File is the path as it was given on the command line, so a reader can
	// find it again without resolving anything.
	File string `protobuf:"bytes,1,opt,name=file,proto3" json:"file,omitempty"`
	// Cases is one result per `tests:` entry in the file, in the order they
	// were written. Empty when Refused is set — no case was ever reached.
	Cases []*TestCase `protobuf:"bytes,2,rep,name=cases,proto3" json:"cases,omitempty"`
	// Refused reports that the file itself could not be run as a test file — it
	// did not parse, or named a workflow that could not be loaded — which is
	// reported once for the file rather than once per case.
	Refused       string `protobuf:"bytes,3,opt,name=refused,proto3" json:"refused,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TestReport) Reset() {
	*x = TestReport{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[47]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TestReport) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TestReport) ProtoMessage() {}

func (x *TestReport) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[47]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TestReport.ProtoReflect.Descriptor instead.
func (*TestReport) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{47}
}

func (x *TestReport) GetFile() string {
	if x != nil {
		return x.File
	}
	return ""
}

func (x *TestReport) GetCases() []*TestCase {
	if x != nil {
		return x.Cases
	}
	return nil
}

func (x *TestReport) GetRefused() string {
	if x != nil {
		return x.Refused
	}
	return ""
}

// TestReports is what `flow test` produced, across every file it discovered,
// on the same reasoning as [FixReports].
type TestReports struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Files is one report per test file discovered, in the order they were
	// found.
	Files         []*TestReport `protobuf:"bytes,1,rep,name=files,proto3" json:"files,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TestReports) Reset() {
	*x = TestReports{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[48]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TestReports) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TestReports) ProtoMessage() {}

func (x *TestReports) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[48]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TestReports.ProtoReflect.Descriptor instead.
func (*TestReports) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{48}
}

func (x *TestReports) GetFiles() []*TestReport {
	if x != nil {
		return x.Files
	}
	return nil
}

// RunState is the durable workflow state used by the Temporal Run entrypoint.
// It allows the workflow to continue-as-new while carrying only the minimal
// required subset of previously produced outputs for the remaining steps.
// WorkloadIdentity describes who a run acts as.
//
// A running workload has two identities at once, and both matter. It is a
// specific workload — this workflow, this run, this step — and it is acting on
// behalf of whoever asked for the work. Downstream systems need both to make an
// authorization decision: "the nightly-reconcile workflow" and "started by the
// deploy pipeline" answer different questions, and a system granting access
// usually cares about the pair.
//
// This is carried through the run so that every step knows what it is acting as,
// and so an assertion presented to an external system states something true
// rather than something assumed. It holds identity, never credentials: it is
// persisted in workflow history, which is durable and broadly readable, so
// nothing secret may appear here.
type WorkloadIdentity struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Subject identifies the caller that requested the run, as established by
	// authenticating their request. Empty when the run was started without
	// authentication, which is only possible in development.
	Subject string `protobuf:"bytes,1,opt,name=subject,proto3" json:"subject,omitempty"`
	// Issuer is the identity provider that vouched for the subject.
	Issuer string `protobuf:"bytes,2,opt,name=issuer,proto3" json:"issuer,omitempty"`
	// Claims carries additional non-secret claims from the caller's token that
	// authorization decisions may key on, such as a repository, environment, or
	// team. Only claims an operator has configured as relevant are copied here,
	// so this does not become a dumping ground for whole tokens.
	Claims map[string]string `protobuf:"bytes,3,rep,name=claims,proto3" json:"claims,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Namespace is the tenant, and it is the boundary every authorization decision
	// about a run turns on: the caller's namespace is compared against the one
	// recorded on the run, and a mismatch is answered "no such run".
	//
	// Worth stating outright, because this field used to be described alongside
	// `deployment` as identifying "which Flowstate installation is running the
	// workload". That is what `deployment` is for. Reading the two as a pair
	// suggests both are labels for telling environments apart, and one of them
	// decides who may act on what.
	Namespace string `protobuf:"bytes,4,opt,name=namespace,proto3" json:"namespace,omitempty"`
	// Deployment identifies which Flowstate installation is running the workload,
	// so an assertion from a staging deployment is distinguishable from a
	// production one.
	//
	// Not the tenant, and not what run access turns on — that is `namespace`
	// above. But not decorative either: an outbound authorization policy sees it as
	// `workload.deployment` and may key on it, which is how a rule like
	// `target == "internal" && workload.deployment == "prod"` keeps a staging
	// installation from assuming a production role.
	//
	// So the two are load-bearing for different decisions. `namespace` decides who
	// may act on a run; `deployment` is one of the attributes a policy may use to
	// decide what a run may reach.
	Deployment    string `protobuf:"bytes,5,opt,name=deployment,proto3" json:"deployment,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *WorkloadIdentity) Reset() {
	*x = WorkloadIdentity{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[49]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *WorkloadIdentity) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*WorkloadIdentity) ProtoMessage() {}

func (x *WorkloadIdentity) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[49]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use WorkloadIdentity.ProtoReflect.Descriptor instead.
func (*WorkloadIdentity) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{49}
}

func (x *WorkloadIdentity) GetSubject() string {
	if x != nil {
		return x.Subject
	}
	return ""
}

func (x *WorkloadIdentity) GetIssuer() string {
	if x != nil {
		return x.Issuer
	}
	return ""
}

func (x *WorkloadIdentity) GetClaims() map[string]string {
	if x != nil {
		return x.Claims
	}
	return nil
}

func (x *WorkloadIdentity) GetNamespace() string {
	if x != nil {
		return x.Namespace
	}
	return ""
}

func (x *WorkloadIdentity) GetDeployment() string {
	if x != nil {
		return x.Deployment
	}
	return ""
}

// Frame is one level of the execution position within a workflow.
//
// A workflow with nested control flow cannot record where it is with a single
// index: "the third item of the loop in the second branch" is a path, not a
// number. Frames are that path, outermost first, which is what allows a run to be
// suspended and resumed from inside a loop rather than only between top-level
// steps.
type Frame struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// NextNode is the index of the next node to execute at this level.
	NextNode int32 `protobuf:"varint,1,opt,name=next_node,json=nextNode,proto3" json:"next_node,omitempty"`
	// NextIteration is the index of the next item to process, for a frame
	// standing inside a for_each.
	NextIteration int32 `protobuf:"varint,2,opt,name=next_iteration,json=nextIteration,proto3" json:"next_iteration,omitempty"`
	// Results holds the outputs of iterations already completed by a for_each at
	// this level, so a resumed run still reports every iteration's results and not
	// only those from the run that finished the loop.
	Results []*Workflow_StepOutputs `protobuf:"bytes,3,rep,name=results,proto3" json:"results,omitempty"`
	// CallOutputs holds the step outputs a called workflow's own steps had
	// produced when a run suspended partway through it, for a frame standing
	// inside a call.
	//
	// A call is transparent to suspension (see the executor's suspend rule), so a
	// callee's steps may span a Continue-As-New exactly as a top-level sequential
	// loop's do. The callee runs in its own isolated scope, though — that is the
	// entire point of [CallScope] — so unlike a top-level segment, whose step
	// outputs live in RunState.outputs, a callee's step outputs exist nowhere
	// else. Without this they would vanish at the handover and the resumed
	// segment would fail on the callee's own later steps referencing its
	// earlier ones, on a specification that never changed.
	//
	// Absent for every frame that predates this field, which reads as an empty
	// callee scope — correct, because no run written before this field existed
	// could have been suspended inside a call; the capability did not exist yet.
	CallOutputs *Workflow_StepOutputs `protobuf:"bytes,4,opt,name=call_outputs,json=callOutputs,proto3" json:"call_outputs,omitempty"`
	// CallVars holds the callee's own `vars:` block, evaluated once when the
	// call began, for a frame standing inside a call whose callee declares any.
	//
	// Computed once and carried rather than re-evaluated on each resumed
	// segment, for the identical reason a top-level run's own vars are: a
	// Continue-As-New may hand a later segment to a different interpreter
	// version under auto-upgrade (see docs/ARCHITECTURE.md's versioning
	// section), and a profile pins which functions exist rather than how
	// cel-go implements them. Re-evaluating inline would let the *same*
	// expression compute two different answers across one call, on a
	// specification that never changed — precisely the hazard
	// `engine.WorkflowVars` exists to remove at the top level. A callee's vars
	// get no weaker a guarantee merely for being nested.
	//
	// Absent for every frame that predates this field, or whose callee
	// declares no `vars:` at all — both read as "nothing bound", which is
	// correct in the first case for the reason call_outputs's is, and correct
	// in the second because there is nothing to evaluate.
	CallVars map[string]*Value `protobuf:"bytes,5,rep,name=call_vars,json=callVars,proto3" json:"call_vars,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// LoopState is the value a [Loop] was carrying when a run suspended between its
	// iterations, for a frame standing inside a loop.
	//
	// A `for_each` needs no equivalent, because its per-iteration binding is the item
	// at [NextIteration] of a list [ForEach.items] recomputes on resume — the list is
	// in the specification. A loop's carried value is not: it is computed by
	// [Loop.update] from the *previous* iteration's body outputs, which do not escape
	// the iteration and are gone once it ends. So the value itself has to travel, the
	// same way [call_outputs] carries a callee's own step outputs across the seam for
	// the same reason — the state exists nowhere in [RunState.outputs].
	//
	// A resolved value, never an expression: [Loop.update] is evaluated in workflow
	// code at the end of each iteration (invariant 4 permits it, as it does a loop's
	// `items:` and a step's `vars:`) and what is stored here is the result. So a
	// resumed segment binds the carried value directly and evaluates nothing to get
	// it — the same discipline [PendingUndo] follows, and for the same determinism
	// reason.
	//
	// Absent for every frame that predates this field, and for a loop that carries no
	// state at all — both read as "nothing carried", which is correct: a
	// retry-until loop binds no name, so there is nothing for a resumed segment to
	// rebind.
	//
	// Weighed by `CheckRunStateSize` along with the rest of [RunState] (invariant 9),
	// which is the bound a *finite* loop's state needs — it is one value, carried
	// across at most the loop's bounded iteration count. The unbounded entity loop
	// that would need a byte bound of its own is deferred; see [Loop].
	LoopState     *Value `protobuf:"bytes,6,opt,name=loop_state,json=loopState,proto3" json:"loop_state,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Frame) Reset() {
	*x = Frame{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[50]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Frame) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Frame) ProtoMessage() {}

func (x *Frame) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[50]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Frame.ProtoReflect.Descriptor instead.
func (*Frame) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{50}
}

func (x *Frame) GetNextNode() int32 {
	if x != nil {
		return x.NextNode
	}
	return 0
}

func (x *Frame) GetNextIteration() int32 {
	if x != nil {
		return x.NextIteration
	}
	return 0
}

func (x *Frame) GetResults() []*Workflow_StepOutputs {
	if x != nil {
		return x.Results
	}
	return nil
}

func (x *Frame) GetCallOutputs() *Workflow_StepOutputs {
	if x != nil {
		return x.CallOutputs
	}
	return nil
}

func (x *Frame) GetCallVars() map[string]*Value {
	if x != nil {
		return x.CallVars
	}
	return nil
}

func (x *Frame) GetLoopState() *Value {
	if x != nil {
		return x.LoopState
	}
	return nil
}

type RunState struct {
	state    protoimpl.MessageState `protogen:"open.v1"`
	Workflow *Workflow              `protobuf:"bytes,1,opt,name=workflow,proto3" json:"workflow,omitempty"`
	// Zero-based index of the next top-level step to execute.
	//
	// Superseded by frames, which can also describe a position inside nested
	// control flow. It is still honored when frames is empty, so a run started
	// before frames existed continues correctly.
	NextStep int32 `protobuf:"varint,2,opt,name=next_step,json=nextStep,proto3" json:"next_step,omitempty"`
	// Compact subset of outputs needed by subsequent steps.
	Outputs *Workflow_StepOutputs `protobuf:"bytes,3,opt,name=outputs,proto3" json:"outputs,omitempty"`
	// Step budget for Continue-As-New. If 0 or unset, server/workflow defaults apply.
	StepsBudget int32 `protobuf:"varint,4,opt,name=steps_budget,json=stepsBudget,proto3" json:"steps_budget,omitempty"`
	// Execution position, outermost frame first. When empty, execution starts from
	// next_step at the top level.
	Frames []*Frame `protobuf:"bytes,6,rep,name=frames,proto3" json:"frames,omitempty"`
	// Identity the run acts as, established when the run was requested and
	// preserved across Continue-As-New so later steps act as the same principal
	// the caller was authenticated as.
	Identity *WorkloadIdentity `protobuf:"bytes,5,opt,name=identity,proto3" json:"identity,omitempty"`
	// PendingSignals are signals that arrived before the step waiting for them was
	// reached, drained from their channels before this run suspended.
	//
	// They must be carried, because a suspended run drops whatever is still
	// buffered on a channel it never read. A workload whose approval arrived while
	// it was on an earlier step would otherwise reach the gate with the approval
	// already lost, and wait forever — the worst failure available to a feature
	// whose whole promise is that waiting is reliable.
	PendingSignals []*PendingSignal `protobuf:"bytes,7,rep,name=pending_signals,json=pendingSignals,proto3" json:"pending_signals,omitempty"`
	// Vars are the workflow's `vars:` block, already evaluated.
	//
	// Carried rather than recomputed, so that "evaluated once at the start of the run"
	// means once for the whole run and not once per Continue-As-New segment. The
	// difference is not academic: invariant 10 has a continued run pick up whichever
	// interpreter version is current, so a segment that re-evaluated its vars could
	// compute a different answer from the one earlier steps saw — a value changing
	// under a workload halfway through, for no cause visible in the file.
	//
	// Storing it also makes the evaluation happen exactly once per run rather than once
	// per segment, which is the cheaper half of the same argument.
	Vars map[string]*Value `protobuf:"bytes,8,rep,name=vars,proto3" json:"vars,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Inputs are the arguments the run was started with, already checked against
	// the workflow's declarations and with defaults filled in.
	//
	// Carried rather than re-derived, for the reason `vars` above is: the checking
	// and the defaulting happen once, at submit, and a later segment must see the
	// same values the first one did. Re-applying a default in a later segment would
	// mean a declaration edited between deploys could change an argument underneath
	// a run in flight, which is the class of thing invariant 10 exists to stop.
	//
	// Add-only and absent-tolerant, like everything else here. A run started before
	// this field existed reads back with it empty, which is exactly right — that run
	// has no arguments — so nothing has to distinguish "old writer" from "no
	// inputs", and no number is ever reused. Values only; see [RunRequest.inputs].
	Inputs map[string]*Value `protobuf:"bytes,9,rep,name=inputs,proto3" json:"inputs,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Outputs are the run's declared outputs once computed.
	//
	// Empty until the run reaches the end of its steps, and empty forever for a
	// workflow that declares none. Absent on a run started before this field
	// existed, which reads as "no declared outputs" — the same answer that run
	// would give if asked, so an old state and a new reader agree without a
	// compatibility arm.
	RunOutputs *RunOutputs `protobuf:"bytes,10,opt,name=run_outputs,json=runOutputs,proto3" json:"run_outputs,omitempty"`
	// PendingUndo are the compensations registered by steps that have already
	// succeeded, in the order they were registered.
	//
	// Carried because a saga is exactly the workload that outlives one segment: a
	// provisioning run that suspends after step two and fails at step three has to
	// undo work done by a segment that is no longer executing. Nothing else can know
	// about it — history is not replayed after a Continue-As-New, which is the whole
	// point of the seam.
	//
	// Append-only within a run, and consumed exactly once: a segment either finishes
	// the run, suspends (appending what it registered), or fails — and only the
	// failing segment runs these, after which the run ends. There is no path where a
	// segment compensates and then continues, which is what makes "a compensation
	// runs at most once" a property of the shape rather than a flag somebody checks.
	//
	// Absent on a run started before this field existed, which reads as "nothing to
	// undo" — the truth for such a run, since no specification it could be executing
	// had an `undo:` to register (invariant 10, with no compatibility arm needed).
	PendingUndo   []*PendingUndo `protobuf:"bytes,11,rep,name=pending_undo,json=pendingUndo,proto3" json:"pending_undo,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *RunState) Reset() {
	*x = RunState{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[51]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *RunState) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*RunState) ProtoMessage() {}

func (x *RunState) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[51]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use RunState.ProtoReflect.Descriptor instead.
func (*RunState) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{51}
}

func (x *RunState) GetWorkflow() *Workflow {
	if x != nil {
		return x.Workflow
	}
	return nil
}

func (x *RunState) GetNextStep() int32 {
	if x != nil {
		return x.NextStep
	}
	return 0
}

func (x *RunState) GetOutputs() *Workflow_StepOutputs {
	if x != nil {
		return x.Outputs
	}
	return nil
}

func (x *RunState) GetStepsBudget() int32 {
	if x != nil {
		return x.StepsBudget
	}
	return 0
}

func (x *RunState) GetFrames() []*Frame {
	if x != nil {
		return x.Frames
	}
	return nil
}

func (x *RunState) GetIdentity() *WorkloadIdentity {
	if x != nil {
		return x.Identity
	}
	return nil
}

func (x *RunState) GetPendingSignals() []*PendingSignal {
	if x != nil {
		return x.PendingSignals
	}
	return nil
}

func (x *RunState) GetVars() map[string]*Value {
	if x != nil {
		return x.Vars
	}
	return nil
}

func (x *RunState) GetInputs() map[string]*Value {
	if x != nil {
		return x.Inputs
	}
	return nil
}

func (x *RunState) GetRunOutputs() *RunOutputs {
	if x != nil {
		return x.RunOutputs
	}
	return nil
}

func (x *RunState) GetPendingUndo() []*PendingUndo {
	if x != nil {
		return x.PendingUndo
	}
	return nil
}

// SignalRequest delivers a signal to a waiting run.
type SignalRequest struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// WorkflowId identifies the run to signal.
	//
	// A caller may address only runs in a namespace their own authenticated
	// identity establishes. The namespace is never taken from this request: a
	// caller who could name their own tenant could name someone else's, and an id
	// is not a capability.
	WorkflowId string `protobuf:"bytes,1,opt,name=workflow_id,json=workflowId,proto3" json:"workflow_id,omitempty"`
	// RunId optionally pins the signal to one run of the workload. Unset addresses
	// whichever run is current, which is what a human approving a gate means: they
	// are approving the workload, and they neither know nor care how many times it
	// has been continued as new.
	RunId string `protobuf:"bytes,2,opt,name=run_id,json=runId,proto3" json:"run_id,omitempty"`
	// Name is the signal being sent, matching what the waiting step declared.
	Name string `protobuf:"bytes,3,opt,name=name,proto3" json:"name,omitempty"`
	// Payload becomes the waiting step's outputs, so `${approval.approved}`
	// resolves to what a sender put here.
	Payload       *Node_Outputs `protobuf:"bytes,4,opt,name=payload,proto3" json:"payload,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *SignalRequest) Reset() {
	*x = SignalRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[52]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *SignalRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*SignalRequest) ProtoMessage() {}

func (x *SignalRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[52]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use SignalRequest.ProtoReflect.Descriptor instead.
func (*SignalRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{52}
}

func (x *SignalRequest) GetWorkflowId() string {
	if x != nil {
		return x.WorkflowId
	}
	return ""
}

func (x *SignalRequest) GetRunId() string {
	if x != nil {
		return x.RunId
	}
	return ""
}

func (x *SignalRequest) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *SignalRequest) GetPayload() *Node_Outputs {
	if x != nil {
		return x.Payload
	}
	return nil
}

type SignalResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *SignalResponse) Reset() {
	*x = SignalResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[53]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *SignalResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*SignalResponse) ProtoMessage() {}

func (x *SignalResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[53]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use SignalResponse.ProtoReflect.Descriptor instead.
func (*SignalResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{53}
}

// CancelRequest asks a run to stop, letting it clean up on the way out.
type CancelRequest struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// WorkflowId identifies the run to cancel.
	//
	// A caller may address only runs in a namespace their own authenticated
	// identity establishes. The namespace is never taken from this request: a
	// caller who could name their own tenant could name someone else's, and an id
	// is not a capability.
	WorkflowId string `protobuf:"bytes,1,opt,name=workflow_id,json=workflowId,proto3" json:"workflow_id,omitempty"`
	// RunId optionally pins the request to one run of the workload. Unset
	// addresses whichever run is current, which is what someone stopping a
	// workload means: they neither know nor care how many times it has been
	// continued as new.
	RunId         string `protobuf:"bytes,2,opt,name=run_id,json=runId,proto3" json:"run_id,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *CancelRequest) Reset() {
	*x = CancelRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[54]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *CancelRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*CancelRequest) ProtoMessage() {}

func (x *CancelRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[54]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use CancelRequest.ProtoReflect.Descriptor instead.
func (*CancelRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{54}
}

func (x *CancelRequest) GetWorkflowId() string {
	if x != nil {
		return x.WorkflowId
	}
	return ""
}

func (x *CancelRequest) GetRunId() string {
	if x != nil {
		return x.RunId
	}
	return ""
}

type CancelResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *CancelResponse) Reset() {
	*x = CancelResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[55]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *CancelResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*CancelResponse) ProtoMessage() {}

func (x *CancelResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[55]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use CancelResponse.ProtoReflect.Descriptor instead.
func (*CancelResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{55}
}

// TerminateRequest stops a run immediately, without letting it clean up.
type TerminateRequest struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// WorkflowId identifies the run to terminate.
	//
	// A caller may address only runs in a namespace their own authenticated
	// identity establishes. The namespace is never taken from this request: a
	// caller who could name their own tenant could name someone else's, and an id
	// is not a capability.
	WorkflowId string `protobuf:"bytes,1,opt,name=workflow_id,json=workflowId,proto3" json:"workflow_id,omitempty"`
	// RunId optionally pins the request to one run of the workload. Unset
	// addresses whichever run is current.
	RunId string `protobuf:"bytes,2,opt,name=run_id,json=runId,proto3" json:"run_id,omitempty"`
	// Reason is recorded on the terminated execution, so whoever finds the run
	// later learns why it was stopped rather than only that it was.
	//
	// A terminated run leaves no trace of its own explaining itself — that is what
	// terminate means — so this is the only account of the decision there will be.
	Reason        string `protobuf:"bytes,3,opt,name=reason,proto3" json:"reason,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TerminateRequest) Reset() {
	*x = TerminateRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[56]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TerminateRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TerminateRequest) ProtoMessage() {}

func (x *TerminateRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[56]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TerminateRequest.ProtoReflect.Descriptor instead.
func (*TerminateRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{56}
}

func (x *TerminateRequest) GetWorkflowId() string {
	if x != nil {
		return x.WorkflowId
	}
	return ""
}

func (x *TerminateRequest) GetRunId() string {
	if x != nil {
		return x.RunId
	}
	return ""
}

func (x *TerminateRequest) GetReason() string {
	if x != nil {
		return x.Reason
	}
	return ""
}

type TerminateResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TerminateResponse) Reset() {
	*x = TerminateResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[57]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TerminateResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TerminateResponse) ProtoMessage() {}

func (x *TerminateResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[57]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TerminateResponse.ProtoReflect.Descriptor instead.
func (*TerminateResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{57}
}

// ListRequest asks for the caller's runs.
type ListRequest struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// PageSize bounds how many runs come back. Zero takes the server's default.
	//
	// This bounds the answer, not the work: the server filters by tenant itself
	// (see ListResponse.next_page_token), so a small page is not a promise of a
	// small scan.
	PageSize int32 `protobuf:"varint,1,opt,name=page_size,json=pageSize,proto3" json:"page_size,omitempty"`
	// PageToken continues a previous List. Opaque: it encodes where the scan
	// stopped, and nothing else should be read from or written into it.
	PageToken string `protobuf:"bytes,2,opt,name=page_token,json=pageToken,proto3" json:"page_token,omitempty"`
	// Filter keeps only the runs a CEL expression answers yes about.
	//
	// CEL because that is what everything else an author writes here is: the
	// expressions in a Flowfile, the rules in an egress or auth policy, the
	// condition on a step. A query language of its own would be a second grammar
	// to learn, a second parser to bound, and a second place for `status` to mean
	// something slightly different.
	//
	// The names it binds are a run's own: `workflow_id`, `run_id`, `status`,
	// `start_time`, `close_time`, `finished`. `status` is the short name —
	// `FAILED`, not `STATUS_FAILED` — and comparing it to a name no status has is
	// refused rather than silently matching nothing.
	//
	//	status == "FAILED"
	//	status == "RUNNING" && start_time < timestamp("2026-08-01T00:00:00Z")
	//	finished && close_time - start_time > duration("1h")
	//
	// `close_time` is null while a run is going — null rather than the epoch,
	// because a run that has not finished has no close time and a filter that
	// compared one against a date would otherwise report every running run as
	// having finished in 1970. Comparing null errors, which is why `finished`
	// exists: CEL's `&&` absorbs errors from the side it does not need, so the
	// guarded form above does what it looks like it does.
	//
	// # What this bounds, and what it does not
	//
	// A filter narrows the *answer*, never the work. The tenant a run belongs to
	// is a memo, which Temporal cannot query, so a listing is already a bounded
	// scan with a predicate applied to each result — and a filter that matches
	// little makes a page more likely to come back short with a token, not more
	// likely to scan further. The scan bounds are unchanged and unaffected by what
	// is written here.
	//
	// The evaluation itself is cost-bounded per run, well below the budget an
	// ordinary expression gets, because this one is evaluated once per execution
	// read and the two multiply.
	//
	// # Room this leaves
	//
	// When a deployment registers search attributes, the parts of a filter the
	// visibility store can answer become a query pushed down into it, and the rest
	// stays a predicate here. That changes what a listing costs and never what it
	// means: the same filter returns the same runs either way, which is what lets
	// an operator turn pushdown on without re-reading every saved query.
	Filter        string `protobuf:"bytes,3,opt,name=filter,proto3" json:"filter,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ListRequest) Reset() {
	*x = ListRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[58]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ListRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ListRequest) ProtoMessage() {}

func (x *ListRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[58]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ListRequest.ProtoReflect.Descriptor instead.
func (*ListRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{58}
}

func (x *ListRequest) GetPageSize() int32 {
	if x != nil {
		return x.PageSize
	}
	return 0
}

func (x *ListRequest) GetPageToken() string {
	if x != nil {
		return x.PageToken
	}
	return ""
}

func (x *ListRequest) GetFilter() string {
	if x != nil {
		return x.Filter
	}
	return ""
}

// RunSummary describes one run in a listing.
//
// Deliberately not a run's outputs. A listing is about which workloads exist and
// what they are doing; reading what one produced is Get, which is one run and one
// authorization decision. A list that returned outputs would make "show me my
// runs" the cheapest way to read every workload's data at once.
type RunSummary struct {
	state      protoimpl.MessageState `protogen:"open.v1"`
	WorkflowId string                 `protobuf:"bytes,1,opt,name=workflow_id,json=workflowId,proto3" json:"workflow_id,omitempty"`
	RunId      string                 `protobuf:"bytes,2,opt,name=run_id,json=runId,proto3" json:"run_id,omitempty"`
	Status     RunResponse_Status     `protobuf:"varint,3,opt,name=status,proto3,enum=flowstate.v1.RunResponse_Status" json:"status,omitempty"`
	// StartTime is when the workload began.
	StartTime *timestamppb.Timestamp `protobuf:"bytes,4,opt,name=start_time,json=startTime,proto3" json:"start_time,omitempty"`
	// CloseTime is when it finished, unset while it is still running.
	CloseTime *timestamppb.Timestamp `protobuf:"bytes,5,opt,name=close_time,json=closeTime,proto3" json:"close_time,omitempty"`
	// Name is the workflow's own declared name (Workflow.name), not Temporal's
	// WorkflowType — every run's WorkflowType is "Run", the one interpreter
	// workflow, so it cannot distinguish "nightly-etl" from "onboard-tenant".
	//
	// Populated from the FlowstateWorkflowName search attribute when the
	// deployment has one registered and the run recorded it; empty otherwise,
	// which includes every run started before this field existed and every run
	// in a deployment where registration never succeeded. Absence is not an
	// error — see server/list.go and runfilter.go, where a filter comparing
	// against `name` simply does not match a run that carries none.
	Name          string `protobuf:"bytes,6,opt,name=name,proto3" json:"name,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *RunSummary) Reset() {
	*x = RunSummary{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[59]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *RunSummary) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*RunSummary) ProtoMessage() {}

func (x *RunSummary) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[59]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use RunSummary.ProtoReflect.Descriptor instead.
func (*RunSummary) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{59}
}

func (x *RunSummary) GetWorkflowId() string {
	if x != nil {
		return x.WorkflowId
	}
	return ""
}

func (x *RunSummary) GetRunId() string {
	if x != nil {
		return x.RunId
	}
	return ""
}

func (x *RunSummary) GetStatus() RunResponse_Status {
	if x != nil {
		return x.Status
	}
	return RunResponse_STATUS_UNSPECIFIED
}

func (x *RunSummary) GetStartTime() *timestamppb.Timestamp {
	if x != nil {
		return x.StartTime
	}
	return nil
}

func (x *RunSummary) GetCloseTime() *timestamppb.Timestamp {
	if x != nil {
		return x.CloseTime
	}
	return nil
}

func (x *RunSummary) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

// ListResponse returns a page of the caller's runs.
type ListResponse struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	Runs  []*RunSummary          `protobuf:"bytes,1,rep,name=runs,proto3" json:"runs,omitempty"`
	// NextPageToken continues the listing, and is set whenever the scan stopped
	// with more to look at — including when this page came back short or empty.
	//
	// That case is real rather than theoretical. The tenant a run belongs to is
	// recorded as a memo, which Temporal cannot filter on, so the server reads a
	// bounded number of executions and keeps the ones that are the caller's. In a
	// namespace shared by several tenants a scan can therefore end having found
	// few matches, or none, while plenty remain. An empty page with a token set
	// means "keep asking", and only an empty token means the listing is done.
	NextPageToken string `protobuf:"bytes,2,opt,name=next_page_token,json=nextPageToken,proto3" json:"next_page_token,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ListResponse) Reset() {
	*x = ListResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[60]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ListResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ListResponse) ProtoMessage() {}

func (x *ListResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[60]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ListResponse.ProtoReflect.Descriptor instead.
func (*ListResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{60}
}

func (x *ListResponse) GetRuns() []*RunSummary {
	if x != nil {
		return x.Runs
	}
	return nil
}

func (x *ListResponse) GetNextPageToken() string {
	if x != nil {
		return x.NextPageToken
	}
	return ""
}

// SourceFile is one file's contents, with the name to report diagnostics against.
type SourceFile struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is what a diagnostic should call this file. It is a label, not a path
	// the server resolves — nothing here opens anything — so a caller with no
	// filesystem may say `<stdin>` or a request id and still get a readable report.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Source is the file's bytes.
	//
	// Bounded at the same megabyte the offline parser bounds a Flowfile at, and for
	// the same reason: this is input an outside party chooses, and the resource it
	// spends is memory in the parser. A caller sending more is refused by
	// protovalidate before anything reads it.
	Source        []byte `protobuf:"bytes,2,opt,name=source,proto3" json:"source,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *SourceFile) Reset() {
	*x = SourceFile{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[61]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *SourceFile) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*SourceFile) ProtoMessage() {}

func (x *SourceFile) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[61]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use SourceFile.ProtoReflect.Descriptor instead.
func (*SourceFile) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{61}
}

func (x *SourceFile) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *SourceFile) GetSource() []byte {
	if x != nil {
		return x.Source
	}
	return nil
}

// ValidateRequest is one or more files to check.
type ValidateRequest struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Files are the files to check, in the order the report should list them.
	//
	// Bounded by count as well as by size, because bounding one resource does not
	// bound another the peer controls the ratio to: a thousand megabyte files is a
	// gigabyte of parsing, and every one of them is individually within its bound.
	Files         []*SourceFile `protobuf:"bytes,1,rep,name=files,proto3" json:"files,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ValidateRequest) Reset() {
	*x = ValidateRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[62]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ValidateRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ValidateRequest) ProtoMessage() {}

func (x *ValidateRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[62]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ValidateRequest.ProtoReflect.Descriptor instead.
func (*ValidateRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{62}
}

func (x *ValidateRequest) GetFiles() []*SourceFile {
	if x != nil {
		return x.Files
	}
	return nil
}

// ValidateResponse is what the check found.
type ValidateResponse struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Report is one entry per file, including the files that were clean.
	//
	// Wrapped rather than returned bare so the RPC can grow a second thing to say —
	// which version of the grammar answered, how long it took — without changing
	// the shape a caller already parses.
	Report        *ValidationReport `protobuf:"bytes,1,opt,name=report,proto3" json:"report,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ValidateResponse) Reset() {
	*x = ValidateResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[63]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ValidateResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ValidateResponse) ProtoMessage() {}

func (x *ValidateResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[63]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ValidateResponse.ProtoReflect.Descriptor instead.
func (*ValidateResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{63}
}

func (x *ValidateResponse) GetReport() *ValidationReport {
	if x != nil {
		return x.Report
	}
	return nil
}

// CompileRequest is one Flowfile to compile.
//
// One rather than many, unlike ValidateRequest: validation is a survey and its
// answer is a report, while compilation produces the artifact the next call
// submits, and a batch of artifacts would leave a caller matching outputs back
// to inputs by position.
type CompileRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	File          *SourceFile            `protobuf:"bytes,1,opt,name=file,proto3" json:"file,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *CompileRequest) Reset() {
	*x = CompileRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[64]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *CompileRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*CompileRequest) ProtoMessage() {}

func (x *CompileRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[64]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use CompileRequest.ProtoReflect.Descriptor instead.
func (*CompileRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{64}
}

func (x *CompileRequest) GetFile() *SourceFile {
	if x != nil {
		return x.File
	}
	return nil
}

// CompileResponse is the specification, or the reasons there is not one.
type CompileResponse struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Workflow is the compiled specification, set only when the file compiled
	// clean. It is exactly what RunRequest.workflow takes.
	Workflow *Workflow `protobuf:"bytes,1,opt,name=workflow,proto3" json:"workflow,omitempty"`
	// Report carries the diagnostics for the file, present even when empty so
	// "compiled clean" is stated rather than inferred from absence.
	Report        *DiagnosticReport `protobuf:"bytes,2,opt,name=report,proto3" json:"report,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *CompileResponse) Reset() {
	*x = CompileResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[65]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *CompileResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*CompileResponse) ProtoMessage() {}

func (x *CompileResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[65]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use CompileResponse.ProtoReflect.Descriptor instead.
func (*CompileResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{65}
}

func (x *CompileResponse) GetWorkflow() *Workflow {
	if x != nil {
		return x.Workflow
	}
	return nil
}

func (x *CompileResponse) GetReport() *DiagnosticReport {
	if x != nil {
		return x.Report
	}
	return nil
}

// GetCatalogRequest asks what this deployment can execute. It takes nothing
// today; a filter or a profile selector would go here.
type GetCatalogRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *GetCatalogRequest) Reset() {
	*x = GetCatalogRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[66]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *GetCatalogRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*GetCatalogRequest) ProtoMessage() {}

func (x *GetCatalogRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[66]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use GetCatalogRequest.ProtoReflect.Descriptor instead.
func (*GetCatalogRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{66}
}

// GetCatalogResponse is the deployment's capability.
type GetCatalogResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Catalog       *TaskCatalog           `protobuf:"bytes,1,opt,name=catalog,proto3" json:"catalog,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *GetCatalogResponse) Reset() {
	*x = GetCatalogResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[67]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *GetCatalogResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*GetCatalogResponse) ProtoMessage() {}

func (x *GetCatalogResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[67]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use GetCatalogResponse.ProtoReflect.Descriptor instead.
func (*GetCatalogResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{67}
}

func (x *GetCatalogResponse) GetCatalog() *TaskCatalog {
	if x != nil {
		return x.Catalog
	}
	return nil
}

// ScheduleActionResult is one firing that happened.
type ScheduleActionResult struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// ScheduleTime is the moment the firing was due, including any jitter.
	ScheduleTime *timestamppb.Timestamp `protobuf:"bytes,1,opt,name=schedule_time,json=scheduleTime,proto3" json:"schedule_time,omitempty"`
	// ActualTime is when the run was actually started.
	ActualTime *timestamppb.Timestamp `protobuf:"bytes,2,opt,name=actual_time,json=actualTime,proto3" json:"actual_time,omitempty"`
	// WorkflowId is the run the firing started, which `flow get` takes.
	WorkflowId string `protobuf:"bytes,3,opt,name=workflow_id,json=workflowId,proto3" json:"workflow_id,omitempty"`
	// RunId is the first execution of that workload. A run that continues as new
	// has later executions this does not name; see RunResponse for why a workflow
	// id is the durable handle and a run id is a position.
	RunId         string `protobuf:"bytes,4,opt,name=run_id,json=runId,proto3" json:"run_id,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ScheduleActionResult) Reset() {
	*x = ScheduleActionResult{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[68]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ScheduleActionResult) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ScheduleActionResult) ProtoMessage() {}

func (x *ScheduleActionResult) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[68]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ScheduleActionResult.ProtoReflect.Descriptor instead.
func (*ScheduleActionResult) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{68}
}

func (x *ScheduleActionResult) GetScheduleTime() *timestamppb.Timestamp {
	if x != nil {
		return x.ScheduleTime
	}
	return nil
}

func (x *ScheduleActionResult) GetActualTime() *timestamppb.Timestamp {
	if x != nil {
		return x.ActualTime
	}
	return nil
}

func (x *ScheduleActionResult) GetWorkflowId() string {
	if x != nil {
		return x.WorkflowId
	}
	return ""
}

func (x *ScheduleActionResult) GetRunId() string {
	if x != nil {
		return x.RunId
	}
	return ""
}

// ScheduleSummary describes one schedule in a listing.
//
// Deliberately not the whole description: a listing answers which schedules exist
// and whether they are live, and reading what one is configured to pass a run is
// DescribeSchedule — one schedule and one authorization decision, the same split
// [RunSummary] makes for runs.
type ScheduleSummary struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is what this tenant calls the schedule.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Paused is whether firings are currently suppressed.
	Paused bool `protobuf:"varint,2,opt,name=paused,proto3" json:"paused,omitempty"`
	// Note is the human-readable message carried on the schedule, which is where
	// the reason for a pause is written — by whoever paused it, or by Temporal when
	// a failure paused it.
	Note string `protobuf:"bytes,3,opt,name=note,proto3" json:"note,omitempty"`
	// NextRunTime is when this would next fire, unset when it is paused or when
	// the cluster has no next time for it.
	NextRunTime   *timestamppb.Timestamp `protobuf:"bytes,4,opt,name=next_run_time,json=nextRunTime,proto3" json:"next_run_time,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ScheduleSummary) Reset() {
	*x = ScheduleSummary{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[69]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ScheduleSummary) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ScheduleSummary) ProtoMessage() {}

func (x *ScheduleSummary) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[69]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ScheduleSummary.ProtoReflect.Descriptor instead.
func (*ScheduleSummary) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{69}
}

func (x *ScheduleSummary) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *ScheduleSummary) GetPaused() bool {
	if x != nil {
		return x.Paused
	}
	return false
}

func (x *ScheduleSummary) GetNote() string {
	if x != nil {
		return x.Note
	}
	return ""
}

func (x *ScheduleSummary) GetNextRunTime() *timestamppb.Timestamp {
	if x != nil {
		return x.NextRunTime
	}
	return nil
}

// ScheduleDescription is one schedule in full.
type ScheduleDescription struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is what this tenant calls the schedule.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// WorkflowName is the workflow a firing runs, read back out of the
	// specification the schedule carries.
	WorkflowName string `protobuf:"bytes,2,opt,name=workflow_name,json=workflowName,proto3" json:"workflow_name,omitempty"`
	// Trigger is the cadence, as it was created — the same message a Flowfile's
	// `triggers.schedule` compiles to, so what comes back out reads like what went
	// in.
	//
	// Read back out of the specification the schedule stores, which is where the
	// author's own words survive. Temporal translates a cron expression into a
	// calendar spec when it stores one, so asking the cluster what the cadence is
	// answers `0 9 * * MON-FRI` as a set of calendar ranges — true, and not what
	// anybody wrote or would recognise.
	//
	// The two cannot disagree, because CreateSchedule is the only thing that writes
	// either and writes them from one value. What can diverge is a schedule edited
	// out from under Flowstate with the `temporal` CLI; that is visible there, and
	// `flow schedule describe` does not pretend to be the cluster's own view of its
	// own object.
	Trigger *ScheduleTrigger `protobuf:"bytes,3,opt,name=trigger,proto3" json:"trigger,omitempty"`
	// Paused is whether firings are currently suppressed.
	Paused bool `protobuf:"varint,4,opt,name=paused,proto3" json:"paused,omitempty"`
	// Note is the message carried on the schedule; see [ScheduleSummary.note].
	Note string `protobuf:"bytes,5,opt,name=note,proto3" json:"note,omitempty"`
	// NumActions is how many firings this schedule has taken, including manual
	// ones.
	NumActions int64 `protobuf:"varint,6,opt,name=num_actions,json=numActions,proto3" json:"num_actions,omitempty"`
	// NextRunTimes are the next few moments this is due to fire. Empty while
	// paused.
	NextRunTimes []*timestamppb.Timestamp `protobuf:"bytes,7,rep,name=next_run_times,json=nextRunTimes,proto3" json:"next_run_times,omitempty"`
	// RecentRuns are the last few firings, oldest first.
	RecentRuns []*ScheduleActionResult `protobuf:"bytes,8,rep,name=recent_runs,json=recentRuns,proto3" json:"recent_runs,omitempty"`
	// Inputs are the arguments every firing starts its run with, bound and
	// type-checked when the schedule was created.
	//
	// Reported because a schedule that has been running for a month is a thing
	// somebody eventually has to explain, and "what is it passing" is the first
	// question. Absent rather than empty when the stored specification cannot be
	// read back — a schedule created by a build whose message shape has since moved
	// is describable in every other respect, and failing the whole description over
	// one field would take away the answer along with the doubt.
	Inputs        map[string]*Value `protobuf:"bytes,9,rep,name=inputs,proto3" json:"inputs,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ScheduleDescription) Reset() {
	*x = ScheduleDescription{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[70]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ScheduleDescription) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ScheduleDescription) ProtoMessage() {}

func (x *ScheduleDescription) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[70]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ScheduleDescription.ProtoReflect.Descriptor instead.
func (*ScheduleDescription) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{70}
}

func (x *ScheduleDescription) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *ScheduleDescription) GetWorkflowName() string {
	if x != nil {
		return x.WorkflowName
	}
	return ""
}

func (x *ScheduleDescription) GetTrigger() *ScheduleTrigger {
	if x != nil {
		return x.Trigger
	}
	return nil
}

func (x *ScheduleDescription) GetPaused() bool {
	if x != nil {
		return x.Paused
	}
	return false
}

func (x *ScheduleDescription) GetNote() string {
	if x != nil {
		return x.Note
	}
	return ""
}

func (x *ScheduleDescription) GetNumActions() int64 {
	if x != nil {
		return x.NumActions
	}
	return 0
}

func (x *ScheduleDescription) GetNextRunTimes() []*timestamppb.Timestamp {
	if x != nil {
		return x.NextRunTimes
	}
	return nil
}

func (x *ScheduleDescription) GetRecentRuns() []*ScheduleActionResult {
	if x != nil {
		return x.RecentRuns
	}
	return nil
}

func (x *ScheduleDescription) GetInputs() map[string]*Value {
	if x != nil {
		return x.Inputs
	}
	return nil
}

// CreateScheduleRequest asks for a schedule that runs a workflow on a cadence.
type CreateScheduleRequest struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Workflow is the specification every firing runs, exactly as [RunRequest]
	// takes it — the same compiled artifact, validated and size-checked the same
	// way.
	//
	// The whole specification rather than a reference to one stored elsewhere,
	// because there is nowhere else: Flowstate has no workflow registry, and a
	// schedule that named a file would be a schedule that breaks when somebody
	// moves it. What fires a year from now is what was created, which is also what
	// makes a schedule reviewable.
	Workflow *Workflow `protobuf:"bytes,1,opt,name=workflow,proto3" json:"workflow,omitempty"`
	// Inputs are the arguments every firing starts its run with.
	//
	// Bound and type-checked *here*, once, while whoever is creating the schedule
	// is still present to be told (invariant 6). The alternative — checking at each
	// firing — puts the refusal at three in the morning in a worker's log, for a
	// mistake made at a keyboard weeks earlier, which is the failure this whole
	// repository's fail-closed rule is about.
	//
	// Same contract as [RunRequest.inputs] and through the same
	// `v1.BindRunInputs`: values and never expressions, no undeclared name, every
	// required input present, defaults filled in. What is stored is the bound map,
	// so a declaration edited later does not change what a schedule already created
	// passes.
	Inputs map[string]*Value `protobuf:"bytes,2,rep,name=inputs,proto3" json:"inputs,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Name is what this tenant will call the schedule. Empty takes the workflow's
	// own name, which is the right default and usually the only name anybody wants.
	//
	// Given explicitly when one workflow wants two cadences — a report that runs
	// hourly for one region and nightly for another is one file and two schedules —
	// which is exactly when a generated id would be useless.
	Name string `protobuf:"bytes,3,opt,name=name,proto3" json:"name,omitempty"`
	// Paused creates the schedule without letting it fire.
	//
	// A property of the act and not of the file, which is why it is here rather
	// than on [ScheduleTrigger]: the file says what the cadence is, and whether this
	// deployment is currently honouring it is the deployment's answer. It is also
	// what makes creating one safe to rehearse — create paused, describe it, and
	// resume when the next firing time is one somebody meant.
	Paused        bool `protobuf:"varint,4,opt,name=paused,proto3" json:"paused,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *CreateScheduleRequest) Reset() {
	*x = CreateScheduleRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[71]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *CreateScheduleRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*CreateScheduleRequest) ProtoMessage() {}

func (x *CreateScheduleRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[71]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use CreateScheduleRequest.ProtoReflect.Descriptor instead.
func (*CreateScheduleRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{71}
}

func (x *CreateScheduleRequest) GetWorkflow() *Workflow {
	if x != nil {
		return x.Workflow
	}
	return nil
}

func (x *CreateScheduleRequest) GetInputs() map[string]*Value {
	if x != nil {
		return x.Inputs
	}
	return nil
}

func (x *CreateScheduleRequest) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *CreateScheduleRequest) GetPaused() bool {
	if x != nil {
		return x.Paused
	}
	return false
}

// CreateScheduleResponse confirms the schedule and describes what was created.
type CreateScheduleResponse struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Schedule is the created schedule as the cluster now holds it, so a caller
	// sees the next firing times without a second request — which is the one fact
	// that most often reveals a cadence meaning something other than what was
	// intended.
	Schedule      *ScheduleDescription `protobuf:"bytes,1,opt,name=schedule,proto3" json:"schedule,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *CreateScheduleResponse) Reset() {
	*x = CreateScheduleResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[72]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *CreateScheduleResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*CreateScheduleResponse) ProtoMessage() {}

func (x *CreateScheduleResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[72]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use CreateScheduleResponse.ProtoReflect.Descriptor instead.
func (*CreateScheduleResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{72}
}

func (x *CreateScheduleResponse) GetSchedule() *ScheduleDescription {
	if x != nil {
		return x.Schedule
	}
	return nil
}

// ListSchedulesRequest asks for the caller's schedules. It takes nothing today.
//
// # No paging, deliberately, and what that costs
//
// `List` pages runs because a namespace holds them by the hundred thousand and
// the tenant filter is a memo Temporal cannot query, so an unbounded listing is a
// denial of service waiting to be written. Schedules are not that: they are
// created one at a time by people, and a deployment with a thousand of them has a
// different problem.
//
// The scan is still bounded — see `maxScheduleScan` — and when the bound stops it
// the response says so rather than presenting a partial listing as the whole of
// it. That is a smaller promise than `List` makes, and it is stated instead of
// implied. A `page_token` is a field 1 and 2 away on either message the day a
// deployment needs it, with nothing existing to break.
type ListSchedulesRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ListSchedulesRequest) Reset() {
	*x = ListSchedulesRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[73]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ListSchedulesRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ListSchedulesRequest) ProtoMessage() {}

func (x *ListSchedulesRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[73]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ListSchedulesRequest.ProtoReflect.Descriptor instead.
func (*ListSchedulesRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{73}
}

// ListSchedulesResponse returns the caller's schedules.
type ListSchedulesResponse struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Schedules are the caller's, in whatever order the cluster returned them.
	Schedules []*ScheduleSummary `protobuf:"bytes,1,rep,name=schedules,proto3" json:"schedules,omitempty"`
	// Truncated reports that the scan stopped at its bound with schedules still
	// unexamined, so this listing is not the whole of it.
	//
	// Said rather than hidden, on the same reasoning as [ListResponse.next_page_token]:
	// a caller concluding from a short answer that they have seen everything is the
	// failure worth preventing, and it is worse than an answer that admits its own
	// limit.
	Truncated     bool `protobuf:"varint,2,opt,name=truncated,proto3" json:"truncated,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ListSchedulesResponse) Reset() {
	*x = ListSchedulesResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[74]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ListSchedulesResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ListSchedulesResponse) ProtoMessage() {}

func (x *ListSchedulesResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[74]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ListSchedulesResponse.ProtoReflect.Descriptor instead.
func (*ListSchedulesResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{74}
}

func (x *ListSchedulesResponse) GetSchedules() []*ScheduleSummary {
	if x != nil {
		return x.Schedules
	}
	return nil
}

func (x *ListSchedulesResponse) GetTruncated() bool {
	if x != nil {
		return x.Truncated
	}
	return false
}

// DescribeScheduleRequest asks about one of the caller's schedules by name.
type DescribeScheduleRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Name          string                 `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *DescribeScheduleRequest) Reset() {
	*x = DescribeScheduleRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[75]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *DescribeScheduleRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*DescribeScheduleRequest) ProtoMessage() {}

func (x *DescribeScheduleRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[75]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use DescribeScheduleRequest.ProtoReflect.Descriptor instead.
func (*DescribeScheduleRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{75}
}

func (x *DescribeScheduleRequest) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

// DescribeScheduleResponse is the schedule in full.
type DescribeScheduleResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Schedule      *ScheduleDescription   `protobuf:"bytes,1,opt,name=schedule,proto3" json:"schedule,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *DescribeScheduleResponse) Reset() {
	*x = DescribeScheduleResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[76]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *DescribeScheduleResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*DescribeScheduleResponse) ProtoMessage() {}

func (x *DescribeScheduleResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[76]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use DescribeScheduleResponse.ProtoReflect.Descriptor instead.
func (*DescribeScheduleResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{76}
}

func (x *DescribeScheduleResponse) GetSchedule() *ScheduleDescription {
	if x != nil {
		return x.Schedule
	}
	return nil
}

// DeleteScheduleRequest removes one of the caller's schedules.
type DeleteScheduleRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Name          string                 `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *DeleteScheduleRequest) Reset() {
	*x = DeleteScheduleRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[77]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *DeleteScheduleRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*DeleteScheduleRequest) ProtoMessage() {}

func (x *DeleteScheduleRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[77]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use DeleteScheduleRequest.ProtoReflect.Descriptor instead.
func (*DeleteScheduleRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{77}
}

func (x *DeleteScheduleRequest) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

// DeleteScheduleResponse is empty: a deleted schedule has nothing to report.
type DeleteScheduleResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *DeleteScheduleResponse) Reset() {
	*x = DeleteScheduleResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[78]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *DeleteScheduleResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*DeleteScheduleResponse) ProtoMessage() {}

func (x *DeleteScheduleResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[78]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use DeleteScheduleResponse.ProtoReflect.Descriptor instead.
func (*DeleteScheduleResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{78}
}

// PauseScheduleRequest stops a schedule firing without removing it.
type PauseScheduleRequest struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	Name  string                 `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Note is recorded on the schedule and shown by list and describe.
	//
	// Worth writing, because a paused schedule found by somebody else is a thing
	// with no explanation attached — the same reason [TerminateRequest] carries a
	// reason.
	Note          string `protobuf:"bytes,2,opt,name=note,proto3" json:"note,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *PauseScheduleRequest) Reset() {
	*x = PauseScheduleRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[79]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *PauseScheduleRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*PauseScheduleRequest) ProtoMessage() {}

func (x *PauseScheduleRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[79]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use PauseScheduleRequest.ProtoReflect.Descriptor instead.
func (*PauseScheduleRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{79}
}

func (x *PauseScheduleRequest) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *PauseScheduleRequest) GetNote() string {
	if x != nil {
		return x.Note
	}
	return ""
}

// PauseScheduleResponse is empty.
type PauseScheduleResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *PauseScheduleResponse) Reset() {
	*x = PauseScheduleResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[80]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *PauseScheduleResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*PauseScheduleResponse) ProtoMessage() {}

func (x *PauseScheduleResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[80]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use PauseScheduleResponse.ProtoReflect.Descriptor instead.
func (*PauseScheduleResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{80}
}

// ResumeScheduleRequest lets a paused schedule fire again.
type ResumeScheduleRequest struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	Name  string                 `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Note replaces the message on the schedule, which is usually where the reason
	// it was paused is still written.
	Note          string `protobuf:"bytes,2,opt,name=note,proto3" json:"note,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ResumeScheduleRequest) Reset() {
	*x = ResumeScheduleRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[81]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ResumeScheduleRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ResumeScheduleRequest) ProtoMessage() {}

func (x *ResumeScheduleRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[81]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ResumeScheduleRequest.ProtoReflect.Descriptor instead.
func (*ResumeScheduleRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{81}
}

func (x *ResumeScheduleRequest) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *ResumeScheduleRequest) GetNote() string {
	if x != nil {
		return x.Note
	}
	return ""
}

// ResumeScheduleResponse is empty.
type ResumeScheduleResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ResumeScheduleResponse) Reset() {
	*x = ResumeScheduleResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[82]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ResumeScheduleResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ResumeScheduleResponse) ProtoMessage() {}

func (x *ResumeScheduleResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[82]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ResumeScheduleResponse.ProtoReflect.Descriptor instead.
func (*ResumeScheduleResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{82}
}

// TriggerScheduleRequest fires a schedule now, without waiting for its cadence.
type TriggerScheduleRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Name          string                 `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TriggerScheduleRequest) Reset() {
	*x = TriggerScheduleRequest{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[83]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TriggerScheduleRequest) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TriggerScheduleRequest) ProtoMessage() {}

func (x *TriggerScheduleRequest) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[83]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TriggerScheduleRequest.ProtoReflect.Descriptor instead.
func (*TriggerScheduleRequest) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{83}
}

func (x *TriggerScheduleRequest) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

// TriggerScheduleResponse is empty.
//
// Not the run it started, and that is Temporal's shape rather than a choice: a
// trigger is asynchronous — the cluster takes the action and the schedule's own
// history records it — so there is no execution to name by the time this returns.
// `DescribeSchedule` reports it as a recent run once it has been taken, which is
// what the CLI points at.
type TriggerScheduleResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TriggerScheduleResponse) Reset() {
	*x = TriggerScheduleResponse{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[84]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TriggerScheduleResponse) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TriggerScheduleResponse) ProtoMessage() {}

func (x *TriggerScheduleResponse) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[84]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TriggerScheduleResponse.ProtoReflect.Descriptor instead.
func (*TriggerScheduleResponse) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{84}
}

// StepOutputs is a map of step IDs to their outputs. Each step's outputs are
// represented as a map of named values, allowing for structured outputs that
// can be referenced by subsequent steps in the workflow.
//
// This is used to collect the outputs of each step in the workflow, which can
// then be used as inputs to other steps or returned as the final result of the
// workflow run.
type Workflow_StepOutputs struct {
	state      protoimpl.MessageState   `protogen:"open.v1"`
	StepValues map[string]*Node_Outputs `protobuf:"bytes,1,rep,name=step_values,json=stepValues,proto3" json:"step_values,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// RunOutputs are the values [Workflow.declared_outputs] promised, once this
	// run has computed them. Unset for a workflow that declares none, and unset
	// for a run that has not finished.
	//
	// # Why the answer travels beside the transcript
	//
	// This message *is* what a finished run hands back — the local driver returns
	// it and the durable workflow completes with it — so it is the one value both
	// drivers already carry out of a run. A field here is therefore how the answer
	// reaches a caller without either driver changing shape, which is what keeps
	// `flow run local` and a durable run reporting one thing (invariant 3).
	//
	// The alternative was a new result message wrapping both. That is a *different
	// type* on the workflow's completion payload, and a run that completed before
	// the change is read back by decoding its stored payload into whatever type the
	// reader names — so an old result would be decoded as a new message and mean
	// something else, or fail. Adding a field is free and reads back as absent
	// (invariant 10); replacing the type is not, and this is a payload nobody can
	// rewrite after the fact.
	//
	// It stays a distinct message rather than more entries in `step_values` for the
	// reason [RunOutputs] gives: the transcript is keyed by the step that produced
	// each value, and these are the few values the *workflow* said it would report.
	// A reader never has to ask which of the two it is holding, because they are
	// different fields of different types.
	//
	// Meaningless on the copies of this message that are not a run's result — a
	// scope's visible outputs, one loop iteration's results, the outputs carried
	// across a Continue-As-New — where it is simply never set. The run's own end is
	// the only place anything writes it.
	RunOutputs    *RunOutputs `protobuf:"bytes,2,opt,name=run_outputs,json=runOutputs,proto3" json:"run_outputs,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Workflow_StepOutputs) Reset() {
	*x = Workflow_StepOutputs{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[85]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Workflow_StepOutputs) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Workflow_StepOutputs) ProtoMessage() {}

func (x *Workflow_StepOutputs) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[85]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Workflow_StepOutputs.ProtoReflect.Descriptor instead.
func (*Workflow_StepOutputs) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{0, 0}
}

func (x *Workflow_StepOutputs) GetStepValues() map[string]*Node_Outputs {
	if x != nil {
		return x.StepValues
	}
	return nil
}

func (x *Workflow_StepOutputs) GetRunOutputs() *RunOutputs {
	if x != nil {
		return x.RunOutputs
	}
	return nil
}

// Outputs are the results produced by the node. Each output is a named value that can be
// referenced by subsequent nodes in the workflow. This allows for structured outputs that
// can be used to pass data between nodes.
type Node_Outputs struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	NamedValues   map[string]*Value      `protobuf:"bytes,1,rep,name=named_values,json=namedValues,proto3" json:"named_values,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Node_Outputs) Reset() {
	*x = Node_Outputs{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[92]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Node_Outputs) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Node_Outputs) ProtoMessage() {}

func (x *Node_Outputs) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[92]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Node_Outputs.ProtoReflect.Descriptor instead.
func (*Node_Outputs) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{8, 0}
}

func (x *Node_Outputs) GetNamedValues() map[string]*Value {
	if x != nil {
		return x.NamedValues
	}
	return nil
}

type Parallel_Branch struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Steps         []*Node                `protobuf:"bytes,1,rep,name=steps,proto3" json:"steps,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Parallel_Branch) Reset() {
	*x = Parallel_Branch{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[98]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Parallel_Branch) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Parallel_Branch) ProtoMessage() {}

func (x *Parallel_Branch) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[98]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Parallel_Branch.ProtoReflect.Descriptor instead.
func (*Parallel_Branch) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{19, 0}
}

func (x *Parallel_Branch) GetSteps() []*Node {
	if x != nil {
		return x.Steps
	}
	return nil
}

type Value_Error struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Message       string                 `protobuf:"bytes,1,opt,name=message,proto3" json:"message,omitempty"`
	Code          Value_Error_Code       `protobuf:"varint,2,opt,name=code,proto3,enum=flowstate.v1.Value_Error_Code" json:"code,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Value_Error) Reset() {
	*x = Value_Error{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[100]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Value_Error) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Value_Error) ProtoMessage() {}

func (x *Value_Error) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[100]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Value_Error.ProtoReflect.Descriptor instead.
func (*Value_Error) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{24, 0}
}

func (x *Value_Error) GetMessage() string {
	if x != nil {
		return x.Message
	}
	return ""
}

func (x *Value_Error) GetCode() Value_Error_Code {
	if x != nil {
		return x.Code
	}
	return Value_Error_CODE_UNSPECIFIED
}

// Structure is a list or a mapping whose entries are Values in their own
// right, rather than one value some expression produces.
//
// It exists for exactly one reason: so a secret reference can sit inside a
// header map, a form or a JSON body. A structure containing an expression
// compiles into a single CEL expression, and the *workflow* evaluates that —
// which is what evaluating a reference must never mean, because everything
// workflow code produces is written to durable, broadly readable history.
// Keeping every entry a Value keeps the reference a reference for the whole
// path, and the worker resolves it at the moment it applies the header or
// encodes the body.
//
// Its entries are literals and references, never expressions. That is a
// deliberate restriction rather than an omission: the walkers that decide
// which step outputs a resumed run has to carry look for a whole-value
// expression and cannot see one nested in here, and an output they miss is
// pruned from a run that then fails after Continue-As-New. The compiler
// refuses the mixture and says so, which is the fail-closed half of the same
// fact.
type Value_Structure struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Types that are valid to be assigned to Kind:
	//
	//	*Value_Structure_List_
	//	*Value_Structure_Map_
	Kind          isValue_Structure_Kind `protobuf_oneof:"kind"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Value_Structure) Reset() {
	*x = Value_Structure{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[101]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Value_Structure) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Value_Structure) ProtoMessage() {}

func (x *Value_Structure) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[101]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Value_Structure.ProtoReflect.Descriptor instead.
func (*Value_Structure) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{24, 1}
}

func (x *Value_Structure) GetKind() isValue_Structure_Kind {
	if x != nil {
		return x.Kind
	}
	return nil
}

func (x *Value_Structure) GetList() *Value_Structure_List {
	if x != nil {
		if x, ok := x.Kind.(*Value_Structure_List_); ok {
			return x.List
		}
	}
	return nil
}

func (x *Value_Structure) GetMap() *Value_Structure_Map {
	if x != nil {
		if x, ok := x.Kind.(*Value_Structure_Map_); ok {
			return x.Map
		}
	}
	return nil
}

type isValue_Structure_Kind interface {
	isValue_Structure_Kind()
}

type Value_Structure_List_ struct {
	List *Value_Structure_List `protobuf:"bytes,1,opt,name=list,proto3,oneof"`
}

type Value_Structure_Map_ struct {
	Map *Value_Structure_Map `protobuf:"bytes,2,opt,name=map,proto3,oneof"`
}

func (*Value_Structure_List_) isValue_Structure_Kind() {}

func (*Value_Structure_Map_) isValue_Structure_Kind() {}

// List is a sequence, in the order it was written.
type Value_Structure_List struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Values        []*Value               `protobuf:"bytes,1,rep,name=values,proto3" json:"values,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Value_Structure_List) Reset() {
	*x = Value_Structure_List{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[102]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Value_Structure_List) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Value_Structure_List) ProtoMessage() {}

func (x *Value_Structure_List) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[102]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Value_Structure_List.ProtoReflect.Descriptor instead.
func (*Value_Structure_List) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{24, 1, 0}
}

func (x *Value_Structure_List) GetValues() []*Value {
	if x != nil {
		return x.Values
	}
	return nil
}

// Map is a mapping. Entry order is not carried, because nothing that
// consumes one depends on it: headers are a set, a form and a query are
// encoded in sorted key order, and a JSON object is unordered by
// definition.
type Value_Structure_Map struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Entries       map[string]*Value      `protobuf:"bytes,1,rep,name=entries,proto3" json:"entries,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Value_Structure_Map) Reset() {
	*x = Value_Structure_Map{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[103]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Value_Structure_Map) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Value_Structure_Map) ProtoMessage() {}

func (x *Value_Structure_Map) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[103]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Value_Structure_Map.ProtoReflect.Descriptor instead.
func (*Value_Structure_Map) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{24, 1, 1}
}

func (x *Value_Structure_Map) GetEntries() map[string]*Value {
	if x != nil {
		return x.Entries
	}
	return nil
}

// Log is a task that emits a message for a person to read.
//
// It exists because `echo` was doing this job under a name that describes the
// wrong thing: echo *returns* a value, and the two uses that survived `vars:` —
// hello-world scaffolding and "this branch ran" markers — were never about the
// return. They were about someone seeing it. Naming the capability after what it
// is for is what lets the surfaces that show it (a `flow run` transcript, the
// watch view, the run record) treat it as a message rather than as a value that
// happens to be printed.
//
// # It has no outputs, deliberately
//
// A log line is an effect on a reader, not a value for a later step. Offering
// `result` would make `${steps.announce.result}` legal, which is the `vars:` use
// case sneaking back in through the one task that has no business carrying data —
// and it would give a step two reasons to exist, so the file could no longer say
// which one was meant. A capability that emits nothing is how this language says
// "this produces nothing you can read".
type Task_Log struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Task_Log) Reset() {
	*x = Task_Log{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[105]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Task_Log) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Task_Log) ProtoMessage() {}

func (x *Task_Log) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[105]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Task_Log.ProtoReflect.Descriptor instead.
func (*Task_Log) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{31, 0}
}

// HTTP is a task that performs an HTTP request to a specified URL with optional method, headers, and body.
// It returns the HTTP response status code, headers, and body as outputs.
//
// This is obviously useful, but also dangerous since it's just SSRF (Server-Side Request Forgery) in disguise.
type Task_HTTP struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Task_HTTP) Reset() {
	*x = Task_HTTP{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[106]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Task_HTTP) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Task_HTTP) ProtoMessage() {}

func (x *Task_HTTP) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[106]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Task_HTTP.ProtoReflect.Descriptor instead.
func (*Task_HTTP) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{31, 1}
}

type Task_Log_Inputs struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Message is the line a person reads. It is the whole point, so it is the
	// only required input.
	Message string `protobuf:"bytes,1,opt,name=message,proto3" json:"message,omitempty"`
	// Level says how much it matters, and defaults to LEVEL_INFO.
	//
	// An enum rather than a free string, because a sink has to map it onto
	// something — a terminal colour, a severity field, a filter — and a set that
	// an author can extend by typo is a set nothing downstream can switch on.
	Level Task_Log_Level `protobuf:"varint,2,opt,name=level,proto3,enum=flowstate.v1.Task_Log_Level" json:"level,omitempty"`
	// Fields carry structure for a sink that has somewhere to put it, and are
	// ignored by one that does not.
	//
	// String-valued rather than Value-valued, and that is a real limit rather
	// than an oversight: every sink this can reach — a terminal line, a logging
	// backend's attributes, a run record — renders to text in the end, and a
	// nested value would render differently at each of them. Compose the string
	// in the expression, where the author can see what it will say.
	//
	// Bounded because a workflow chooses both the count and the size, and a log
	// line is written to durable history.
	Fields        map[string]string `protobuf:"bytes,3,rep,name=fields,proto3" json:"fields,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Task_Log_Inputs) Reset() {
	*x = Task_Log_Inputs{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[108]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Task_Log_Inputs) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Task_Log_Inputs) ProtoMessage() {}

func (x *Task_Log_Inputs) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[108]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Task_Log_Inputs.ProtoReflect.Descriptor instead.
func (*Task_Log_Inputs) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{31, 0, 0}
}

func (x *Task_Log_Inputs) GetMessage() string {
	if x != nil {
		return x.Message
	}
	return ""
}

func (x *Task_Log_Inputs) GetLevel() Task_Log_Level {
	if x != nil {
		return x.Level
	}
	return Task_Log_LEVEL_UNSPECIFIED
}

func (x *Task_Log_Inputs) GetFields() map[string]string {
	if x != nil {
		return x.Fields
	}
	return nil
}

// Outputs is empty and stays empty.
//
// Present rather than absent because every task's shape is described from a
// descriptor — `flow tasks` and the language server both read one — and a task
// with no message at all would have to be a special case in each of them. An
// empty message says "nothing" in the same vocabulary everything else uses.
type Task_Log_Outputs struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Task_Log_Outputs) Reset() {
	*x = Task_Log_Outputs{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[109]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Task_Log_Outputs) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Task_Log_Outputs) ProtoMessage() {}

func (x *Task_Log_Outputs) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[109]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Task_Log_Outputs.ProtoReflect.Descriptor instead.
func (*Task_Log_Outputs) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{31, 0, 1}
}

type Task_HTTP_Inputs struct {
	state  protoimpl.MessageState `protogen:"open.v1"`
	Url    string                 `protobuf:"bytes,1,opt,name=url,proto3" json:"url,omitempty"`
	Method *string                `protobuf:"bytes,2,opt,name=method,proto3,oneof" json:"method,omitempty"`
	// Request headers.
	//
	// Strings, because a header is text on the wire — and a secret reference is
	// not a string, so a `headers:` mapping carrying one never reaches this
	// field at all. It arrives as a [Value.Structure] among the task's inputs
	// and is applied entry by entry, each reference resolved as its header is
	// set. Both paths end on the same [http.Header]; only one of them can hold
	// a reference, and it holds it as a reference right up to the last moment.
	//
	// Resolving into this field instead would be the mistake worth naming: a
	// proto struct field is exactly what `fmt` reflects into, so a header
	// resolved here would print under `%+v` of the inputs, of the task, and of
	// anything holding either.
	Headers map[string]string `protobuf:"bytes,3,rep,name=headers,proto3" json:"headers,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// A raw request body, sent as written.
	//
	// A string, and so unable to carry a secret reference: there is nothing
	// between the field and the wire that could resolve one. A body that needs
	// a credential in it is a `json` or a `form` body, both of which the task
	// encodes itself and can therefore resolve into.
	Body *string `protobuf:"bytes,4,opt,name=body,proto3,oneof" json:"body,omitempty"`
	// Bearer is the credential for an Authorization header, and it is the
	// first task input built to take a secret reference.
	//
	// A `Value` rather than a string, deliberately: a field declared as Value
	// receives the author's value whole — reference included — where a string
	// field can only hold what the workflow already evaluated, and evaluating
	// a secret is what would put it in history.
	//
	// A reference can also sit inside `headers` now, as one entry of a
	// [Value.Structure], which is what makes `Authorization` writable by hand.
	// This field stays, and stays the recommended spelling: it says what the
	// credential *is*, so the worker renders the header and an author cannot
	// get the scheme wrong — and it leaves the rest of `headers` free to hold
	// expressions, which a structure carrying a reference deliberately cannot.
	//
	// The worker resolves it inside the activity and sends
	// `Authorization: Bearer <value>`. The name says the scheme so the field
	// cannot quietly become a junk drawer: a future credential shape gets its
	// own field, named for what it is.
	Bearer *Value `protobuf:"bytes,12,opt,name=bearer,proto3" json:"bearer,omitempty"`
	// Credential names an outbound federation target configured by the
	// deployment, such as "aws-prod" or "partner-api". The worker authorizes
	// the exact workload and step, mints a short-lived assertion, exchanges it,
	// and applies the resulting bearer credential inside the activity. No
	// credential material crosses workflow history.
	//
	// AWS session targets are deliberately refused by the generic HTTP task:
	// they require SigV4 request signing and belong in an AWS-aware task.
	Credential *string `protobuf:"bytes,13,opt,name=credential,proto3,oneof" json:"credential,omitempty"`
	// Optional outputs shaping: when provided, the HTTP task will return only
	// the named outputs defined by this map instead of the default outputs.
	// Values are evaluated as CEL expressions (or used as literals) with the
	// following variables available:
	//   - status_code (int)
	//   - body (string)
	//   - headers (map<string,string>)
	//   - json (the parsed body, when `parse_json` asked for it)
	//
	// This enables payload minimization by selecting just the fields needed.
	Outputs map[string]*Value `protobuf:"bytes,5,rep,name=outputs,proto3" json:"outputs,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Query parameters, escaped and appended to the URL.
	//
	// Building a query string by hand is where escaping bugs live, and they are
	// the kind that work until a value contains an ampersand.
	//
	// A secret reference here is refused rather than resolved, and it is the one
	// input of this task where that is a decision about the *destination* rather
	// than about the mechanism: query strings are written to access logs, kept in
	// browser history, and forwarded in Referer headers by anything that follows
	// a redirect. `form` is the same kind of map, encoded by the same function a
	// few lines away, and accepts one — so the refusal is stated twice on
	// purpose, once in the compiler where an author can see it and once in
	// `valueToQueryString` for a specification that never met the compiler.
	Query map[string]*Value `protobuf:"bytes,6,rep,name=query,proto3" json:"query,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// A structured request body, serialized as JSON with a matching Content-Type.
	//
	// The common case, and until now the most laborious: sending an object meant
	// building the JSON by hand in a CEL expression and remembering the header.
	// Mutually exclusive with `body` and `form` — three ways to say what to send
	// is a request whose meaning depends on field order.
	//
	// A field of the body may be a secret reference, at any depth: the task
	// serializes the body itself, inside the activity, so the reference is read
	// as the bytes are written and nowhere earlier.
	Json *Value `protobuf:"bytes,7,opt,name=json,proto3" json:"json,omitempty"`
	// A form body, url-encoded with a matching Content-Type. Mutually exclusive
	// with `body` and `json`.
	//
	// An entry may be a secret reference — an OAuth `client_secret` is the case
	// — and it is resolved as the body is encoded, inside the activity. The
	// difference from `query`, which is the same kind of map encoded by the same
	// function, is entirely the destination: a form body is sent in the request,
	// where a query string is written down by everything it passes.
	Form map[string]*Value `protobuf:"bytes,8,rep,name=form,proto3" json:"form,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Which responses count as success, as a CEL expression over the same
	// variables `outputs` sees.
	//
	// The default is unchanged and needs no expression: 2xx succeeds, 4xx fails
	// permanently, 5xx is retried. This is for the cases that default cannot
	// express — a 404 that means "not there yet, and that is fine", or an API
	// that answers 200 with an error in the body:
	//
	//	expect: ${status_code == 200 || status_code == 404}
	//	expect: ${status_code == 200 && !json.error}
	//
	// One expression rather than a status list plus a predicate, because a list
	// is the expression `status_code in [200, 404]` and two spellings of one idea
	// is two things to keep in agreement.
	Expect *Value `protobuf:"bytes,9,opt,name=expect,proto3" json:"expect,omitempty"`
	// Parse the response body as JSON into the `json` output.
	//
	// Off by default, and deliberately not inferred from Content-Type: parsing an
	// untrusted body is work an attacker chooses, so it happens when an author
	// asked for it. A body that is not JSON is an error naming the offset, not a
	// silently empty value.
	ParseJson *bool `protobuf:"varint,10,opt,name=parse_json,json=parseJson,proto3,oneof" json:"parse_json,omitempty"`
	// Retry a non-idempotent request whose outcome is unknown.
	//
	// A POST that reached the server and then failed before answering may have
	// taken effect, so by default it is not retried: doing the operation twice is
	// worse than surfacing a failure that might have resolved on its own. Set
	// this when the endpoint is idempotency-keyed and a retry is therefore safe.
	//
	// It does not affect a request that never connected, which is retried for any
	// method because it cannot have taken effect.
	RetryOnUnknownOutcome *bool `protobuf:"varint,11,opt,name=retry_on_unknown_outcome,json=retryOnUnknownOutcome,proto3,oneof" json:"retry_on_unknown_outcome,omitempty"`
	unknownFields         protoimpl.UnknownFields
	sizeCache             protoimpl.SizeCache
}

func (x *Task_HTTP_Inputs) Reset() {
	*x = Task_HTTP_Inputs{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[111]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Task_HTTP_Inputs) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Task_HTTP_Inputs) ProtoMessage() {}

func (x *Task_HTTP_Inputs) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[111]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Task_HTTP_Inputs.ProtoReflect.Descriptor instead.
func (*Task_HTTP_Inputs) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{31, 1, 0}
}

func (x *Task_HTTP_Inputs) GetUrl() string {
	if x != nil {
		return x.Url
	}
	return ""
}

func (x *Task_HTTP_Inputs) GetMethod() string {
	if x != nil && x.Method != nil {
		return *x.Method
	}
	return ""
}

func (x *Task_HTTP_Inputs) GetHeaders() map[string]string {
	if x != nil {
		return x.Headers
	}
	return nil
}

func (x *Task_HTTP_Inputs) GetBody() string {
	if x != nil && x.Body != nil {
		return *x.Body
	}
	return ""
}

func (x *Task_HTTP_Inputs) GetBearer() *Value {
	if x != nil {
		return x.Bearer
	}
	return nil
}

func (x *Task_HTTP_Inputs) GetCredential() string {
	if x != nil && x.Credential != nil {
		return *x.Credential
	}
	return ""
}

func (x *Task_HTTP_Inputs) GetOutputs() map[string]*Value {
	if x != nil {
		return x.Outputs
	}
	return nil
}

func (x *Task_HTTP_Inputs) GetQuery() map[string]*Value {
	if x != nil {
		return x.Query
	}
	return nil
}

func (x *Task_HTTP_Inputs) GetJson() *Value {
	if x != nil {
		return x.Json
	}
	return nil
}

func (x *Task_HTTP_Inputs) GetForm() map[string]*Value {
	if x != nil {
		return x.Form
	}
	return nil
}

func (x *Task_HTTP_Inputs) GetExpect() *Value {
	if x != nil {
		return x.Expect
	}
	return nil
}

func (x *Task_HTTP_Inputs) GetParseJson() bool {
	if x != nil && x.ParseJson != nil {
		return *x.ParseJson
	}
	return false
}

func (x *Task_HTTP_Inputs) GetRetryOnUnknownOutcome() bool {
	if x != nil && x.RetryOnUnknownOutcome != nil {
		return *x.RetryOnUnknownOutcome
	}
	return false
}

type Task_HTTP_Outputs struct {
	state      protoimpl.MessageState `protogen:"open.v1"`
	StatusCode int32                  `protobuf:"varint,1,opt,name=status_code,json=statusCode,proto3" json:"status_code,omitempty"`
	Headers    map[string]string      `protobuf:"bytes,2,rep,name=headers,proto3" json:"headers,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Response body, returned by default so a step can be referenced as
	// ${steps.<id>.body}. Large responses are better trimmed with the `outputs`
	// input above, which keeps workflow payloads small.
	Body string `protobuf:"bytes,3,opt,name=body,proto3" json:"body,omitempty"`
	// The response body parsed as JSON, present when `parse_json` asked for it,
	// so ${steps.<id>.json.items[0].id} works without a second step to parse it.
	//
	// Deliberately a single value rather than a repeated one, and it should stay
	// that way. A streaming response is not a unary response with more elements —
	// it is a different interaction shape with a different execution model, since
	// a long-lived stream cannot live inside one activity at all. When streaming
	// arrives it gets its own field, which costs nothing because adding a field is
	// additive. Widening this one to repeated would break every workflow that
	// reads it, and would make every author write [0] forever to accommodate a
	// shape their request does not have.
	//
	// Carried as a CEL value rather than google.protobuf.Value because that is
	// already how this schema represents a literal, so the conversions an
	// expression needs to read it exist rather than being a third value type to
	// translate between.
	//
	// A JSON number arrives as a double, so `{"id":7}` reads as 7.0 rather than
	// 7. That is what encoding/json produces for an untyped number and what the
	// `json_parse` CEL function already does, and one rule that surprises is
	// better than two that disagree. It matters when comparing: write
	// `${call.json.id == 7}` and CEL will compare numerically, but do not expect
	// `string(call.json.id)` to read as "7".
	Json          *v1alpha1.Value `protobuf:"bytes,4,opt,name=json,proto3" json:"json,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Task_HTTP_Outputs) Reset() {
	*x = Task_HTTP_Outputs{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[112]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Task_HTTP_Outputs) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Task_HTTP_Outputs) ProtoMessage() {}

func (x *Task_HTTP_Outputs) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[112]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Task_HTTP_Outputs.ProtoReflect.Descriptor instead.
func (*Task_HTTP_Outputs) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{31, 1, 1}
}

func (x *Task_HTTP_Outputs) GetStatusCode() int32 {
	if x != nil {
		return x.StatusCode
	}
	return 0
}

func (x *Task_HTTP_Outputs) GetHeaders() map[string]string {
	if x != nil {
		return x.Headers
	}
	return nil
}

func (x *Task_HTTP_Outputs) GetBody() string {
	if x != nil {
		return x.Body
	}
	return ""
}

func (x *Task_HTTP_Outputs) GetJson() *v1alpha1.Value {
	if x != nil {
		return x.Json
	}
	return nil
}

type RunResponse_Error struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Message       string                 `protobuf:"bytes,1,opt,name=message,proto3" json:"message,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *RunResponse_Error) Reset() {
	*x = RunResponse_Error{}
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[119]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *RunResponse_Error) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*RunResponse_Error) ProtoMessage() {}

func (x *RunResponse_Error) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_flowstate_proto_msgTypes[119]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use RunResponse_Error.ProtoReflect.Descriptor instead.
func (*RunResponse_Error) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_flowstate_proto_rawDescGZIP(), []int{33, 0}
}

func (x *RunResponse_Error) GetMessage() string {
	if x != nil {
		return x.Message
	}
	return ""
}

var File_flowstate_v1_flowstate_proto protoreflect.FileDescriptor

const file_flowstate_v1_flowstate_proto_rawDesc = "" +
	"\n" +
	"\x1cflowstate/v1/flowstate.proto\x12\fflowstate.v1\x1a\x1bbuf/validate/validate.proto\x1a%google/api/expr/v1alpha1/syntax.proto\x1a$google/api/expr/v1alpha1/value.proto\x1a\x1fgoogle/api/field_behavior.proto\x1a\x1egoogle/protobuf/duration.proto\x1a\x1fgoogle/protobuf/timestamp.proto\"\xd6\t\n" +
	"\bWorkflow\x127\n" +
	"\x04name\x18\x01 \x01(\tB#\xe2A\x01\x02\xbaH\x1c\xc8\x01\x01r\x17\x10\x01\x18\x80\x012\x10^[A-Za-z0-9-_]+$R\x04name\x12/\n" +
	"\vdescription\x18\x02 \x01(\tB\b\xbaH\x05r\x03\x18\x80\x02H\x00R\vdescription\x88\x01\x01\x12;\n" +
	"\x05steps\x18\x03 \x03(\v2\x12.flowstate.v1.NodeB\x11\xe2A\x01\x02\xbaH\n" +
	"\xc8\x01\x01\x92\x01\x04\b\x01\x10dR\x05steps\x12L\n" +
	"\x06labels\x18\x05 \x03(\v2\".flowstate.v1.Workflow.LabelsEntryB\x10\xe2A\x01\x01\xbaH\t\x9a\x01\x06\"\x04r\x02\x10\x01R\x06labels\x12\x1e\n" +
	"\aprofile\x18\x06 \x01(\tB\x04\xe2A\x01\x01R\aprofile\x12H\n" +
	"\x04vars\x18\a \x03(\v2 .flowstate.v1.Workflow.VarsEntryB\x12\xe2A\x01\x01\xbaH\v\x9a\x01\b\x10@\"\x04r\x02\x10\x01R\x04vars\x12U\n" +
	"\x0fdeclared_inputs\x18\b \x03(\v2\x1e.flowstate.v1.InputDeclarationB\f\xe2A\x01\x01\xbaH\x05\x92\x01\x02\x10@R\x0edeclaredInputs\x12X\n" +
	"\x10declared_outputs\x18\t \x03(\v2\x1f.flowstate.v1.OutputDeclarationB\f\xe2A\x01\x01\xbaH\x05\x92\x01\x02\x10@R\x0fdeclaredOutputs\x128\n" +
	"\btriggers\x18\n" +
	" \x01(\v2\x16.flowstate.v1.TriggersB\x04\xe2A\x01\x01R\btriggers\x12q\n" +
	"\asignals\x18\v \x03(\v2#.flowstate.v1.Workflow.SignalsEntryB2\xe2A\x01\x01\xbaH+\x9a\x01(\x10@\"$r\"\x10\x01\x18\x80\x012\x1b^[A-Za-z0-9][A-Za-z0-9-_]*$R\asignals\x1a\x8d\x02\n" +
	"\vStepOutputs\x12h\n" +
	"\vstep_values\x18\x01 \x03(\v22.flowstate.v1.Workflow.StepOutputs.StepValuesEntryB\x13\xe2A\x01\x02\xbaH\f\xc8\x01\x01\x9a\x01\x06\"\x04r\x02\x10\x01R\n" +
	"stepValues\x129\n" +
	"\vrun_outputs\x18\x02 \x01(\v2\x18.flowstate.v1.RunOutputsR\n" +
	"runOutputs\x1aY\n" +
	"\x0fStepValuesEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x120\n" +
	"\x05value\x18\x02 \x01(\v2\x1a.flowstate.v1.Node.OutputsR\x05value:\x028\x01\x1a9\n" +
	"\vLabelsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12\x14\n" +
	"\x05value\x18\x02 \x01(\tR\x05value:\x028\x01\x1aL\n" +
	"\tVarsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\x1aV\n" +
	"\fSignalsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x120\n" +
	"\x05value\x18\x02 \x01(\v2\x1a.flowstate.v1.SignalPolicyR\x05value:\x028\x01B\x0e\n" +
	"\f_descriptionJ\x04\b\x04\x10\x05R\x06inputs\"P\n" +
	"\fSignalPolicy\x12@\n" +
	"\x05allow\x18\x01 \x03(\v2\x1e.flowstate.v1.SignalPolicyRuleB\n" +
	"\xbaH\a\x92\x01\x04\b\x01\x10 R\x05allow\"\x86\x02\n" +
	"\x10SignalPolicyRule\x121\n" +
	"\asubject\x18\x01 \x01(\tB\x17\xbaH\x14\xd8\x01\x01r\x0f\x18\xc0\x022\n" +
	"^[^#]+#.+$R\asubject\x12&\n" +
	"\tnamespace\x18\x02 \x01(\tB\b\xbaH\x05r\x03\x18\x80\x01R\tnamespace\x12\\\n" +
	"\x06claims\x18\x03 \x03(\v2*.flowstate.v1.SignalPolicyRule.ClaimsEntryB\x18\xbaH\x15\x9a\x01\x12\x10\x10\"\ar\x05\x10\x01\x18\x80\x01*\x05r\x03\x18\x80\x02R\x06claims\x1a9\n" +
	"\vClaimsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12\x14\n" +
	"\x05value\x18\x02 \x01(\tR\x05value:\x028\x01\"\xcb\x06\n" +
	"\x10InputDeclaration\x12?\n" +
	"\x04name\x18\x01 \x01(\tB+\xe2A\x01\x02\xbaH$\xc8\x01\x01r\x1f\x10\x01\x18\x80\x012\x18^[A-Za-z_][A-Za-z0-9_]*$R\x04name\x12J\n" +
	"\x04type\x18\x02 \x01(\x0e2#.flowstate.v1.InputDeclaration.TypeB\x11\xe2A\x01\x02\xbaH\n" +
	"\xc8\x01\x01\x82\x01\x04\x10\x01 \x00R\x04type\x12\x1a\n" +
	"\brequired\x18\x03 \x01(\bR\brequired\x12-\n" +
	"\adefault\x18\x04 \x01(\v2\x13.flowstate.v1.ValueR\adefault\x12/\n" +
	"\vdescription\x18\x05 \x01(\tB\b\xbaH\x05r\x03\x18\x80\x02H\x00R\vdescription\x88\x01\x01\x12-\n" +
	"\aexample\x18\x06 \x01(\v2\x13.flowstate.v1.ValueR\aexample\x12\x1c\n" +
	"\tsensitive\x18\a \x01(\bR\tsensitive\x12\x1d\n" +
	"\apattern\x18\b \x01(\tH\x01R\apattern\x88\x01\x01\x12\x1c\n" +
	"\amin_len\x18\t \x01(\x04H\x02R\x06minLen\x88\x01\x01\x12\x1c\n" +
	"\amax_len\x18\n" +
	" \x01(\x04H\x03R\x06maxLen\x88\x01\x01\x12\x15\n" +
	"\x03min\x18\v \x01(\x01H\x04R\x03min\x88\x01\x01\x12\x15\n" +
	"\x03max\x18\f \x01(\x01H\x05R\x03max\x88\x01\x01\x12 \n" +
	"\tmin_items\x18\r \x01(\x04H\x06R\bminItems\x88\x01\x01\x12 \n" +
	"\tmax_items\x18\x0e \x01(\x04H\aR\bmaxItems\x88\x01\x01\x12\x16\n" +
	"\x06unique\x18\x0f \x01(\bR\x06unique\x12\x17\n" +
	"\x04must\x18\x10 \x01(\tH\bR\x04must\x88\x01\x01\"z\n" +
	"\x04Type\x12\x14\n" +
	"\x10TYPE_UNSPECIFIED\x10\x00\x12\x0f\n" +
	"\vTYPE_STRING\x10\x01\x12\f\n" +
	"\bTYPE_INT\x10\x02\x12\x0e\n" +
	"\n" +
	"TYPE_FLOAT\x10\x03\x12\r\n" +
	"\tTYPE_BOOL\x10\x04\x12\x0f\n" +
	"\vTYPE_STRUCT\x10\x05\x12\r\n" +
	"\tTYPE_LIST\x10\x06B\x0e\n" +
	"\f_descriptionB\n" +
	"\n" +
	"\b_patternB\n" +
	"\n" +
	"\b_min_lenB\n" +
	"\n" +
	"\b_max_lenB\x06\n" +
	"\x04_minB\x06\n" +
	"\x04_maxB\f\n" +
	"\n" +
	"_min_itemsB\f\n" +
	"\n" +
	"_max_itemsB\a\n" +
	"\x05_must\"\x8c\x02\n" +
	"\x11OutputDeclaration\x12?\n" +
	"\x04name\x18\x01 \x01(\tB+\xe2A\x01\x02\xbaH$\xc8\x01\x01r\x1f\x10\x01\x18\x80\x012\x18^[A-Za-z_][A-Za-z0-9_]*$R\x04name\x125\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\x05value\x12/\n" +
	"\vdescription\x18\x03 \x01(\tB\b\xbaH\x05r\x03\x18\x80\x02H\x00R\vdescription\x88\x01\x01\x12\x17\n" +
	"\x04must\x18\x04 \x01(\tH\x01R\x04must\x88\x01\x01\x12\x1c\n" +
	"\tsensitive\x18\x05 \x01(\bR\tsensitiveB\x0e\n" +
	"\f_descriptionB\a\n" +
	"\x05_must\"\xae\x01\n" +
	"\n" +
	"RunOutputs\x12P\n" +
	"\x06values\x18\x01 \x03(\v2$.flowstate.v1.RunOutputs.ValuesEntryB\x12\xe2A\x01\x01\xbaH\v\x9a\x01\b\x10@\"\x04r\x02\x10\x01R\x06values\x1aN\n" +
	"\vValuesEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\"K\n" +
	"\bTriggers\x12?\n" +
	"\bschedule\x18\x01 \x01(\v2\x1d.flowstate.v1.ScheduleTriggerB\x04\xe2A\x01\x01R\bschedule\"\xf9\x03\n" +
	"\x0fScheduleTrigger\x12)\n" +
	"\x04cron\x18\x01 \x03(\tB\x15\xe2A\x01\x01\xbaH\x0e\x92\x01\v\x10 \"\ar\x05\x10\x01\x18\x80\x02R\x04cron\x12F\n" +
	"\x05every\x18\x02 \x01(\v2\x19.google.protobuf.DurationB\x15\xe2A\x01\x01\xbaH\x0e\xaa\x01\v\"\x05\b\x80\xe7\x84\x0f2\x02\b\x01R\x05every\x12(\n" +
	"\ttime_zone\x18\x03 \x01(\tB\v\xe2A\x01\x01\xbaH\x04r\x02\x18@R\btimeZone\x12E\n" +
	"\x06jitter\x18\x04 \x01(\v2\x19.google.protobuf.DurationB\x12\xe2A\x01\x01\xbaH\v\xaa\x01\b\"\x04\b\x80\xa3\x052\x00R\x06jitter\x12M\n" +
	"\aoverlap\x18\x05 \x01(\x0e2%.flowstate.v1.ScheduleTrigger.OverlapB\f\xe2A\x01\x01\xbaH\x05\x82\x01\x02\x10\x01R\aoverlap\"\xb2\x01\n" +
	"\aOverlap\x12\x17\n" +
	"\x13OVERLAP_UNSPECIFIED\x10\x00\x12\x10\n" +
	"\fOVERLAP_SKIP\x10\x01\x12\x16\n" +
	"\x12OVERLAP_BUFFER_ONE\x10\x02\x12\x16\n" +
	"\x12OVERLAP_BUFFER_ALL\x10\x03\x12\x18\n" +
	"\x14OVERLAP_CANCEL_OTHER\x10\x04\x12\x1b\n" +
	"\x17OVERLAP_TERMINATE_OTHER\x10\x05\x12\x15\n" +
	"\x11OVERLAP_ALLOW_ALL\x10\x06\"\x88\a\n" +
	"\x04Node\x123\n" +
	"\x02id\x18\x01 \x01(\tB#\xe2A\x01\x02\xbaH\x1c\xc8\x01\x01r\x17\x10\x01\x18\x80\x012\x10^[A-Za-z0-9-_]+$R\x02id\x12(\n" +
	"\x04task\x18\x02 \x01(\v2\x12.flowstate.v1.TaskH\x00R\x04task\x122\n" +
	"\bfor_each\x18\x05 \x01(\v2\x15.flowstate.v1.ForEachH\x00R\aforEach\x124\n" +
	"\bparallel\x18\x06 \x01(\v2\x16.flowstate.v1.ParallelH\x00R\bparallel\x12(\n" +
	"\x04wait\x18\a \x01(\v2\x12.flowstate.v1.WaitH\x00R\x04wait\x12(\n" +
	"\x04call\x18\v \x01(\v2\x12.flowstate.v1.CallH\x00R\x04call\x12(\n" +
	"\x04loop\x18\f \x01(\v2\x12.flowstate.v1.LoopH\x00R\x04loop\x121\n" +
	"\tcondition\x18\x03 \x01(\v2\x13.flowstate.v1.ValueR\tcondition\x120\n" +
	"\x06policy\x18\x04 \x01(\v2\x18.flowstate.v1.StepPolicyR\x06policy\x12/\n" +
	"\vdescription\x18\b \x01(\tB\b\xbaH\x05r\x03\x18\x80\x02H\x01R\vdescription\x88\x01\x01\x12@\n" +
	"\x04vars\x18\t \x03(\v2\x1c.flowstate.v1.Node.VarsEntryB\x0e\xbaH\v\x9a\x01\b\x10@\"\x04r\x02\x10\x01R\x04vars\x12.\n" +
	"\x04undo\x18\n" +
	" \x01(\v2\x1a.flowstate.v1.CompensationR\x04undo\x1a\xc3\x01\n" +
	"\aOutputs\x12c\n" +
	"\fnamed_values\x18\x01 \x03(\v2+.flowstate.v1.Node.Outputs.NamedValuesEntryB\x13\xe2A\x01\x02\xbaH\f\xc8\x01\x01\x9a\x01\x06\"\x04r\x02\x10\x01R\vnamedValues\x1aS\n" +
	"\x10NamedValuesEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\x1aL\n" +
	"\tVarsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01B\r\n" +
	"\x04kind\x12\x05\xbaH\x02\b\x01B\x0e\n" +
	"\f_description\"B\n" +
	"\fCompensation\x122\n" +
	"\x04task\x18\x01 \x01(\v2\x12.flowstate.v1.TaskB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\x04task\"m\n" +
	"\vPendingUndo\x12*\n" +
	"\astep_id\x18\x01 \x01(\tB\x11\xe2A\x01\x02\xbaH\n" +
	"\xc8\x01\x01r\x05\x10\x01\x18\x80\x01R\x06stepId\x122\n" +
	"\x04task\x18\x02 \x01(\v2\x12.flowstate.v1.TaskB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\x04task\"j\n" +
	"\tSecretRef\x126\n" +
	"\x06scheme\x18\x01 \x01(\tB\x1e\xe2A\x01\x02\xbaH\x17\xc8\x01\x01r\x12\x10\x01\x18 2\f^[a-z0-9-]+$R\x06scheme\x12%\n" +
	"\x04name\x18\x02 \x01(\tB\x11\xe2A\x01\x02\xbaH\n" +
	"\xc8\x01\x01r\x05\x10\x01\x18\x80\bR\x04name\"\xe0\x01\n" +
	"\x04Wait\x127\n" +
	"\bduration\x18\x01 \x01(\v2\x19.google.protobuf.DurationH\x00R\bduration\x12+\n" +
	"\x05until\x18\x02 \x01(\v2\x13.flowstate.v1.ValueH\x00R\x05until\x12.\n" +
	"\x06signal\x18\x03 \x01(\v2\x14.flowstate.v1.SignalH\x00R\x06signal\x123\n" +
	"\atimeout\x18\x04 \x01(\v2\x19.google.protobuf.DurationR\atimeoutB\r\n" +
	"\x04kind\x12\x05\xbaH\x02\b\x01\"L\n" +
	"\x06Signal\x12B\n" +
	"\x04name\x18\x01 \x01(\tB.\xe2A\x01\x02\xbaH'\xc8\x01\x01r\"\x10\x01\x18\x80\x012\x1b^[A-Za-z0-9][A-Za-z0-9-_]*$R\x04name\"\x9d\x01\n" +
	"\fSignalSender\x12:\n" +
	"\bidentity\x18\x01 \x01(\v2\x1e.flowstate.v1.WorkloadIdentityR\bidentity\x12;\n" +
	"\vaccepted_at\x18\x02 \x01(\v2\x1a.google.protobuf.TimestampR\n" +
	"acceptedAt\x12\x14\n" +
	"\x05local\x18\x03 \x01(\bR\x05local\"\x99\x01\n" +
	"\rPendingSignal\x12\x1e\n" +
	"\x04name\x18\x01 \x01(\tB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\x04name\x124\n" +
	"\apayload\x18\x02 \x01(\v2\x1a.flowstate.v1.Node.OutputsR\apayload\x122\n" +
	"\x06sender\x18\x03 \x01(\v2\x1a.flowstate.v1.SignalSenderR\x06sender\"z\n" +
	"\x0eSignalDelivery\x124\n" +
	"\apayload\x18\x01 \x01(\v2\x1a.flowstate.v1.Node.OutputsR\apayload\x122\n" +
	"\x06sender\x18\x02 \x01(\v2\x1a.flowstate.v1.SignalSenderR\x06sender\"\x85\x05\n" +
	"\x05Scope\x12<\n" +
	"\aoutputs\x18\x01 \x01(\v2\".flowstate.v1.Workflow.StepOutputsR\aoutputs\x12\x18\n" +
	"\aprofile\x18\x03 \x01(\tR\aprofile\x12?\n" +
	"\x04vars\x18\x02 \x03(\v2\x1d.flowstate.v1.Scope.VarsEntryB\f\xbaH\t\x9a\x01\x06\"\x04r\x02\x10\x01R\x04vars\x12U\n" +
	"\fambient_vars\x18\x04 \x03(\v2$.flowstate.v1.Scope.AmbientVarsEntryB\f\xbaH\t\x9a\x01\x06\"\x04r\x02\x10\x01R\vambientVars\x12G\n" +
	"\x06inputs\x18\x05 \x03(\v2\x1f.flowstate.v1.Scope.InputsEntryB\x0e\xbaH\v\x9a\x01\b\x10@\"\x04r\x02\x10\x01R\x06inputs\x12:\n" +
	"\bidentity\x18\x06 \x01(\v2\x1e.flowstate.v1.WorkloadIdentityR\bidentity\x12\x14\n" +
	"\x05local\x18\a \x01(\bR\x05local\x1aL\n" +
	"\tVarsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\x1aS\n" +
	"\x10AmbientVarsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\x1aN\n" +
	"\vInputsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\"\xe6\x01\n" +
	"\aForEach\x125\n" +
	"\x05items\x18\x01 \x01(\v2\x13.flowstate.v1.ValueB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\x05items\x12>\n" +
	"\biterator\x18\x02 \x01(\tB\"\xbaH\x1fr\x1d\x18\x80\x012\x18^[A-Za-z_][A-Za-z0-9_]*$R\biterator\x125\n" +
	"\x04body\x18\x03 \x03(\v2\x12.flowstate.v1.NodeB\r\xbaH\n" +
	"\xc8\x01\x01\x92\x01\x04\b\x01\x10dR\x04body\x12-\n" +
	"\fmax_parallel\x18\x04 \x01(\x05B\n" +
	"\xbaH\a\x1a\x05\x18\xe8\a(\x00R\vmaxParallel\"\x97\x01\n" +
	"\bParallel\x12H\n" +
	"\bbranches\x18\x01 \x03(\v2\x1d.flowstate.v1.Parallel.BranchB\r\xbaH\n" +
	"\xc8\x01\x01\x92\x01\x04\b\x01\x10dR\bbranches\x1aA\n" +
	"\x06Branch\x127\n" +
	"\x05steps\x18\x01 \x03(\v2\x12.flowstate.v1.NodeB\r\xbaH\n" +
	"\xc8\x01\x01\x92\x01\x04\b\x01\x10dR\x05steps\"\xc1\x02\n" +
	"\x04Loop\x125\n" +
	"\x04body\x18\x01 \x03(\v2\x12.flowstate.v1.NodeB\r\xbaH\n" +
	"\xc8\x01\x01\x92\x01\x04\b\x01\x10dR\x04body\x125\n" +
	"\x05until\x18\x02 \x01(\v2\x13.flowstate.v1.ValueB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\x05until\x122\n" +
	"\x0emax_iterations\x18\x03 \x01(\x05B\v\xbaH\b\x1a\x06\x18\xa0\x8d\x06(\x00R\rmaxIterations\x12;\n" +
	"\x05state\x18\x04 \x01(\tB%\xbaH\"\xd8\x01\x01r\x1d\x18\x80\x012\x18^[A-Za-z_][A-Za-z0-9_]*$R\x05state\x12-\n" +
	"\ainitial\x18\x05 \x01(\v2\x13.flowstate.v1.ValueR\ainitial\x12+\n" +
	"\x06update\x18\x06 \x01(\v2\x13.flowstate.v1.ValueR\x06update\"\xba\x02\n" +
	"\x04Call\x12:\n" +
	"\bworkflow\x18\x01 \x01(\v2\x16.flowstate.v1.WorkflowB\x06\xbaH\x03\xc8\x01\x01R\bworkflow\x12R\n" +
	"\targuments\x18\x02 \x03(\v2!.flowstate.v1.Call.ArgumentsEntryB\x11\xbaH\x0e\x9a\x01\v\x10d\"\ar\x05\x10\x01\x18\x80\x01R\targuments\x12 \n" +
	"\x06source\x18\x03 \x01(\tB\b\xbaH\x05r\x03\x18\x80\bR\x06source\x12-\n" +
	"\rsource_digest\x18\x04 \x01(\tB\b\xbaH\x05r\x03\x18\x80\x01R\fsourceDigest\x1aQ\n" +
	"\x0eArgumentsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\"\xa8\x01\n" +
	"\n" +
	"StepPolicy\x12=\n" +
	"\atimeout\x18\x01 \x01(\v2\x19.google.protobuf.DurationB\b\xbaH\x05\xaa\x01\x02*\x00R\atimeout\x12/\n" +
	"\x05retry\x18\x02 \x01(\v2\x19.flowstate.v1.RetryPolicyR\x05retry\x12*\n" +
	"\x11continue_on_error\x18\x03 \x01(\bR\x0fcontinueOnError\"\x92\x02\n" +
	"\vRetryPolicy\x12*\n" +
	"\fmax_attempts\x18\x01 \x01(\x05B\a\xbaH\x04\x1a\x02(\x00R\vmaxAttempts\x12N\n" +
	"\x10initial_interval\x18\x02 \x01(\v2\x19.google.protobuf.DurationB\b\xbaH\x05\xaa\x01\x02*\x00R\x0finitialInterval\x12?\n" +
	"\x13backoff_coefficient\x18\x03 \x01(\x01B\x0e\xbaH\v\x12\t)\x00\x00\x00\x00\x00\x00\xf0?R\x12backoffCoefficient\x12F\n" +
	"\fmax_interval\x18\x04 \x01(\v2\x19.google.protobuf.DurationB\b\xbaH\x05\xaa\x01\x02*\x00R\vmaxInterval\"\xe1\b\n" +
	"\x05Value\x12:\n" +
	"\x04expr\x18\x01 \x01(\v2$.google.api.expr.v1alpha1.ParsedExprH\x00R\x04expr\x12;\n" +
	"\aliteral\x18\x02 \x01(\v2\x1f.google.api.expr.v1alpha1.ValueH\x00R\aliteral\x121\n" +
	"\x05error\x18\x03 \x01(\v2\x19.flowstate.v1.Value.ErrorH\x00R\x05error\x128\n" +
	"\n" +
	"secret_ref\x18\x04 \x01(\v2\x17.flowstate.v1.SecretRefH\x00R\tsecretRef\x12=\n" +
	"\tstructure\x18\x05 \x01(\v2\x1d.flowstate.v1.Value.StructureH\x00R\tstructure\x1a\xa1\x02\n" +
	"\x05Error\x12$\n" +
	"\amessage\x18\x01 \x01(\tB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\amessage\x12E\n" +
	"\x04code\x18\x02 \x01(\x0e2\x1e.flowstate.v1.Value.Error.CodeB\x11\xe2A\x01\x02\xbaH\n" +
	"\xc8\x01\x01\x82\x01\x04\x10\x01 \x00R\x04code\"\xaa\x01\n" +
	"\x04Code\x12\x14\n" +
	"\x10CODE_UNSPECIFIED\x10\x00\x12\x19\n" +
	"\x15CODE_INVALID_ARGUMENT\x10\x01\x12\x12\n" +
	"\x0eCODE_NOT_FOUND\x10\x02\x12\x1a\n" +
	"\x16CODE_PERMISSION_DENIED\x10\x03\x12\x18\n" +
	"\x14CODE_UNAUTHENTICATED\x10\x04\x12\x11\n" +
	"\rCODE_INTERNAL\x10\x05\x12\x14\n" +
	"\x10CODE_UNAVAILABLE\x10\x06\x1a\xe3\x02\n" +
	"\tStructure\x128\n" +
	"\x04list\x18\x01 \x01(\v2\".flowstate.v1.Value.Structure.ListH\x00R\x04list\x125\n" +
	"\x03map\x18\x02 \x01(\v2!.flowstate.v1.Value.Structure.MapH\x00R\x03map\x1a3\n" +
	"\x04List\x12+\n" +
	"\x06values\x18\x01 \x03(\v2\x13.flowstate.v1.ValueR\x06values\x1a\xa0\x01\n" +
	"\x03Map\x12H\n" +
	"\aentries\x18\x01 \x03(\v2..flowstate.v1.Value.Structure.Map.EntriesEntryR\aentries\x1aO\n" +
	"\fEntriesEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01B\r\n" +
	"\x04kind\x12\x05\xbaH\x02\b\x01\"\x99\x01\n" +
	"\x04Type\x12\x14\n" +
	"\x10TYPE_UNSPECIFIED\x10\x00\x12\x0f\n" +
	"\vTYPE_STRING\x10\x01\x12\f\n" +
	"\bTYPE_INT\x10\x02\x12\x0e\n" +
	"\n" +
	"TYPE_FLOAT\x10\x03\x12\r\n" +
	"\tTYPE_BOOL\x10\x04\x12\x0f\n" +
	"\vTYPE_STRUCT\x10\x05\x12\r\n" +
	"\tTYPE_LIST\x10\x06\x12\r\n" +
	"\tTYPE_EXPR\x10\a\x12\x0e\n" +
	"\n" +
	"TYPE_ERROR\x10\bB\r\n" +
	"\x04kind\x12\x05\xbaH\x02\b\x01\"\x96\x02\n" +
	"\vTaskCatalog\x123\n" +
	"\x05tasks\x18\x01 \x03(\v2\x1d.flowstate.v1.TaskDescriptionR\x05tasks\x12#\n" +
	"\rcel_libraries\x18\x02 \x03(\tR\fcelLibraries\x12%\n" +
	"\x0eduration_units\x18\x03 \x03(\tR\rdurationUnits\x12%\n" +
	"\x0enow_identifier\x18\x04 \x01(\tR\rnowIdentifier\x12\x1f\n" +
	"\vvalue_roots\x18\x05 \x03(\tR\n" +
	"valueRoots\x12>\n" +
	"\rcel_functions\x18\x06 \x03(\v2\x19.flowstate.v1.CELFunctionR\fcelFunctions\"k\n" +
	"\vCELFunction\x12\x12\n" +
	"\x04name\x18\x01 \x01(\tR\x04name\x12\x18\n" +
	"\alibrary\x18\x02 \x01(\tR\alibrary\x12\x14\n" +
	"\x05macro\x18\x03 \x01(\bR\x05macro\x12\x18\n" +
	"\aexample\x18\x04 \x01(\tR\aexample\"\xa3\x01\n" +
	"\x0fTaskDescription\x12\x12\n" +
	"\x04name\x18\x01 \x01(\tR\x04name\x12\x18\n" +
	"\asummary\x18\x02 \x01(\tR\asummary\x12/\n" +
	"\x06inputs\x18\x03 \x03(\v2\x17.flowstate.v1.TaskFieldR\x06inputs\x121\n" +
	"\aoutputs\x18\x04 \x03(\v2\x17.flowstate.v1.TaskFieldR\aoutputs\"k\n" +
	"\rPluginCatalog\x129\n" +
	"\aplugins\x18\x01 \x03(\v2\x1f.flowstate.v1.PluginDescriptionR\aplugins\x12\x1f\n" +
	"\vsearch_path\x18\x02 \x03(\tR\n" +
	"searchPath\"\xd3\x01\n" +
	"\x11PluginDescription\x12\x12\n" +
	"\x04name\x18\x01 \x01(\tR\x04name\x12\x18\n" +
	"\aversion\x18\x02 \x01(\tR\aversion\x12 \n" +
	"\vdescription\x18\x03 \x01(\tR\vdescription\x12\x12\n" +
	"\x04path\x18\x04 \x01(\tR\x04path\x12%\n" +
	"\x0esecret_schemes\x18\x05 \x03(\tR\rsecretSchemes\x123\n" +
	"\x05tasks\x18\x06 \x03(\v2\x1d.flowstate.v1.TaskDescriptionR\x05tasks\"k\n" +
	"\tTaskField\x12\x12\n" +
	"\x04name\x18\x01 \x01(\tR\x04name\x12\x12\n" +
	"\x04type\x18\x02 \x01(\tR\x04type\x12\x1a\n" +
	"\brequired\x18\x03 \x01(\bR\brequired\x12\x1a\n" +
	"\bdeferred\x18\x04 \x01(\bR\bdeferred\"\xbc\x0f\n" +
	"\x04Task\x12J\n" +
	"\x04name\x18\x01 \x01(\tB6\xe2A\x01\x02\xbaH/\xc8\x01\x01r*\x10\x01\x18\x80\x012#^[A-Za-z0-9-_]+(\\.[A-Za-z0-9-_]+)?$R\x04name\x12K\n" +
	"\x06inputs\x18\x03 \x03(\v2\x1e.flowstate.v1.Task.InputsEntryB\x13\xe2A\x01\x01\xbaH\f\xc8\x01\x01\x9a\x01\x06\"\x04r\x02\x10\x01R\x06inputs\x1a\xe7\x02\n" +
	"\x03Log\x1a\x83\x02\n" +
	"\x06Inputs\x12$\n" +
	"\amessage\x18\x01 \x01(\tB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\amessage\x12<\n" +
	"\x05level\x18\x02 \x01(\x0e2\x1c.flowstate.v1.Task.Log.LevelB\b\xbaH\x05\x82\x01\x02\x10\x01R\x05level\x12Z\n" +
	"\x06fields\x18\x03 \x03(\v2).flowstate.v1.Task.Log.Inputs.FieldsEntryB\x17\xbaH\x14\x9a\x01\x11\x10 \"\x06r\x04\x10\x01\x18@*\x05r\x03\x18\x80\bR\x06fields\x1a9\n" +
	"\vFieldsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12\x14\n" +
	"\x05value\x18\x02 \x01(\tR\x05value:\x028\x01\x1a\t\n" +
	"\aOutputs\"O\n" +
	"\x05Level\x12\x15\n" +
	"\x11LEVEL_UNSPECIFIED\x10\x00\x12\x0e\n" +
	"\n" +
	"LEVEL_INFO\x10\x01\x12\x0e\n" +
	"\n" +
	"LEVEL_WARN\x10\x02\x12\x0f\n" +
	"\vLEVEL_ERROR\x10\x03\x1a\xcd\n" +
	"\n" +
	"\x04HTTP\x1a\xbe\b\n" +
	"\x06Inputs\x12!\n" +
	"\x03url\x18\x01 \x01(\tB\x0f\xe2A\x01\x02\xbaH\b\xc8\x01\x01r\x03\x88\x01\x01R\x03url\x12I\n" +
	"\x06method\x18\x02 \x01(\tB,\xbaH)r'\x10\x03\x18\x062!^(?i)(GET|POST|PUT|PATCH|DELETE)$H\x00R\x06method\x88\x01\x01\x12E\n" +
	"\aheaders\x18\x03 \x03(\v2+.flowstate.v1.Task.HTTP.Inputs.HeadersEntryR\aheaders\x12\x17\n" +
	"\x04body\x18\x04 \x01(\tH\x01R\x04body\x88\x01\x01\x12+\n" +
	"\x06bearer\x18\f \x01(\v2\x13.flowstate.v1.ValueR\x06bearer\x12B\n" +
	"\n" +
	"credential\x18\r \x01(\tB\x1d\xbaH\x1ar\x18\x10\x01\x18\x80\x012\x11^[a-z][a-z0-9-]*$H\x02R\n" +
	"credential\x88\x01\x01\x12E\n" +
	"\aoutputs\x18\x05 \x03(\v2+.flowstate.v1.Task.HTTP.Inputs.OutputsEntryR\aoutputs\x12?\n" +
	"\x05query\x18\x06 \x03(\v2).flowstate.v1.Task.HTTP.Inputs.QueryEntryR\x05query\x12'\n" +
	"\x04json\x18\a \x01(\v2\x13.flowstate.v1.ValueR\x04json\x12<\n" +
	"\x04form\x18\b \x03(\v2(.flowstate.v1.Task.HTTP.Inputs.FormEntryR\x04form\x12+\n" +
	"\x06expect\x18\t \x01(\v2\x13.flowstate.v1.ValueR\x06expect\x12\"\n" +
	"\n" +
	"parse_json\x18\n" +
	" \x01(\bH\x03R\tparseJson\x88\x01\x01\x12<\n" +
	"\x18retry_on_unknown_outcome\x18\v \x01(\bH\x04R\x15retryOnUnknownOutcome\x88\x01\x01\x1a:\n" +
	"\fHeadersEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12\x14\n" +
	"\x05value\x18\x02 \x01(\tR\x05value:\x028\x01\x1aO\n" +
	"\fOutputsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\x1aM\n" +
	"\n" +
	"QueryEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\x1aL\n" +
	"\tFormEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01B\t\n" +
	"\a_methodB\a\n" +
	"\x05_bodyB\r\n" +
	"\v_credentialB\r\n" +
	"\v_parse_jsonB\x1b\n" +
	"\x19_retry_on_unknown_outcome\x1a\x83\x02\n" +
	"\aOutputs\x12+\n" +
	"\vstatus_code\x18\x01 \x01(\x05B\n" +
	"\xbaH\a\x1a\x05\x18\xd7\x04(dR\n" +
	"statusCode\x12F\n" +
	"\aheaders\x18\x02 \x03(\v2,.flowstate.v1.Task.HTTP.Outputs.HeadersEntryR\aheaders\x12\x12\n" +
	"\x04body\x18\x03 \x01(\tR\x04body\x123\n" +
	"\x04json\x18\x04 \x01(\v2\x1f.google.api.expr.v1alpha1.ValueR\x04json\x1a:\n" +
	"\fHeadersEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12\x14\n" +
	"\x05value\x18\x02 \x01(\tR\x05value:\x028\x01\x1aN\n" +
	"\vInputsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01J\x04\b\x02\x10\x03R\vdescription\"\xee\x01\n" +
	"\n" +
	"RunRequest\x12>\n" +
	"\bworkflow\x18\x01 \x01(\v2\x16.flowstate.v1.WorkflowB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\bworkflow\x12P\n" +
	"\x06inputs\x18\x02 \x03(\v2$.flowstate.v1.RunRequest.InputsEntryB\x12\xe2A\x01\x01\xbaH\v\x9a\x01\b\x10@\"\x04r\x02\x10\x01R\x06inputs\x1aN\n" +
	"\vInputsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\"\xfd\x03\n" +
	"\vRunResponse\x12,\n" +
	"\vworkflow_id\x18\x01 \x01(\tB\v\xbaH\b\xc8\x01\x01r\x03\xb0\x01\x01R\n" +
	"workflowId\x12\"\n" +
	"\x06run_id\x18\x02 \x01(\tB\v\xbaH\b\xc8\x01\x01r\x03\xb0\x01\x01R\x05runId\x12G\n" +
	"\x06status\x18\x03 \x01(\x0e2 .flowstate.v1.RunResponse.StatusB\r\xbaH\n" +
	"\xc8\x01\x01\x82\x01\x04\x10\x01 \x00R\x06status\x127\n" +
	"\x05error\x18\x04 \x01(\v2\x1f.flowstate.v1.RunResponse.ErrorH\x00R\x05error\x12>\n" +
	"\aoutputs\x18\x05 \x01(\v2\".flowstate.v1.Workflow.StepOutputsH\x00R\aoutputs\x1a)\n" +
	"\x05Error\x12 \n" +
	"\amessage\x18\x01 \x01(\tB\x06\xbaH\x03\xc8\x01\x01R\amessage\"\x9f\x01\n" +
	"\x06Status\x12\x16\n" +
	"\x12STATUS_UNSPECIFIED\x10\x00\x12\x12\n" +
	"\x0eSTATUS_RUNNING\x10\x01\x12\x14\n" +
	"\x10STATUS_COMPLETED\x10\x02\x12\x11\n" +
	"\rSTATUS_FAILED\x10\x03\x12\x13\n" +
	"\x0fSTATUS_CANCELED\x10\x04\x12\x15\n" +
	"\x11STATUS_TERMINATED\x10\x05\x12\x14\n" +
	"\x10STATUS_TIMED_OUT\x10\x06B\r\n" +
	"\x04kind\x12\x05\xbaH\x02\b\x01\"j\n" +
	"\n" +
	"GetRequest\x12+\n" +
	"\vworkflow_id\x18\x01 \x01(\tB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\n" +
	"workflowId\x12$\n" +
	"\x06run_id\x18\x02 \x01(\tB\b\xbaH\x05r\x03\xb0\x01\x01H\x00R\x05runId\x88\x01\x01B\t\n" +
	"\a_run_id\"\xcc\x04\n" +
	"\vGetResponse\x12\x1f\n" +
	"\vworkflow_id\x18\x01 \x01(\tR\n" +
	"workflowId\x12\x15\n" +
	"\x06run_id\x18\x02 \x01(\tR\x05runId\x12G\n" +
	"\x06status\x18\x03 \x01(\x0e2 .flowstate.v1.RunResponse.StatusB\r\xbaH\n" +
	"\xc8\x01\x01\x82\x01\x04\x10\x01 \x00R\x06status\x127\n" +
	"\x05error\x18\x04 \x01(\v2\x1f.flowstate.v1.RunResponse.ErrorH\x00R\x05error\x12>\n" +
	"\aoutputs\x18\x05 \x01(\v2\".flowstate.v1.Workflow.StepOutputsH\x00R\aoutputs\x129\n" +
	"\n" +
	"start_time\x18\x06 \x01(\v2\x1a.google.protobuf.TimestampR\tstartTime\x129\n" +
	"\n" +
	"close_time\x18\a \x01(\v2\x1a.google.protobuf.TimestampR\tcloseTime\x125\n" +
	"\bprogress\x18\b \x01(\v2\x19.flowstate.v1.RunProgressR\bprogress\x12L\n" +
	"\x12pending_activities\x18\t \x03(\v2\x1d.flowstate.v1.PendingActivityR\x11pendingActivities\x129\n" +
	"\vrun_outputs\x18\n" +
	" \x01(\v2\x18.flowstate.v1.RunOutputsR\n" +
	"runOutputsB\r\n" +
	"\x04kind\x12\x05\xbaH\x02\b\x01\"\xbf\x01\n" +
	"\x0fPendingActivity\x12\x18\n" +
	"\aattempt\x18\x01 \x01(\x05R\aattempt\x12!\n" +
	"\flast_failure\x18\x02 \x01(\tR\vlastFailure\x12Y\n" +
	"\x1bnext_attempt_scheduled_time\x18\x03 \x01(\v2\x1a.google.protobuf.TimestampR\x18nextAttemptScheduledTime\x12\x14\n" +
	"\x05phase\x18\x04 \x01(\tR\x05phase\"c\n" +
	"\vRunProgress\x12\x17\n" +
	"\astep_id\x18\x01 \x01(\tR\x06stepId\x12\x12\n" +
	"\x04path\x18\x02 \x03(\tR\x04path\x12'\n" +
	"\x0fcompleted_steps\x18\x03 \x01(\x05R\x0ecompletedSteps\"\xae\x01\n" +
	"\n" +
	"Diagnostic\x12\x12\n" +
	"\x04line\x18\x01 \x01(\rR\x04line\x12\x16\n" +
	"\x06column\x18\x02 \x01(\rR\x06column\x12 \n" +
	"\amessage\x18\x03 \x01(\tB\x06\xbaH\x03\xc8\x01\x01R\amessage\x12\x12\n" +
	"\x04step\x18\x04 \x01(\tR\x04step\x12\x14\n" +
	"\x05field\x18\x05 \x01(\tR\x05field\x12\x12\n" +
	"\x04kind\x18\x06 \x01(\tR\x04kind\x12\x14\n" +
	"\x05value\x18\a \x01(\tR\x05value\"j\n" +
	"\x10DiagnosticReport\x12\x1a\n" +
	"\x04file\x18\x01 \x01(\tB\x06\xbaH\x03\xc8\x01\x01R\x04file\x12:\n" +
	"\vdiagnostics\x18\x02 \x03(\v2\x18.flowstate.v1.DiagnosticR\vdiagnostics\"H\n" +
	"\x10ValidationReport\x124\n" +
	"\x05files\x18\x01 \x03(\v2\x1e.flowstate.v1.DiagnosticReportR\x05files\"A\n" +
	"\tFixChange\x12\x12\n" +
	"\x04line\x18\x01 \x01(\rR\x04line\x12 \n" +
	"\amessage\x18\x02 \x01(\tB\x06\xbaH\x03\xc8\x01\x01R\amessage\"\xda\x01\n" +
	"\tFixReport\x12\x1a\n" +
	"\x04file\x18\x01 \x01(\tB\x06\xbaH\x03\xc8\x01\x01R\x04file\x12\x18\n" +
	"\achanged\x18\x02 \x01(\bR\achanged\x121\n" +
	"\achanges\x18\x03 \x03(\v2\x17.flowstate.v1.FixChangeR\achanges\x124\n" +
	"\brefusals\x18\x04 \x03(\v2\x18.flowstate.v1.DiagnosticR\brefusals\x12.\n" +
	"\x05notes\x18\x05 \x03(\v2\x18.flowstate.v1.DiagnosticR\x05notes\"w\n" +
	"\tFmtReport\x12\x1a\n" +
	"\x04file\x18\x01 \x01(\tB\x06\xbaH\x03\xc8\x01\x01R\x04file\x12\x18\n" +
	"\achanged\x18\x02 \x01(\bR\achanged\x124\n" +
	"\brefusals\x18\x03 \x03(\v2\x18.flowstate.v1.DiagnosticR\brefusals\";\n" +
	"\n" +
	"FixReports\x12-\n" +
	"\x05files\x18\x01 \x03(\v2\x17.flowstate.v1.FixReportR\x05files\";\n" +
	"\n" +
	"FmtReports\x12-\n" +
	"\x05files\x18\x01 \x03(\v2\x17.flowstate.v1.FmtReportR\x05files\"\xc1\x01\n" +
	"\bTestCase\x12\x1a\n" +
	"\x04name\x18\x01 \x01(\tB\x06\xbaH\x03\xc8\x01\x01R\x04name\x12\x16\n" +
	"\x06passed\x18\x02 \x01(\bR\x06passed\x124\n" +
	"\bfailures\x18\x03 \x03(\v2\x18.flowstate.v1.DiagnosticR\bfailures\x12\x14\n" +
	"\x05error\x18\x04 \x01(\tR\x05error\x125\n" +
	"\bduration\x18\x05 \x01(\v2\x19.google.protobuf.DurationR\bduration\"p\n" +
	"\n" +
	"TestReport\x12\x1a\n" +
	"\x04file\x18\x01 \x01(\tB\x06\xbaH\x03\xc8\x01\x01R\x04file\x12,\n" +
	"\x05cases\x18\x02 \x03(\v2\x16.flowstate.v1.TestCaseR\x05cases\x12\x18\n" +
	"\arefused\x18\x03 \x01(\tR\arefused\"=\n" +
	"\vTestReports\x12.\n" +
	"\x05files\x18\x01 \x03(\v2\x18.flowstate.v1.TestReportR\x05files\"\x81\x02\n" +
	"\x10WorkloadIdentity\x12\x18\n" +
	"\asubject\x18\x01 \x01(\tR\asubject\x12\x16\n" +
	"\x06issuer\x18\x02 \x01(\tR\x06issuer\x12B\n" +
	"\x06claims\x18\x03 \x03(\v2*.flowstate.v1.WorkloadIdentity.ClaimsEntryR\x06claims\x12\x1c\n" +
	"\tnamespace\x18\x04 \x01(\tR\tnamespace\x12\x1e\n" +
	"\n" +
	"deployment\x18\x05 \x01(\tR\n" +
	"deployment\x1a9\n" +
	"\vClaimsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12\x14\n" +
	"\x05value\x18\x02 \x01(\tR\x05value:\x028\x01\"\xa8\x03\n" +
	"\x05Frame\x12$\n" +
	"\tnext_node\x18\x01 \x01(\x05B\a\xbaH\x04\x1a\x02(\x00R\bnextNode\x12.\n" +
	"\x0enext_iteration\x18\x02 \x01(\x05B\a\xbaH\x04\x1a\x02(\x00R\rnextIteration\x12<\n" +
	"\aresults\x18\x03 \x03(\v2\".flowstate.v1.Workflow.StepOutputsR\aresults\x12E\n" +
	"\fcall_outputs\x18\x04 \x01(\v2\".flowstate.v1.Workflow.StepOutputsR\vcallOutputs\x12>\n" +
	"\tcall_vars\x18\x05 \x03(\v2!.flowstate.v1.Frame.CallVarsEntryR\bcallVars\x122\n" +
	"\n" +
	"loop_state\x18\x06 \x01(\v2\x13.flowstate.v1.ValueR\tloopState\x1aP\n" +
	"\rCallVarsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\"\x9e\x06\n" +
	"\bRunState\x12>\n" +
	"\bworkflow\x18\x01 \x01(\v2\x16.flowstate.v1.WorkflowB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\bworkflow\x12\x1b\n" +
	"\tnext_step\x18\x02 \x01(\x05R\bnextStep\x12<\n" +
	"\aoutputs\x18\x03 \x01(\v2\".flowstate.v1.Workflow.StepOutputsR\aoutputs\x12!\n" +
	"\fsteps_budget\x18\x04 \x01(\x05R\vstepsBudget\x12+\n" +
	"\x06frames\x18\x06 \x03(\v2\x13.flowstate.v1.FrameR\x06frames\x12:\n" +
	"\bidentity\x18\x05 \x01(\v2\x1e.flowstate.v1.WorkloadIdentityR\bidentity\x12D\n" +
	"\x0fpending_signals\x18\a \x03(\v2\x1b.flowstate.v1.PendingSignalR\x0ependingSignals\x12B\n" +
	"\x04vars\x18\b \x03(\v2 .flowstate.v1.RunState.VarsEntryB\f\xbaH\t\x9a\x01\x06\"\x04r\x02\x10\x01R\x04vars\x12J\n" +
	"\x06inputs\x18\t \x03(\v2\".flowstate.v1.RunState.InputsEntryB\x0e\xbaH\v\x9a\x01\b\x10@\"\x04r\x02\x10\x01R\x06inputs\x129\n" +
	"\vrun_outputs\x18\n" +
	" \x01(\v2\x18.flowstate.v1.RunOutputsR\n" +
	"runOutputs\x12<\n" +
	"\fpending_undo\x18\v \x03(\v2\x19.flowstate.v1.PendingUndoR\vpendingUndo\x1aL\n" +
	"\tVarsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\x1aN\n" +
	"\vInputsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\"\xde\x01\n" +
	"\rSignalRequest\x122\n" +
	"\vworkflow_id\x18\x01 \x01(\tB\x11\xe2A\x01\x02\xbaH\n" +
	"\xc8\x01\x01r\x05\x10\x01\x18\x80\x02R\n" +
	"workflowId\x12\x1f\n" +
	"\x06run_id\x18\x02 \x01(\tB\b\xbaH\x05r\x03\x18\x80\x02R\x05runId\x12B\n" +
	"\x04name\x18\x03 \x01(\tB.\xe2A\x01\x02\xbaH'\xc8\x01\x01r\"\x10\x01\x18\x80\x012\x1b^[A-Za-z0-9][A-Za-z0-9-_]*$R\x04name\x124\n" +
	"\apayload\x18\x04 \x01(\v2\x1a.flowstate.v1.Node.OutputsR\apayload\"\x10\n" +
	"\x0eSignalResponse\"d\n" +
	"\rCancelRequest\x122\n" +
	"\vworkflow_id\x18\x01 \x01(\tB\x11\xe2A\x01\x02\xbaH\n" +
	"\xc8\x01\x01r\x05\x10\x01\x18\x80\x02R\n" +
	"workflowId\x12\x1f\n" +
	"\x06run_id\x18\x02 \x01(\tB\b\xbaH\x05r\x03\x18\x80\x02R\x05runId\"\x10\n" +
	"\x0eCancelResponse\"\x89\x01\n" +
	"\x10TerminateRequest\x122\n" +
	"\vworkflow_id\x18\x01 \x01(\tB\x11\xe2A\x01\x02\xbaH\n" +
	"\xc8\x01\x01r\x05\x10\x01\x18\x80\x02R\n" +
	"workflowId\x12\x1f\n" +
	"\x06run_id\x18\x02 \x01(\tB\b\xbaH\x05r\x03\x18\x80\x02R\x05runId\x12 \n" +
	"\x06reason\x18\x03 \x01(\tB\b\xbaH\x05r\x03\x18\x80\bR\x06reason\"\x13\n" +
	"\x11TerminateResponse\"\x81\x01\n" +
	"\vListRequest\x12'\n" +
	"\tpage_size\x18\x01 \x01(\x05B\n" +
	"\xbaH\a\x1a\x05\x18\xe8\a(\x00R\bpageSize\x12'\n" +
	"\n" +
	"page_token\x18\x02 \x01(\tB\b\xbaH\x05r\x03\x18\x80 R\tpageToken\x12 \n" +
	"\x06filter\x18\x03 \x01(\tB\b\xbaH\x05r\x03\x18\x80 R\x06filter\"\x88\x02\n" +
	"\n" +
	"RunSummary\x12\x1f\n" +
	"\vworkflow_id\x18\x01 \x01(\tR\n" +
	"workflowId\x12\x15\n" +
	"\x06run_id\x18\x02 \x01(\tR\x05runId\x128\n" +
	"\x06status\x18\x03 \x01(\x0e2 .flowstate.v1.RunResponse.StatusR\x06status\x129\n" +
	"\n" +
	"start_time\x18\x04 \x01(\v2\x1a.google.protobuf.TimestampR\tstartTime\x129\n" +
	"\n" +
	"close_time\x18\x05 \x01(\v2\x1a.google.protobuf.TimestampR\tcloseTime\x12\x12\n" +
	"\x04name\x18\x06 \x01(\tR\x04name\"d\n" +
	"\fListResponse\x12,\n" +
	"\x04runs\x18\x01 \x03(\v2\x18.flowstate.v1.RunSummaryR\x04runs\x12&\n" +
	"\x0fnext_page_token\x18\x02 \x01(\tR\rnextPageToken\"O\n" +
	"\n" +
	"SourceFile\x12\x1e\n" +
	"\x04name\x18\x01 \x01(\tB\n" +
	"\xbaH\ar\x05\x10\x01\x18\x80\x04R\x04name\x12!\n" +
	"\x06source\x18\x02 \x01(\fB\t\xbaH\x06z\x04\x18\x80\x80@R\x06source\"M\n" +
	"\x0fValidateRequest\x12:\n" +
	"\x05files\x18\x01 \x03(\v2\x18.flowstate.v1.SourceFileB\n" +
	"\xbaH\a\x92\x01\x04\b\x01\x10@R\x05files\"J\n" +
	"\x10ValidateResponse\x126\n" +
	"\x06report\x18\x01 \x01(\v2\x1e.flowstate.v1.ValidationReportR\x06report\"J\n" +
	"\x0eCompileRequest\x128\n" +
	"\x04file\x18\x01 \x01(\v2\x18.flowstate.v1.SourceFileB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\x04file\"}\n" +
	"\x0fCompileResponse\x122\n" +
	"\bworkflow\x18\x01 \x01(\v2\x16.flowstate.v1.WorkflowR\bworkflow\x126\n" +
	"\x06report\x18\x02 \x01(\v2\x1e.flowstate.v1.DiagnosticReportR\x06report\"\x13\n" +
	"\x11GetCatalogRequest\"I\n" +
	"\x12GetCatalogResponse\x123\n" +
	"\acatalog\x18\x01 \x01(\v2\x19.flowstate.v1.TaskCatalogR\acatalog\"\xcc\x01\n" +
	"\x14ScheduleActionResult\x12?\n" +
	"\rschedule_time\x18\x01 \x01(\v2\x1a.google.protobuf.TimestampR\fscheduleTime\x12;\n" +
	"\vactual_time\x18\x02 \x01(\v2\x1a.google.protobuf.TimestampR\n" +
	"actualTime\x12\x1f\n" +
	"\vworkflow_id\x18\x03 \x01(\tR\n" +
	"workflowId\x12\x15\n" +
	"\x06run_id\x18\x04 \x01(\tR\x05runId\"\x91\x01\n" +
	"\x0fScheduleSummary\x12\x12\n" +
	"\x04name\x18\x01 \x01(\tR\x04name\x12\x16\n" +
	"\x06paused\x18\x02 \x01(\bR\x06paused\x12\x12\n" +
	"\x04note\x18\x03 \x01(\tR\x04note\x12>\n" +
	"\rnext_run_time\x18\x04 \x01(\v2\x1a.google.protobuf.TimestampR\vnextRunTime\"\xf2\x03\n" +
	"\x13ScheduleDescription\x12\x12\n" +
	"\x04name\x18\x01 \x01(\tR\x04name\x12#\n" +
	"\rworkflow_name\x18\x02 \x01(\tR\fworkflowName\x127\n" +
	"\atrigger\x18\x03 \x01(\v2\x1d.flowstate.v1.ScheduleTriggerR\atrigger\x12\x16\n" +
	"\x06paused\x18\x04 \x01(\bR\x06paused\x12\x12\n" +
	"\x04note\x18\x05 \x01(\tR\x04note\x12\x1f\n" +
	"\vnum_actions\x18\x06 \x01(\x03R\n" +
	"numActions\x12@\n" +
	"\x0enext_run_times\x18\a \x03(\v2\x1a.google.protobuf.TimestampR\fnextRunTimes\x12C\n" +
	"\vrecent_runs\x18\b \x03(\v2\".flowstate.v1.ScheduleActionResultR\n" +
	"recentRuns\x12E\n" +
	"\x06inputs\x18\t \x03(\v2-.flowstate.v1.ScheduleDescription.InputsEntryR\x06inputs\x1aN\n" +
	"\vInputsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\"\xd9\x02\n" +
	"\x15CreateScheduleRequest\x12>\n" +
	"\bworkflow\x18\x01 \x01(\v2\x16.flowstate.v1.WorkflowB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\bworkflow\x12[\n" +
	"\x06inputs\x18\x02 \x03(\v2/.flowstate.v1.CreateScheduleRequest.InputsEntryB\x12\xe2A\x01\x01\xbaH\v\x9a\x01\b\x10@\"\x04r\x02\x10\x01R\x06inputs\x125\n" +
	"\x04name\x18\x03 \x01(\tB!\xe2A\x01\x01\xbaH\x1ar\x18\x18\x80\x012\x13^$|^[A-Za-z0-9-_]+$R\x04name\x12\x1c\n" +
	"\x06paused\x18\x04 \x01(\bB\x04\xe2A\x01\x01R\x06paused\x1aN\n" +
	"\vInputsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\"W\n" +
	"\x16CreateScheduleResponse\x12=\n" +
	"\bschedule\x18\x01 \x01(\v2!.flowstate.v1.ScheduleDescriptionR\bschedule\"\x16\n" +
	"\x14ListSchedulesRequest\"r\n" +
	"\x15ListSchedulesResponse\x12;\n" +
	"\tschedules\x18\x01 \x03(\v2\x1d.flowstate.v1.ScheduleSummaryR\tschedules\x12\x1c\n" +
	"\ttruncated\x18\x02 \x01(\bR\ttruncated\"R\n" +
	"\x17DescribeScheduleRequest\x127\n" +
	"\x04name\x18\x01 \x01(\tB#\xe2A\x01\x02\xbaH\x1c\xc8\x01\x01r\x17\x10\x01\x18\x80\x012\x10^[A-Za-z0-9-_]+$R\x04name\"Y\n" +
	"\x18DescribeScheduleResponse\x12=\n" +
	"\bschedule\x18\x01 \x01(\v2!.flowstate.v1.ScheduleDescriptionR\bschedule\"P\n" +
	"\x15DeleteScheduleRequest\x127\n" +
	"\x04name\x18\x01 \x01(\tB#\xe2A\x01\x02\xbaH\x1c\xc8\x01\x01r\x17\x10\x01\x18\x80\x012\x10^[A-Za-z0-9-_]+$R\x04name\"\x18\n" +
	"\x16DeleteScheduleResponse\"q\n" +
	"\x14PauseScheduleRequest\x127\n" +
	"\x04name\x18\x01 \x01(\tB#\xe2A\x01\x02\xbaH\x1c\xc8\x01\x01r\x17\x10\x01\x18\x80\x012\x10^[A-Za-z0-9-_]+$R\x04name\x12 \n" +
	"\x04note\x18\x02 \x01(\tB\f\xe2A\x01\x01\xbaH\x05r\x03\x18\x80\x02R\x04note\"\x17\n" +
	"\x15PauseScheduleResponse\"r\n" +
	"\x15ResumeScheduleRequest\x127\n" +
	"\x04name\x18\x01 \x01(\tB#\xe2A\x01\x02\xbaH\x1c\xc8\x01\x01r\x17\x10\x01\x18\x80\x012\x10^[A-Za-z0-9-_]+$R\x04name\x12 \n" +
	"\x04note\x18\x02 \x01(\tB\f\xe2A\x01\x01\xbaH\x05r\x03\x18\x80\x02R\x04note\"\x18\n" +
	"\x16ResumeScheduleResponse\"Q\n" +
	"\x16TriggerScheduleRequest\x127\n" +
	"\x04name\x18\x01 \x01(\tB#\xe2A\x01\x02\xbaH\x1c\xc8\x01\x01r\x17\x10\x01\x18\x80\x012\x10^[A-Za-z0-9-_]+$R\x04name\"\x19\n" +
	"\x17TriggerScheduleResponse2\xb2\n" +
	"\n" +
	"\x0fWorkflowService\x12<\n" +
	"\x03Run\x12\x18.flowstate.v1.RunRequest\x1a\x19.flowstate.v1.RunResponse\"\x00\x12<\n" +
	"\x03Get\x12\x18.flowstate.v1.GetRequest\x1a\x19.flowstate.v1.GetResponse\"\x00\x12E\n" +
	"\x06Signal\x12\x1b.flowstate.v1.SignalRequest\x1a\x1c.flowstate.v1.SignalResponse\"\x00\x12?\n" +
	"\x04List\x12\x19.flowstate.v1.ListRequest\x1a\x1a.flowstate.v1.ListResponse\"\x00\x12E\n" +
	"\x06Cancel\x12\x1b.flowstate.v1.CancelRequest\x1a\x1c.flowstate.v1.CancelResponse\"\x00\x12N\n" +
	"\tTerminate\x12\x1e.flowstate.v1.TerminateRequest\x1a\x1f.flowstate.v1.TerminateResponse\"\x00\x12K\n" +
	"\bValidate\x12\x1d.flowstate.v1.ValidateRequest\x1a\x1e.flowstate.v1.ValidateResponse\"\x00\x12H\n" +
	"\aCompile\x12\x1c.flowstate.v1.CompileRequest\x1a\x1d.flowstate.v1.CompileResponse\"\x00\x12Q\n" +
	"\n" +
	"GetCatalog\x12\x1f.flowstate.v1.GetCatalogRequest\x1a .flowstate.v1.GetCatalogResponse\"\x00\x12]\n" +
	"\x0eCreateSchedule\x12#.flowstate.v1.CreateScheduleRequest\x1a$.flowstate.v1.CreateScheduleResponse\"\x00\x12Z\n" +
	"\rListSchedules\x12\".flowstate.v1.ListSchedulesRequest\x1a#.flowstate.v1.ListSchedulesResponse\"\x00\x12c\n" +
	"\x10DescribeSchedule\x12%.flowstate.v1.DescribeScheduleRequest\x1a&.flowstate.v1.DescribeScheduleResponse\"\x00\x12]\n" +
	"\x0eDeleteSchedule\x12#.flowstate.v1.DeleteScheduleRequest\x1a$.flowstate.v1.DeleteScheduleResponse\"\x00\x12Z\n" +
	"\rPauseSchedule\x12\".flowstate.v1.PauseScheduleRequest\x1a#.flowstate.v1.PauseScheduleResponse\"\x00\x12]\n" +
	"\x0eResumeSchedule\x12#.flowstate.v1.ResumeScheduleRequest\x1a$.flowstate.v1.ResumeScheduleResponse\"\x00\x12`\n" +
	"\x0fTriggerSchedule\x12$.flowstate.v1.TriggerScheduleRequest\x1a%.flowstate.v1.TriggerScheduleResponse\"\x00B\xad\x01\n" +
	"\x10com.flowstate.v1B\x0eFlowstateProtoP\x01Z8github.com/picatz/flowstate/pkg/flowstate/v1;flowstatev1\xa2\x02\x03FXX\xaa\x02\fFlowstate.V1\xca\x02\fFlowstate\\V1\xe2\x02\x18Flowstate\\V1\\GPBMetadata\xea\x02\rFlowstate::V1b\x06proto3"

var (
	file_flowstate_v1_flowstate_proto_rawDescOnce sync.Once
	file_flowstate_v1_flowstate_proto_rawDescData []byte
)

func file_flowstate_v1_flowstate_proto_rawDescGZIP() []byte {
	file_flowstate_v1_flowstate_proto_rawDescOnce.Do(func() {
		file_flowstate_v1_flowstate_proto_rawDescData = protoimpl.X.CompressGZIP(unsafe.Slice(unsafe.StringData(file_flowstate_v1_flowstate_proto_rawDesc), len(file_flowstate_v1_flowstate_proto_rawDesc)))
	})
	return file_flowstate_v1_flowstate_proto_rawDescData
}

var file_flowstate_v1_flowstate_proto_enumTypes = make([]protoimpl.EnumInfo, 6)
var file_flowstate_v1_flowstate_proto_msgTypes = make([]protoimpl.MessageInfo, 126)
var file_flowstate_v1_flowstate_proto_goTypes = []any{
	(InputDeclaration_Type)(0),       // 0: flowstate.v1.InputDeclaration.Type
	(ScheduleTrigger_Overlap)(0),     // 1: flowstate.v1.ScheduleTrigger.Overlap
	(Value_Type)(0),                  // 2: flowstate.v1.Value.Type
	(Value_Error_Code)(0),            // 3: flowstate.v1.Value.Error.Code
	(Task_Log_Level)(0),              // 4: flowstate.v1.Task.Log.Level
	(RunResponse_Status)(0),          // 5: flowstate.v1.RunResponse.Status
	(*Workflow)(nil),                 // 6: flowstate.v1.Workflow
	(*SignalPolicy)(nil),             // 7: flowstate.v1.SignalPolicy
	(*SignalPolicyRule)(nil),         // 8: flowstate.v1.SignalPolicyRule
	(*InputDeclaration)(nil),         // 9: flowstate.v1.InputDeclaration
	(*OutputDeclaration)(nil),        // 10: flowstate.v1.OutputDeclaration
	(*RunOutputs)(nil),               // 11: flowstate.v1.RunOutputs
	(*Triggers)(nil),                 // 12: flowstate.v1.Triggers
	(*ScheduleTrigger)(nil),          // 13: flowstate.v1.ScheduleTrigger
	(*Node)(nil),                     // 14: flowstate.v1.Node
	(*Compensation)(nil),             // 15: flowstate.v1.Compensation
	(*PendingUndo)(nil),              // 16: flowstate.v1.PendingUndo
	(*SecretRef)(nil),                // 17: flowstate.v1.SecretRef
	(*Wait)(nil),                     // 18: flowstate.v1.Wait
	(*Signal)(nil),                   // 19: flowstate.v1.Signal
	(*SignalSender)(nil),             // 20: flowstate.v1.SignalSender
	(*PendingSignal)(nil),            // 21: flowstate.v1.PendingSignal
	(*SignalDelivery)(nil),           // 22: flowstate.v1.SignalDelivery
	(*Scope)(nil),                    // 23: flowstate.v1.Scope
	(*ForEach)(nil),                  // 24: flowstate.v1.ForEach
	(*Parallel)(nil),                 // 25: flowstate.v1.Parallel
	(*Loop)(nil),                     // 26: flowstate.v1.Loop
	(*Call)(nil),                     // 27: flowstate.v1.Call
	(*StepPolicy)(nil),               // 28: flowstate.v1.StepPolicy
	(*RetryPolicy)(nil),              // 29: flowstate.v1.RetryPolicy
	(*Value)(nil),                    // 30: flowstate.v1.Value
	(*TaskCatalog)(nil),              // 31: flowstate.v1.TaskCatalog
	(*CELFunction)(nil),              // 32: flowstate.v1.CELFunction
	(*TaskDescription)(nil),          // 33: flowstate.v1.TaskDescription
	(*PluginCatalog)(nil),            // 34: flowstate.v1.PluginCatalog
	(*PluginDescription)(nil),        // 35: flowstate.v1.PluginDescription
	(*TaskField)(nil),                // 36: flowstate.v1.TaskField
	(*Task)(nil),                     // 37: flowstate.v1.Task
	(*RunRequest)(nil),               // 38: flowstate.v1.RunRequest
	(*RunResponse)(nil),              // 39: flowstate.v1.RunResponse
	(*GetRequest)(nil),               // 40: flowstate.v1.GetRequest
	(*GetResponse)(nil),              // 41: flowstate.v1.GetResponse
	(*PendingActivity)(nil),          // 42: flowstate.v1.PendingActivity
	(*RunProgress)(nil),              // 43: flowstate.v1.RunProgress
	(*Diagnostic)(nil),               // 44: flowstate.v1.Diagnostic
	(*DiagnosticReport)(nil),         // 45: flowstate.v1.DiagnosticReport
	(*ValidationReport)(nil),         // 46: flowstate.v1.ValidationReport
	(*FixChange)(nil),                // 47: flowstate.v1.FixChange
	(*FixReport)(nil),                // 48: flowstate.v1.FixReport
	(*FmtReport)(nil),                // 49: flowstate.v1.FmtReport
	(*FixReports)(nil),               // 50: flowstate.v1.FixReports
	(*FmtReports)(nil),               // 51: flowstate.v1.FmtReports
	(*TestCase)(nil),                 // 52: flowstate.v1.TestCase
	(*TestReport)(nil),               // 53: flowstate.v1.TestReport
	(*TestReports)(nil),              // 54: flowstate.v1.TestReports
	(*WorkloadIdentity)(nil),         // 55: flowstate.v1.WorkloadIdentity
	(*Frame)(nil),                    // 56: flowstate.v1.Frame
	(*RunState)(nil),                 // 57: flowstate.v1.RunState
	(*SignalRequest)(nil),            // 58: flowstate.v1.SignalRequest
	(*SignalResponse)(nil),           // 59: flowstate.v1.SignalResponse
	(*CancelRequest)(nil),            // 60: flowstate.v1.CancelRequest
	(*CancelResponse)(nil),           // 61: flowstate.v1.CancelResponse
	(*TerminateRequest)(nil),         // 62: flowstate.v1.TerminateRequest
	(*TerminateResponse)(nil),        // 63: flowstate.v1.TerminateResponse
	(*ListRequest)(nil),              // 64: flowstate.v1.ListRequest
	(*RunSummary)(nil),               // 65: flowstate.v1.RunSummary
	(*ListResponse)(nil),             // 66: flowstate.v1.ListResponse
	(*SourceFile)(nil),               // 67: flowstate.v1.SourceFile
	(*ValidateRequest)(nil),          // 68: flowstate.v1.ValidateRequest
	(*ValidateResponse)(nil),         // 69: flowstate.v1.ValidateResponse
	(*CompileRequest)(nil),           // 70: flowstate.v1.CompileRequest
	(*CompileResponse)(nil),          // 71: flowstate.v1.CompileResponse
	(*GetCatalogRequest)(nil),        // 72: flowstate.v1.GetCatalogRequest
	(*GetCatalogResponse)(nil),       // 73: flowstate.v1.GetCatalogResponse
	(*ScheduleActionResult)(nil),     // 74: flowstate.v1.ScheduleActionResult
	(*ScheduleSummary)(nil),          // 75: flowstate.v1.ScheduleSummary
	(*ScheduleDescription)(nil),      // 76: flowstate.v1.ScheduleDescription
	(*CreateScheduleRequest)(nil),    // 77: flowstate.v1.CreateScheduleRequest
	(*CreateScheduleResponse)(nil),   // 78: flowstate.v1.CreateScheduleResponse
	(*ListSchedulesRequest)(nil),     // 79: flowstate.v1.ListSchedulesRequest
	(*ListSchedulesResponse)(nil),    // 80: flowstate.v1.ListSchedulesResponse
	(*DescribeScheduleRequest)(nil),  // 81: flowstate.v1.DescribeScheduleRequest
	(*DescribeScheduleResponse)(nil), // 82: flowstate.v1.DescribeScheduleResponse
	(*DeleteScheduleRequest)(nil),    // 83: flowstate.v1.DeleteScheduleRequest
	(*DeleteScheduleResponse)(nil),   // 84: flowstate.v1.DeleteScheduleResponse
	(*PauseScheduleRequest)(nil),     // 85: flowstate.v1.PauseScheduleRequest
	(*PauseScheduleResponse)(nil),    // 86: flowstate.v1.PauseScheduleResponse
	(*ResumeScheduleRequest)(nil),    // 87: flowstate.v1.ResumeScheduleRequest
	(*ResumeScheduleResponse)(nil),   // 88: flowstate.v1.ResumeScheduleResponse
	(*TriggerScheduleRequest)(nil),   // 89: flowstate.v1.TriggerScheduleRequest
	(*TriggerScheduleResponse)(nil),  // 90: flowstate.v1.TriggerScheduleResponse
	(*Workflow_StepOutputs)(nil),     // 91: flowstate.v1.Workflow.StepOutputs
	nil,                              // 92: flowstate.v1.Workflow.LabelsEntry
	nil,                              // 93: flowstate.v1.Workflow.VarsEntry
	nil,                              // 94: flowstate.v1.Workflow.SignalsEntry
	nil,                              // 95: flowstate.v1.Workflow.StepOutputs.StepValuesEntry
	nil,                              // 96: flowstate.v1.SignalPolicyRule.ClaimsEntry
	nil,                              // 97: flowstate.v1.RunOutputs.ValuesEntry
	(*Node_Outputs)(nil),             // 98: flowstate.v1.Node.Outputs
	nil,                              // 99: flowstate.v1.Node.VarsEntry
	nil,                              // 100: flowstate.v1.Node.Outputs.NamedValuesEntry
	nil,                              // 101: flowstate.v1.Scope.VarsEntry
	nil,                              // 102: flowstate.v1.Scope.AmbientVarsEntry
	nil,                              // 103: flowstate.v1.Scope.InputsEntry
	(*Parallel_Branch)(nil),          // 104: flowstate.v1.Parallel.Branch
	nil,                              // 105: flowstate.v1.Call.ArgumentsEntry
	(*Value_Error)(nil),              // 106: flowstate.v1.Value.Error
	(*Value_Structure)(nil),          // 107: flowstate.v1.Value.Structure
	(*Value_Structure_List)(nil),     // 108: flowstate.v1.Value.Structure.List
	(*Value_Structure_Map)(nil),      // 109: flowstate.v1.Value.Structure.Map
	nil,                              // 110: flowstate.v1.Value.Structure.Map.EntriesEntry
	(*Task_Log)(nil),                 // 111: flowstate.v1.Task.Log
	(*Task_HTTP)(nil),                // 112: flowstate.v1.Task.HTTP
	nil,                              // 113: flowstate.v1.Task.InputsEntry
	(*Task_Log_Inputs)(nil),          // 114: flowstate.v1.Task.Log.Inputs
	(*Task_Log_Outputs)(nil),         // 115: flowstate.v1.Task.Log.Outputs
	nil,                              // 116: flowstate.v1.Task.Log.Inputs.FieldsEntry
	(*Task_HTTP_Inputs)(nil),         // 117: flowstate.v1.Task.HTTP.Inputs
	(*Task_HTTP_Outputs)(nil),        // 118: flowstate.v1.Task.HTTP.Outputs
	nil,                              // 119: flowstate.v1.Task.HTTP.Inputs.HeadersEntry
	nil,                              // 120: flowstate.v1.Task.HTTP.Inputs.OutputsEntry
	nil,                              // 121: flowstate.v1.Task.HTTP.Inputs.QueryEntry
	nil,                              // 122: flowstate.v1.Task.HTTP.Inputs.FormEntry
	nil,                              // 123: flowstate.v1.Task.HTTP.Outputs.HeadersEntry
	nil,                              // 124: flowstate.v1.RunRequest.InputsEntry
	(*RunResponse_Error)(nil),        // 125: flowstate.v1.RunResponse.Error
	nil,                              // 126: flowstate.v1.WorkloadIdentity.ClaimsEntry
	nil,                              // 127: flowstate.v1.Frame.CallVarsEntry
	nil,                              // 128: flowstate.v1.RunState.VarsEntry
	nil,                              // 129: flowstate.v1.RunState.InputsEntry
	nil,                              // 130: flowstate.v1.ScheduleDescription.InputsEntry
	nil,                              // 131: flowstate.v1.CreateScheduleRequest.InputsEntry
	(*durationpb.Duration)(nil),      // 132: google.protobuf.Duration
	(*timestamppb.Timestamp)(nil),    // 133: google.protobuf.Timestamp
	(*v1alpha1.ParsedExpr)(nil),      // 134: google.api.expr.v1alpha1.ParsedExpr
	(*v1alpha1.Value)(nil),           // 135: google.api.expr.v1alpha1.Value
}
var file_flowstate_v1_flowstate_proto_depIdxs = []int32{
	14,  // 0: flowstate.v1.Workflow.steps:type_name -> flowstate.v1.Node
	92,  // 1: flowstate.v1.Workflow.labels:type_name -> flowstate.v1.Workflow.LabelsEntry
	93,  // 2: flowstate.v1.Workflow.vars:type_name -> flowstate.v1.Workflow.VarsEntry
	9,   // 3: flowstate.v1.Workflow.declared_inputs:type_name -> flowstate.v1.InputDeclaration
	10,  // 4: flowstate.v1.Workflow.declared_outputs:type_name -> flowstate.v1.OutputDeclaration
	12,  // 5: flowstate.v1.Workflow.triggers:type_name -> flowstate.v1.Triggers
	94,  // 6: flowstate.v1.Workflow.signals:type_name -> flowstate.v1.Workflow.SignalsEntry
	8,   // 7: flowstate.v1.SignalPolicy.allow:type_name -> flowstate.v1.SignalPolicyRule
	96,  // 8: flowstate.v1.SignalPolicyRule.claims:type_name -> flowstate.v1.SignalPolicyRule.ClaimsEntry
	0,   // 9: flowstate.v1.InputDeclaration.type:type_name -> flowstate.v1.InputDeclaration.Type
	30,  // 10: flowstate.v1.InputDeclaration.default:type_name -> flowstate.v1.Value
	30,  // 11: flowstate.v1.InputDeclaration.example:type_name -> flowstate.v1.Value
	30,  // 12: flowstate.v1.OutputDeclaration.value:type_name -> flowstate.v1.Value
	97,  // 13: flowstate.v1.RunOutputs.values:type_name -> flowstate.v1.RunOutputs.ValuesEntry
	13,  // 14: flowstate.v1.Triggers.schedule:type_name -> flowstate.v1.ScheduleTrigger
	132, // 15: flowstate.v1.ScheduleTrigger.every:type_name -> google.protobuf.Duration
	132, // 16: flowstate.v1.ScheduleTrigger.jitter:type_name -> google.protobuf.Duration
	1,   // 17: flowstate.v1.ScheduleTrigger.overlap:type_name -> flowstate.v1.ScheduleTrigger.Overlap
	37,  // 18: flowstate.v1.Node.task:type_name -> flowstate.v1.Task
	24,  // 19: flowstate.v1.Node.for_each:type_name -> flowstate.v1.ForEach
	25,  // 20: flowstate.v1.Node.parallel:type_name -> flowstate.v1.Parallel
	18,  // 21: flowstate.v1.Node.wait:type_name -> flowstate.v1.Wait
	27,  // 22: flowstate.v1.Node.call:type_name -> flowstate.v1.Call
	26,  // 23: flowstate.v1.Node.loop:type_name -> flowstate.v1.Loop
	30,  // 24: flowstate.v1.Node.condition:type_name -> flowstate.v1.Value
	28,  // 25: flowstate.v1.Node.policy:type_name -> flowstate.v1.StepPolicy
	99,  // 26: flowstate.v1.Node.vars:type_name -> flowstate.v1.Node.VarsEntry
	15,  // 27: flowstate.v1.Node.undo:type_name -> flowstate.v1.Compensation
	37,  // 28: flowstate.v1.Compensation.task:type_name -> flowstate.v1.Task
	37,  // 29: flowstate.v1.PendingUndo.task:type_name -> flowstate.v1.Task
	132, // 30: flowstate.v1.Wait.duration:type_name -> google.protobuf.Duration
	30,  // 31: flowstate.v1.Wait.until:type_name -> flowstate.v1.Value
	19,  // 32: flowstate.v1.Wait.signal:type_name -> flowstate.v1.Signal
	132, // 33: flowstate.v1.Wait.timeout:type_name -> google.protobuf.Duration
	55,  // 34: flowstate.v1.SignalSender.identity:type_name -> flowstate.v1.WorkloadIdentity
	133, // 35: flowstate.v1.SignalSender.accepted_at:type_name -> google.protobuf.Timestamp
	98,  // 36: flowstate.v1.PendingSignal.payload:type_name -> flowstate.v1.Node.Outputs
	20,  // 37: flowstate.v1.PendingSignal.sender:type_name -> flowstate.v1.SignalSender
	98,  // 38: flowstate.v1.SignalDelivery.payload:type_name -> flowstate.v1.Node.Outputs
	20,  // 39: flowstate.v1.SignalDelivery.sender:type_name -> flowstate.v1.SignalSender
	91,  // 40: flowstate.v1.Scope.outputs:type_name -> flowstate.v1.Workflow.StepOutputs
	101, // 41: flowstate.v1.Scope.vars:type_name -> flowstate.v1.Scope.VarsEntry
	102, // 42: flowstate.v1.Scope.ambient_vars:type_name -> flowstate.v1.Scope.AmbientVarsEntry
	103, // 43: flowstate.v1.Scope.inputs:type_name -> flowstate.v1.Scope.InputsEntry
	55,  // 44: flowstate.v1.Scope.identity:type_name -> flowstate.v1.WorkloadIdentity
	30,  // 45: flowstate.v1.ForEach.items:type_name -> flowstate.v1.Value
	14,  // 46: flowstate.v1.ForEach.body:type_name -> flowstate.v1.Node
	104, // 47: flowstate.v1.Parallel.branches:type_name -> flowstate.v1.Parallel.Branch
	14,  // 48: flowstate.v1.Loop.body:type_name -> flowstate.v1.Node
	30,  // 49: flowstate.v1.Loop.until:type_name -> flowstate.v1.Value
	30,  // 50: flowstate.v1.Loop.initial:type_name -> flowstate.v1.Value
	30,  // 51: flowstate.v1.Loop.update:type_name -> flowstate.v1.Value
	6,   // 52: flowstate.v1.Call.workflow:type_name -> flowstate.v1.Workflow
	105, // 53: flowstate.v1.Call.arguments:type_name -> flowstate.v1.Call.ArgumentsEntry
	132, // 54: flowstate.v1.StepPolicy.timeout:type_name -> google.protobuf.Duration
	29,  // 55: flowstate.v1.StepPolicy.retry:type_name -> flowstate.v1.RetryPolicy
	132, // 56: flowstate.v1.RetryPolicy.initial_interval:type_name -> google.protobuf.Duration
	132, // 57: flowstate.v1.RetryPolicy.max_interval:type_name -> google.protobuf.Duration
	134, // 58: flowstate.v1.Value.expr:type_name -> google.api.expr.v1alpha1.ParsedExpr
	135, // 59: flowstate.v1.Value.literal:type_name -> google.api.expr.v1alpha1.Value
	106, // 60: flowstate.v1.Value.error:type_name -> flowstate.v1.Value.Error
	17,  // 61: flowstate.v1.Value.secret_ref:type_name -> flowstate.v1.SecretRef
	107, // 62: flowstate.v1.Value.structure:type_name -> flowstate.v1.Value.Structure
	33,  // 63: flowstate.v1.TaskCatalog.tasks:type_name -> flowstate.v1.TaskDescription
	32,  // 64: flowstate.v1.TaskCatalog.cel_functions:type_name -> flowstate.v1.CELFunction
	36,  // 65: flowstate.v1.TaskDescription.inputs:type_name -> flowstate.v1.TaskField
	36,  // 66: flowstate.v1.TaskDescription.outputs:type_name -> flowstate.v1.TaskField
	35,  // 67: flowstate.v1.PluginCatalog.plugins:type_name -> flowstate.v1.PluginDescription
	33,  // 68: flowstate.v1.PluginDescription.tasks:type_name -> flowstate.v1.TaskDescription
	113, // 69: flowstate.v1.Task.inputs:type_name -> flowstate.v1.Task.InputsEntry
	6,   // 70: flowstate.v1.RunRequest.workflow:type_name -> flowstate.v1.Workflow
	124, // 71: flowstate.v1.RunRequest.inputs:type_name -> flowstate.v1.RunRequest.InputsEntry
	5,   // 72: flowstate.v1.RunResponse.status:type_name -> flowstate.v1.RunResponse.Status
	125, // 73: flowstate.v1.RunResponse.error:type_name -> flowstate.v1.RunResponse.Error
	91,  // 74: flowstate.v1.RunResponse.outputs:type_name -> flowstate.v1.Workflow.StepOutputs
	5,   // 75: flowstate.v1.GetResponse.status:type_name -> flowstate.v1.RunResponse.Status
	125, // 76: flowstate.v1.GetResponse.error:type_name -> flowstate.v1.RunResponse.Error
	91,  // 77: flowstate.v1.GetResponse.outputs:type_name -> flowstate.v1.Workflow.StepOutputs
	133, // 78: flowstate.v1.GetResponse.start_time:type_name -> google.protobuf.Timestamp
	133, // 79: flowstate.v1.GetResponse.close_time:type_name -> google.protobuf.Timestamp
	43,  // 80: flowstate.v1.GetResponse.progress:type_name -> flowstate.v1.RunProgress
	42,  // 81: flowstate.v1.GetResponse.pending_activities:type_name -> flowstate.v1.PendingActivity
	11,  // 82: flowstate.v1.GetResponse.run_outputs:type_name -> flowstate.v1.RunOutputs
	133, // 83: flowstate.v1.PendingActivity.next_attempt_scheduled_time:type_name -> google.protobuf.Timestamp
	44,  // 84: flowstate.v1.DiagnosticReport.diagnostics:type_name -> flowstate.v1.Diagnostic
	45,  // 85: flowstate.v1.ValidationReport.files:type_name -> flowstate.v1.DiagnosticReport
	47,  // 86: flowstate.v1.FixReport.changes:type_name -> flowstate.v1.FixChange
	44,  // 87: flowstate.v1.FixReport.refusals:type_name -> flowstate.v1.Diagnostic
	44,  // 88: flowstate.v1.FixReport.notes:type_name -> flowstate.v1.Diagnostic
	44,  // 89: flowstate.v1.FmtReport.refusals:type_name -> flowstate.v1.Diagnostic
	48,  // 90: flowstate.v1.FixReports.files:type_name -> flowstate.v1.FixReport
	49,  // 91: flowstate.v1.FmtReports.files:type_name -> flowstate.v1.FmtReport
	44,  // 92: flowstate.v1.TestCase.failures:type_name -> flowstate.v1.Diagnostic
	132, // 93: flowstate.v1.TestCase.duration:type_name -> google.protobuf.Duration
	52,  // 94: flowstate.v1.TestReport.cases:type_name -> flowstate.v1.TestCase
	53,  // 95: flowstate.v1.TestReports.files:type_name -> flowstate.v1.TestReport
	126, // 96: flowstate.v1.WorkloadIdentity.claims:type_name -> flowstate.v1.WorkloadIdentity.ClaimsEntry
	91,  // 97: flowstate.v1.Frame.results:type_name -> flowstate.v1.Workflow.StepOutputs
	91,  // 98: flowstate.v1.Frame.call_outputs:type_name -> flowstate.v1.Workflow.StepOutputs
	127, // 99: flowstate.v1.Frame.call_vars:type_name -> flowstate.v1.Frame.CallVarsEntry
	30,  // 100: flowstate.v1.Frame.loop_state:type_name -> flowstate.v1.Value
	6,   // 101: flowstate.v1.RunState.workflow:type_name -> flowstate.v1.Workflow
	91,  // 102: flowstate.v1.RunState.outputs:type_name -> flowstate.v1.Workflow.StepOutputs
	56,  // 103: flowstate.v1.RunState.frames:type_name -> flowstate.v1.Frame
	55,  // 104: flowstate.v1.RunState.identity:type_name -> flowstate.v1.WorkloadIdentity
	21,  // 105: flowstate.v1.RunState.pending_signals:type_name -> flowstate.v1.PendingSignal
	128, // 106: flowstate.v1.RunState.vars:type_name -> flowstate.v1.RunState.VarsEntry
	129, // 107: flowstate.v1.RunState.inputs:type_name -> flowstate.v1.RunState.InputsEntry
	11,  // 108: flowstate.v1.RunState.run_outputs:type_name -> flowstate.v1.RunOutputs
	16,  // 109: flowstate.v1.RunState.pending_undo:type_name -> flowstate.v1.PendingUndo
	98,  // 110: flowstate.v1.SignalRequest.payload:type_name -> flowstate.v1.Node.Outputs
	5,   // 111: flowstate.v1.RunSummary.status:type_name -> flowstate.v1.RunResponse.Status
	133, // 112: flowstate.v1.RunSummary.start_time:type_name -> google.protobuf.Timestamp
	133, // 113: flowstate.v1.RunSummary.close_time:type_name -> google.protobuf.Timestamp
	65,  // 114: flowstate.v1.ListResponse.runs:type_name -> flowstate.v1.RunSummary
	67,  // 115: flowstate.v1.ValidateRequest.files:type_name -> flowstate.v1.SourceFile
	46,  // 116: flowstate.v1.ValidateResponse.report:type_name -> flowstate.v1.ValidationReport
	67,  // 117: flowstate.v1.CompileRequest.file:type_name -> flowstate.v1.SourceFile
	6,   // 118: flowstate.v1.CompileResponse.workflow:type_name -> flowstate.v1.Workflow
	45,  // 119: flowstate.v1.CompileResponse.report:type_name -> flowstate.v1.DiagnosticReport
	31,  // 120: flowstate.v1.GetCatalogResponse.catalog:type_name -> flowstate.v1.TaskCatalog
	133, // 121: flowstate.v1.ScheduleActionResult.schedule_time:type_name -> google.protobuf.Timestamp
	133, // 122: flowstate.v1.ScheduleActionResult.actual_time:type_name -> google.protobuf.Timestamp
	133, // 123: flowstate.v1.ScheduleSummary.next_run_time:type_name -> google.protobuf.Timestamp
	13,  // 124: flowstate.v1.ScheduleDescription.trigger:type_name -> flowstate.v1.ScheduleTrigger
	133, // 125: flowstate.v1.ScheduleDescription.next_run_times:type_name -> google.protobuf.Timestamp
	74,  // 126: flowstate.v1.ScheduleDescription.recent_runs:type_name -> flowstate.v1.ScheduleActionResult
	130, // 127: flowstate.v1.ScheduleDescription.inputs:type_name -> flowstate.v1.ScheduleDescription.InputsEntry
	6,   // 128: flowstate.v1.CreateScheduleRequest.workflow:type_name -> flowstate.v1.Workflow
	131, // 129: flowstate.v1.CreateScheduleRequest.inputs:type_name -> flowstate.v1.CreateScheduleRequest.InputsEntry
	76,  // 130: flowstate.v1.CreateScheduleResponse.schedule:type_name -> flowstate.v1.ScheduleDescription
	75,  // 131: flowstate.v1.ListSchedulesResponse.schedules:type_name -> flowstate.v1.ScheduleSummary
	76,  // 132: flowstate.v1.DescribeScheduleResponse.schedule:type_name -> flowstate.v1.ScheduleDescription
	95,  // 133: flowstate.v1.Workflow.StepOutputs.step_values:type_name -> flowstate.v1.Workflow.StepOutputs.StepValuesEntry
	11,  // 134: flowstate.v1.Workflow.StepOutputs.run_outputs:type_name -> flowstate.v1.RunOutputs
	30,  // 135: flowstate.v1.Workflow.VarsEntry.value:type_name -> flowstate.v1.Value
	7,   // 136: flowstate.v1.Workflow.SignalsEntry.value:type_name -> flowstate.v1.SignalPolicy
	98,  // 137: flowstate.v1.Workflow.StepOutputs.StepValuesEntry.value:type_name -> flowstate.v1.Node.Outputs
	30,  // 138: flowstate.v1.RunOutputs.ValuesEntry.value:type_name -> flowstate.v1.Value
	100, // 139: flowstate.v1.Node.Outputs.named_values:type_name -> flowstate.v1.Node.Outputs.NamedValuesEntry
	30,  // 140: flowstate.v1.Node.VarsEntry.value:type_name -> flowstate.v1.Value
	30,  // 141: flowstate.v1.Node.Outputs.NamedValuesEntry.value:type_name -> flowstate.v1.Value
	30,  // 142: flowstate.v1.Scope.VarsEntry.value:type_name -> flowstate.v1.Value
	30,  // 143: flowstate.v1.Scope.AmbientVarsEntry.value:type_name -> flowstate.v1.Value
	30,  // 144: flowstate.v1.Scope.InputsEntry.value:type_name -> flowstate.v1.Value
	14,  // 145: flowstate.v1.Parallel.Branch.steps:type_name -> flowstate.v1.Node
	30,  // 146: flowstate.v1.Call.ArgumentsEntry.value:type_name -> flowstate.v1.Value
	3,   // 147: flowstate.v1.Value.Error.code:type_name -> flowstate.v1.Value.Error.Code
	108, // 148: flowstate.v1.Value.Structure.list:type_name -> flowstate.v1.Value.Structure.List
	109, // 149: flowstate.v1.Value.Structure.map:type_name -> flowstate.v1.Value.Structure.Map
	30,  // 150: flowstate.v1.Value.Structure.List.values:type_name -> flowstate.v1.Value
	110, // 151: flowstate.v1.Value.Structure.Map.entries:type_name -> flowstate.v1.Value.Structure.Map.EntriesEntry
	30,  // 152: flowstate.v1.Value.Structure.Map.EntriesEntry.value:type_name -> flowstate.v1.Value
	30,  // 153: flowstate.v1.Task.InputsEntry.value:type_name -> flowstate.v1.Value
	4,   // 154: flowstate.v1.Task.Log.Inputs.level:type_name -> flowstate.v1.Task.Log.Level
	116, // 155: flowstate.v1.Task.Log.Inputs.fields:type_name -> flowstate.v1.Task.Log.Inputs.FieldsEntry
	119, // 156: flowstate.v1.Task.HTTP.Inputs.headers:type_name -> flowstate.v1.Task.HTTP.Inputs.HeadersEntry
	30,  // 157: flowstate.v1.Task.HTTP.Inputs.bearer:type_name -> flowstate.v1.Value
	120, // 158: flowstate.v1.Task.HTTP.Inputs.outputs:type_name -> flowstate.v1.Task.HTTP.Inputs.OutputsEntry
	121, // 159: flowstate.v1.Task.HTTP.Inputs.query:type_name -> flowstate.v1.Task.HTTP.Inputs.QueryEntry
	30,  // 160: flowstate.v1.Task.HTTP.Inputs.json:type_name -> flowstate.v1.Value
	122, // 161: flowstate.v1.Task.HTTP.Inputs.form:type_name -> flowstate.v1.Task.HTTP.Inputs.FormEntry
	30,  // 162: flowstate.v1.Task.HTTP.Inputs.expect:type_name -> flowstate.v1.Value
	123, // 163: flowstate.v1.Task.HTTP.Outputs.headers:type_name -> flowstate.v1.Task.HTTP.Outputs.HeadersEntry
	135, // 164: flowstate.v1.Task.HTTP.Outputs.json:type_name -> google.api.expr.v1alpha1.Value
	30,  // 165: flowstate.v1.Task.HTTP.Inputs.OutputsEntry.value:type_name -> flowstate.v1.Value
	30,  // 166: flowstate.v1.Task.HTTP.Inputs.QueryEntry.value:type_name -> flowstate.v1.Value
	30,  // 167: flowstate.v1.Task.HTTP.Inputs.FormEntry.value:type_name -> flowstate.v1.Value
	30,  // 168: flowstate.v1.RunRequest.InputsEntry.value:type_name -> flowstate.v1.Value
	30,  // 169: flowstate.v1.Frame.CallVarsEntry.value:type_name -> flowstate.v1.Value
	30,  // 170: flowstate.v1.RunState.VarsEntry.value:type_name -> flowstate.v1.Value
	30,  // 171: flowstate.v1.RunState.InputsEntry.value:type_name -> flowstate.v1.Value
	30,  // 172: flowstate.v1.ScheduleDescription.InputsEntry.value:type_name -> flowstate.v1.Value
	30,  // 173: flowstate.v1.CreateScheduleRequest.InputsEntry.value:type_name -> flowstate.v1.Value
	38,  // 174: flowstate.v1.WorkflowService.Run:input_type -> flowstate.v1.RunRequest
	40,  // 175: flowstate.v1.WorkflowService.Get:input_type -> flowstate.v1.GetRequest
	58,  // 176: flowstate.v1.WorkflowService.Signal:input_type -> flowstate.v1.SignalRequest
	64,  // 177: flowstate.v1.WorkflowService.List:input_type -> flowstate.v1.ListRequest
	60,  // 178: flowstate.v1.WorkflowService.Cancel:input_type -> flowstate.v1.CancelRequest
	62,  // 179: flowstate.v1.WorkflowService.Terminate:input_type -> flowstate.v1.TerminateRequest
	68,  // 180: flowstate.v1.WorkflowService.Validate:input_type -> flowstate.v1.ValidateRequest
	70,  // 181: flowstate.v1.WorkflowService.Compile:input_type -> flowstate.v1.CompileRequest
	72,  // 182: flowstate.v1.WorkflowService.GetCatalog:input_type -> flowstate.v1.GetCatalogRequest
	77,  // 183: flowstate.v1.WorkflowService.CreateSchedule:input_type -> flowstate.v1.CreateScheduleRequest
	79,  // 184: flowstate.v1.WorkflowService.ListSchedules:input_type -> flowstate.v1.ListSchedulesRequest
	81,  // 185: flowstate.v1.WorkflowService.DescribeSchedule:input_type -> flowstate.v1.DescribeScheduleRequest
	83,  // 186: flowstate.v1.WorkflowService.DeleteSchedule:input_type -> flowstate.v1.DeleteScheduleRequest
	85,  // 187: flowstate.v1.WorkflowService.PauseSchedule:input_type -> flowstate.v1.PauseScheduleRequest
	87,  // 188: flowstate.v1.WorkflowService.ResumeSchedule:input_type -> flowstate.v1.ResumeScheduleRequest
	89,  // 189: flowstate.v1.WorkflowService.TriggerSchedule:input_type -> flowstate.v1.TriggerScheduleRequest
	39,  // 190: flowstate.v1.WorkflowService.Run:output_type -> flowstate.v1.RunResponse
	41,  // 191: flowstate.v1.WorkflowService.Get:output_type -> flowstate.v1.GetResponse
	59,  // 192: flowstate.v1.WorkflowService.Signal:output_type -> flowstate.v1.SignalResponse
	66,  // 193: flowstate.v1.WorkflowService.List:output_type -> flowstate.v1.ListResponse
	61,  // 194: flowstate.v1.WorkflowService.Cancel:output_type -> flowstate.v1.CancelResponse
	63,  // 195: flowstate.v1.WorkflowService.Terminate:output_type -> flowstate.v1.TerminateResponse
	69,  // 196: flowstate.v1.WorkflowService.Validate:output_type -> flowstate.v1.ValidateResponse
	71,  // 197: flowstate.v1.WorkflowService.Compile:output_type -> flowstate.v1.CompileResponse
	73,  // 198: flowstate.v1.WorkflowService.GetCatalog:output_type -> flowstate.v1.GetCatalogResponse
	78,  // 199: flowstate.v1.WorkflowService.CreateSchedule:output_type -> flowstate.v1.CreateScheduleResponse
	80,  // 200: flowstate.v1.WorkflowService.ListSchedules:output_type -> flowstate.v1.ListSchedulesResponse
	82,  // 201: flowstate.v1.WorkflowService.DescribeSchedule:output_type -> flowstate.v1.DescribeScheduleResponse
	84,  // 202: flowstate.v1.WorkflowService.DeleteSchedule:output_type -> flowstate.v1.DeleteScheduleResponse
	86,  // 203: flowstate.v1.WorkflowService.PauseSchedule:output_type -> flowstate.v1.PauseScheduleResponse
	88,  // 204: flowstate.v1.WorkflowService.ResumeSchedule:output_type -> flowstate.v1.ResumeScheduleResponse
	90,  // 205: flowstate.v1.WorkflowService.TriggerSchedule:output_type -> flowstate.v1.TriggerScheduleResponse
	190, // [190:206] is the sub-list for method output_type
	174, // [174:190] is the sub-list for method input_type
	174, // [174:174] is the sub-list for extension type_name
	174, // [174:174] is the sub-list for extension extendee
	0,   // [0:174] is the sub-list for field type_name
}

func init() { file_flowstate_v1_flowstate_proto_init() }
func file_flowstate_v1_flowstate_proto_init() {
	if File_flowstate_v1_flowstate_proto != nil {
		return
	}
	file_flowstate_v1_flowstate_proto_msgTypes[0].OneofWrappers = []any{}
	file_flowstate_v1_flowstate_proto_msgTypes[3].OneofWrappers = []any{}
	file_flowstate_v1_flowstate_proto_msgTypes[4].OneofWrappers = []any{}
	file_flowstate_v1_flowstate_proto_msgTypes[8].OneofWrappers = []any{
		(*Node_Task)(nil),
		(*Node_ForEach)(nil),
		(*Node_Parallel)(nil),
		(*Node_Wait)(nil),
		(*Node_Call)(nil),
		(*Node_Loop)(nil),
	}
	file_flowstate_v1_flowstate_proto_msgTypes[12].OneofWrappers = []any{
		(*Wait_Duration)(nil),
		(*Wait_Until)(nil),
		(*Wait_Signal)(nil),
	}
	file_flowstate_v1_flowstate_proto_msgTypes[24].OneofWrappers = []any{
		(*Value_Expr)(nil),
		(*Value_Literal)(nil),
		(*Value_Error_)(nil),
		(*Value_SecretRef)(nil),
		(*Value_Structure_)(nil),
	}
	file_flowstate_v1_flowstate_proto_msgTypes[33].OneofWrappers = []any{
		(*RunResponse_Error_)(nil),
		(*RunResponse_Outputs)(nil),
	}
	file_flowstate_v1_flowstate_proto_msgTypes[34].OneofWrappers = []any{}
	file_flowstate_v1_flowstate_proto_msgTypes[35].OneofWrappers = []any{
		(*GetResponse_Error)(nil),
		(*GetResponse_Outputs)(nil),
	}
	file_flowstate_v1_flowstate_proto_msgTypes[101].OneofWrappers = []any{
		(*Value_Structure_List_)(nil),
		(*Value_Structure_Map_)(nil),
	}
	file_flowstate_v1_flowstate_proto_msgTypes[111].OneofWrappers = []any{}
	type x struct{}
	out := protoimpl.TypeBuilder{
		File: protoimpl.DescBuilder{
			GoPackagePath: reflect.TypeOf(x{}).PkgPath(),
			RawDescriptor: unsafe.Slice(unsafe.StringData(file_flowstate_v1_flowstate_proto_rawDesc), len(file_flowstate_v1_flowstate_proto_rawDesc)),
			NumEnums:      6,
			NumMessages:   126,
			NumExtensions: 0,
			NumServices:   1,
		},
		GoTypes:           file_flowstate_v1_flowstate_proto_goTypes,
		DependencyIndexes: file_flowstate_v1_flowstate_proto_depIdxs,
		EnumInfos:         file_flowstate_v1_flowstate_proto_enumTypes,
		MessageInfos:      file_flowstate_v1_flowstate_proto_msgTypes,
	}.Build()
	File_flowstate_v1_flowstate_proto = out.File
	file_flowstate_v1_flowstate_proto_goTypes = nil
	file_flowstate_v1_flowstate_proto_depIdxs = nil
}
