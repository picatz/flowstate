// Code generated by protoc-gen-go. DO NOT EDIT.
// versions:
// 	protoc-gen-go v1.36.11
// 	protoc        (unknown)
// source: flowstate/v1/trigger.proto

package flowstatev1

import (
	_ "buf.build/gen/go/bufbuild/protovalidate/protocolbuffers/go/buf/validate"
	_ "google.golang.org/genproto/googleapis/api/annotations"
	protoreflect "google.golang.org/protobuf/reflect/protoreflect"
	protoimpl "google.golang.org/protobuf/runtime/protoimpl"
	durationpb "google.golang.org/protobuf/types/known/durationpb"
	timestamppb "google.golang.org/protobuf/types/known/timestamppb"
	reflect "reflect"
	sync "sync"
	unsafe "unsafe"
)

const (
	// Verify that this generated code is sufficiently up-to-date.
	_ = protoimpl.EnforceVersion(20 - protoimpl.MinVersion)
	// Verify that runtime/protoimpl is sufficiently up-to-date.
	_ = protoimpl.EnforceVersion(protoimpl.MaxVersion - 20)
)

// Overlap says what happens when a firing comes due while the previous run is
// still going.
//
// Mirrors Temporal's `ScheduleOverlapPolicy` value for value. Named as a
// Flowstate enum rather than importing Temporal's because this is the schema a
// Flowfile compiles to and a plugin compiles against, and neither should
// acquire a dependency on the execution substrate's API to read a workflow,
// the same reasoning that keeps [RetryPolicy] a Flowstate message.
type ScheduleTrigger_Overlap int32

const (
	// Unspecified takes Temporal's own default, which is SKIP.
	//
	// Left as a real arm rather than defaulted here so that "the author said
	// nothing" and "the author wrote skip" stay distinguishable in the
	// specification. They behave identically; only one of them changes if
	// Temporal's default ever does, and that is the correct one to move.
	ScheduleTrigger_OVERLAP_UNSPECIFIED ScheduleTrigger_Overlap = 0
	// Skip drops the new firing entirely. The safe default: a nightly job that
	// takes longer than a day does not pile up.
	ScheduleTrigger_OVERLAP_SKIP ScheduleTrigger_Overlap = 1
	// BufferOne runs the new firing after the current one finishes, keeping at
	// most one waiting.
	ScheduleTrigger_OVERLAP_BUFFER_ONE ScheduleTrigger_Overlap = 2
	// BufferAll queues every firing, running them one after another. A backlog
	// is bounded only by how long the runs take, so this is the arm to think
	// twice about.
	ScheduleTrigger_OVERLAP_BUFFER_ALL ScheduleTrigger_Overlap = 3
	// CancelOther cancels the running one and starts the new one once it has
	// stopped, letting the old run clean up on the way out.
	ScheduleTrigger_OVERLAP_CANCEL_OTHER ScheduleTrigger_Overlap = 4
	// TerminateOther stops the running one immediately and starts the new one.
	// Nothing the old run would have done on its way out is done.
	ScheduleTrigger_OVERLAP_TERMINATE_OTHER ScheduleTrigger_Overlap = 5
	// AllowAll starts the new firing regardless, so several runs of the workload
	// are in flight at once.
	ScheduleTrigger_OVERLAP_ALLOW_ALL ScheduleTrigger_Overlap = 6
)

// Enum value maps for ScheduleTrigger_Overlap.
var (
	ScheduleTrigger_Overlap_name = map[int32]string{
		0: "OVERLAP_UNSPECIFIED",
		1: "OVERLAP_SKIP",
		2: "OVERLAP_BUFFER_ONE",
		3: "OVERLAP_BUFFER_ALL",
		4: "OVERLAP_CANCEL_OTHER",
		5: "OVERLAP_TERMINATE_OTHER",
		6: "OVERLAP_ALLOW_ALL",
	}
	ScheduleTrigger_Overlap_value = map[string]int32{
		"OVERLAP_UNSPECIFIED":     0,
		"OVERLAP_SKIP":            1,
		"OVERLAP_BUFFER_ONE":      2,
		"OVERLAP_BUFFER_ALL":      3,
		"OVERLAP_CANCEL_OTHER":    4,
		"OVERLAP_TERMINATE_OTHER": 5,
		"OVERLAP_ALLOW_ALL":       6,
	}
)

func (x ScheduleTrigger_Overlap) Enum() *ScheduleTrigger_Overlap {
	p := new(ScheduleTrigger_Overlap)
	*p = x
	return p
}

func (x ScheduleTrigger_Overlap) String() string {
	return protoimpl.X.EnumStringOf(x.Descriptor(), protoreflect.EnumNumber(x))
}

func (ScheduleTrigger_Overlap) Descriptor() protoreflect.EnumDescriptor {
	return file_flowstate_v1_trigger_proto_enumTypes[0].Descriptor()
}

func (ScheduleTrigger_Overlap) Type() protoreflect.EnumType {
	return &file_flowstate_v1_trigger_proto_enumTypes[0]
}

func (x ScheduleTrigger_Overlap) Number() protoreflect.EnumNumber {
	return protoreflect.EnumNumber(x)
}

// Deprecated: Use ScheduleTrigger_Overlap.Descriptor instead.
func (ScheduleTrigger_Overlap) EnumDescriptor() ([]byte, []int) {
	return file_flowstate_v1_trigger_proto_rawDescGZIP(), []int{4, 0}
}

// Triggers is what a workflow says about starting itself.
//
// A message with one field per kind of trigger rather than a oneof, because these
// are not alternatives: a workload that runs nightly *and* on a webhook is an
// ordinary thing to want, and a oneof would make expressing it a schema change
// rather than a second key. Adding a kind is then a new field here, which is
// additive on the wire and additive in the grammar: `triggers:` gains a key and
// every existing file still means what it meant.
//
// Its own message rather than fields on [Workflow] for the same reason the DSL
// spells it `triggers: { schedule: ... }`: a reader meets one block that answers
// "how does this start", and a second kind does not scatter a third and fourth
// top-level key across the file.
type Triggers struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Schedule is the cadence this workload is meant to run at.
	//
	// Optional and, when present, still inert until somebody creates it. See
	// [Workflow.triggers] for why the declaration and the act are separate.
	Schedule *ScheduleTrigger `protobuf:"bytes,1,opt,name=schedule,proto3" json:"schedule,omitempty"`
	// Webhooks are the deliveries this workload is meant to start on, one entry
	// per source.
	//
	// Repeated rather than singular because several sources starting one workload
	// is the ordinary case rather than the exotic one — a payments provider and a
	// storefront both reporting the same order — and each brings its own payload
	// shape, its own signing scheme, and therefore its own mapping into
	// [Workflow.declared_inputs]. That is what makes a webhook a *call site*
	// rather than a second declaration of the inputs: `arguments` here is the same
	// construct as [Call.arguments], checked against the same signature, so N
	// sources are N callers instead of a keyed sub-language nobody asked for.
	//
	// Inert exactly as `schedule` is on the run path: nothing in `Run` reads
	// this, and both execution drivers ignore it. The server's webhook receiver
	// is what acts on it — serving deliveries and verifying each one against the
	// schemes declared here, through [v1.VerifyWebhookDelivery] — while
	// `flow validate` checks the declaration and `flow test` replays stored
	// deliveries against it offline. See [WebhookTrigger] for what the receiver
	// is handed.
	Webhooks []*WebhookTrigger `protobuf:"bytes,2,rep,name=webhooks,proto3" json:"webhooks,omitempty"`
	// Manual narrows the start that already works, and is the one entry in this
	// message that does not enable anything.
	//
	// `flow run`, `flow run local`, the `Run` RPC and MCP all start a workflow
	// today with no declaration at all, so `triggers:` is deliberately *not*
	// exhaustive: adding a webhook must never silently stop `flow run` from
	// working, or authoring and testing break together the first time somebody
	// integrates a payment provider. Absent therefore means what it has always
	// meant — a human may start this — and the field exists to say something
	// narrower. See [ManualTrigger].
	Manual        *ManualTrigger `protobuf:"bytes,3,opt,name=manual,proto3" json:"manual,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *Triggers) Reset() {
	*x = Triggers{}
	mi := &file_flowstate_v1_trigger_proto_msgTypes[0]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *Triggers) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*Triggers) ProtoMessage() {}

func (x *Triggers) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_trigger_proto_msgTypes[0]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use Triggers.ProtoReflect.Descriptor instead.
func (*Triggers) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_trigger_proto_rawDescGZIP(), []int{0}
}

func (x *Triggers) GetSchedule() *ScheduleTrigger {
	if x != nil {
		return x.Schedule
	}
	return nil
}

func (x *Triggers) GetWebhooks() []*WebhookTrigger {
	if x != nil {
		return x.Webhooks
	}
	return nil
}

func (x *Triggers) GetManual() *ManualTrigger {
	if x != nil {
		return x.Manual
	}
	return nil
}

// ManualTrigger is what a workload says about being started by a person.
//
// # Narrowing, never enabling
//
// Every field here removes something. A workflow with no `manual:` block may be
// started by anyone who can reach the server; declaring one requires a recorded
// reason, or restricts who may, or refuses the start outright. There is
// deliberately no field that *grants* a manual start, because one already
// exists in the absence of this message, and a grant-shaped spelling would make
// "did somebody remember to write it" the difference between a runnable file
// and an unrunnable one.
//
// # Where it is enforced, and where it deliberately is not
//
// [FlowstateServer.Run] enforces this, against the caller the server itself
// attested, before Temporal sees anything — the same placement [SignalPolicy]
// has and for the same reason. `flow run local` and `flow test` are never
// gated: the author's machine is not a deployment, and a workflow that cannot
// be developed locally is not one anybody will maintain. That is the rule that
// already keeps egress policy out of the validator, applied to the one entry
// path an author uses while writing.
//
// # Why this and not `if: ${trigger.principal == "admin"}`
//
// Authorization belongs on the trigger, decided when configuration loads,
// where the validator can see it and a deployment owns it. The same test
// written in the body is an expression a test can fake, one `flow validate`
// cannot reason about, and one a later refactor can reorder past the step it
// was guarding. [TriggerContext] is for behaviour; this message is for
// authorization, and the two are kept apart on purpose.
type ManualTrigger struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Denied refuses manual starts outright: this workload is started by its
	// other declared triggers and by nothing else.
	//
	// Explicit and greppable, which is the whole point. "Triggers are
	// exhaustive" would have given the same refusal by inference — declare a
	// webhook, lose `flow run` — and nobody discovers an inferred lock until
	// they need it not to be there.
	//
	// Contradicts both fields below, and the compiler refuses the combination
	// rather than resolving it by precedence: a refusal that also lists who may
	// start the workload is two sentences that cannot both be true.
	Denied bool `protobuf:"varint,1,opt,name=denied,proto3" json:"denied,omitempty"`
	// RequireReason makes a manual start carry a reason, recorded on the run
	// ([RunRequest.reason]).
	//
	// For the workload where "who ran this, and why" is asked after the fact —
	// a break-glass rotation, a manual refund — and where the answer being
	// absent is the failure. A start with no reason is refused while the person
	// who has one is still present to give it.
	RequireReason bool `protobuf:"varint,2,opt,name=require_reason,json=requireReason,proto3" json:"require_reason,omitempty"`
	// AllowedPrincipals restricts a manual start to these issuer-qualified
	// authenticated identities, each written as "<issuer>#<subject>" and matched
	// exactly against the caller's stable Principal.ID. Neither half comes from
	// the request: OIDC and mTLS authentication establish both. The spelling has
	// exactly one "#" separator; an identity with "#" in either half cannot be
	// represented ambiguously and is therefore refused by an allowlist.
	//
	// Empty means every authenticated caller, which is today's behavior and what
	// a workflow with no `manual:` block keeps. Non-empty is a closed set: a
	// qualified principal that is not in it is refused. Bare subjects are invalid,
	// not global aliases: a subject is unique only within its issuer. Missing,
	// zero, and insecure anonymous development identities cannot satisfy the set.
	AllowedPrincipals []string `protobuf:"bytes,3,rep,name=allowed_principals,json=allowedPrincipals,proto3" json:"allowed_principals,omitempty"`
	unknownFields     protoimpl.UnknownFields
	sizeCache         protoimpl.SizeCache
}

func (x *ManualTrigger) Reset() {
	*x = ManualTrigger{}
	mi := &file_flowstate_v1_trigger_proto_msgTypes[1]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ManualTrigger) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ManualTrigger) ProtoMessage() {}

func (x *ManualTrigger) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_trigger_proto_msgTypes[1]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ManualTrigger.ProtoReflect.Descriptor instead.
func (*ManualTrigger) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_trigger_proto_rawDescGZIP(), []int{1}
}

func (x *ManualTrigger) GetDenied() bool {
	if x != nil {
		return x.Denied
	}
	return false
}

func (x *ManualTrigger) GetRequireReason() bool {
	if x != nil {
		return x.RequireReason
	}
	return false
}

func (x *ManualTrigger) GetAllowedPrincipals() []string {
	if x != nil {
		return x.AllowedPrincipals
	}
	return nil
}

// TriggerContext is how a run was started, as its own steps may read it:
// `trigger.kind`, `trigger.name`, `trigger.principal`, `trigger.delivery_id`.
//
// # Safe for durable execution, and why that is not obvious
//
// Every field is fixed when the run starts and carried in [RunState], so it is
// the same value on every replay and across every Continue-As-New. That is what
// separates it from the two facts this language refuses to make ambiently
// readable — the clock and randomness — which change underneath a run and would
// make a replayed step take a different branch than the one history recorded.
// A trigger cannot drift, so it can be read anywhere an ordinary expression is.
//
// # Metadata, never data
//
// The set is closed at these four fields on purpose. Everything a workflow
// *operates on* arrives through a trigger's `with:` into [Workflow.declared_inputs],
// checked against declarations `flow validate` can see. A payload field reachable
// under `trigger` would be a second input path with no contract, invisible to
// the validator — so there is no `trigger.payload`, no `trigger.headers`, and
// none will be added.
//
// # Behaviour, never authorization
//
// `if: ${trigger.kind != "schedule"}` is what this is for: a nightly sweep that
// does not page anyone, a human start that takes a second pair of eyes.
// `if: ${trigger.principal == "admin"}` is the shape to refuse — see
// [ManualTrigger] for where that decision belongs instead.
//
// # Nothing secret rides along
//
// These values are written to durable history, which is broadly readable, so
// [principal] is an attested subject and [delivery_id] is a digest rather than
// the idempotency key it names (a webhook's key is frequently a signature
// header). Neither is material anything can authenticate with.
type TriggerContext struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Kind is how the run started: `manual`, `webhook`, or `schedule`.
	//
	// A string rather than an enum because it is read from a CEL expression, where
	// an enum arrives as a number and `trigger.kind == 1` is not a sentence
	// anybody wants to write or review. The set is closed all the same:
	// `v1.TriggerKinds` names it, and the compiler refuses a comparison against a
	// word that is not in it, so a misspelled `"schedual"` is a diagnostic rather
	// than a branch that is never taken.
	//
	// Empty for a run started before this field existed, which reads as "no
	// trigger recorded" and is the honest answer for one: see
	// `v1.TriggerContextValue` for how that renders, and why every field is
	// rendered rather than omitted.
	Kind string `protobuf:"bytes,1,opt,name=kind,proto3" json:"kind,omitempty"`
	// Name identifies which trigger of that kind started the run: the webhook's
	// declared name, or the schedule's. Empty for a manual start, which has no
	// name to carry — a person is not a declared source.
	Name string `protobuf:"bytes,2,opt,name=name,proto3" json:"name,omitempty"`
	// Principal is who or what started the run, as the server attested it: a
	// caller's subject for a manual start, the trigger the delivery arrived at
	// for a webhook, the schedule's owner for a scheduled firing.
	//
	// Never taken from a request. It is the same value [RunState.identity]
	// carries, restated here because a step reading "how was I started" should
	// not have to know that a webhook's principal is spelled one way under `run`
	// and another under `trigger`.
	Principal string `protobuf:"bytes,3,opt,name=principal,proto3" json:"principal,omitempty"`
	// DeliveryID names the one arrival that started this run, for correlating a
	// run against the provider's own record of what it sent.
	//
	// A digest of the trigger's evaluated `idempotency_key`, never the key: the
	// usual key is a signature header, and this is written to a memo and to
	// history, both durable and broadly readable. Empty for every kind that has
	// no delivery.
	DeliveryId    string `protobuf:"bytes,4,opt,name=delivery_id,json=deliveryId,proto3" json:"delivery_id,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *TriggerContext) Reset() {
	*x = TriggerContext{}
	mi := &file_flowstate_v1_trigger_proto_msgTypes[2]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *TriggerContext) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*TriggerContext) ProtoMessage() {}

func (x *TriggerContext) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_trigger_proto_msgTypes[2]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use TriggerContext.ProtoReflect.Descriptor instead.
func (*TriggerContext) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_trigger_proto_rawDescGZIP(), []int{2}
}

func (x *TriggerContext) GetKind() string {
	if x != nil {
		return x.Kind
	}
	return ""
}

func (x *TriggerContext) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *TriggerContext) GetPrincipal() string {
	if x != nil {
		return x.Principal
	}
	return ""
}

func (x *TriggerContext) GetDeliveryId() string {
	if x != nil {
		return x.DeliveryId
	}
	return ""
}

// WebhookTrigger is one source that may start this workload by delivering to it.
//
// # A call site, not a second declaration of the inputs
//
// `arguments` is the same construct a `call:` step writes as `with:`: the
// workflow's `inputs:` is the signature, and a trigger is another caller of it,
// exactly as `flow run --input` and an MCP invocation are. That choice is the
// whole reason `flow validate` can say `webhook "stripe" does not supply
// required input "amount"` with a line and a column, which a per-input `from:`
// or a per-trigger mapping block could never say: with a mapping block the same
// fact is written twice, in `inputs:` and beside it, with nothing forcing the
// two to agree.
//
// The expressions in `arguments` and in `idempotency_key` are evaluated against
// one bound name, `event`, holding the delivery — its headers and its decoded
// body. That name is bound here and nowhere else in the language: a step body
// that wants to know how the run started is a later slice (`trigger.kind` and
// friends), and payload data must arrive through `arguments` into `inputs:` or
// the file has grown a second input path with no contract.
//
// # Fail closed, declared rather than assumed
//
// Two fields are required by the shape of the problem rather than by taste, and
// both are refused when the file is compiled rather than when a delivery
// arrives:
//
//   - `verify` names at least one signing scheme. There is deliberately no
//     spelling that means "accept anything": an unverifiable delivery is
//     refused, never allowed because it could not be checked.
//   - `idempotency_key` is required. Webhook delivery is at-least-once by
//     nature, so a trigger with no dedupe key turns every retried delivery into
//     a second run — a fact about the transport, not about this integration's
//     luck.
//
// The remaining bound belongs to the receiver that does not exist yet: a
// request body is attacker-chosen input and gets a byte bound *before* it is
// read into memory. `v1.MaxWebhookPayloadBytes` is that number, and it is
// already enforced by the one reader there is today — `flow test` reading a
// stored delivery off disk — so the receiver inherits a bound rather than
// introducing one.
type WebhookTrigger struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name identifies this source, both in a diagnostic (`webhook "stripe" …`)
	// and in a `flow test` case that replays a stored delivery against it. Unique
	// within a file: two sources sharing a name are two mappings a reader cannot
	// tell apart, and a test naming one of them addresses whichever came first.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Verify names the signing schemes a delivery must satisfy, each bound to the
	// key it is checked with — written in a Flowfile as `${secret('...')}`, so the
	// material is a reference the receiver resolves rather than a value in a
	// repository.
	//
	// At least one entry, and the schemes are a closed set
	// ([v1.WebhookVerificationSchemes]) checked when the file compiles: a scheme
	// nobody implements is a delivery nobody can verify, which under the rule
	// above means a delivery that is always refused — better said with a line and
	// a column than discovered in production.
	Verify map[string]*Value `protobuf:"bytes,2,rep,name=verify,proto3" json:"verify,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// IdempotencyKey is the expression that names a delivery, so a redelivery of
	// the same event is recognized as one rather than started again.
	//
	// Evaluated against `event`, like `arguments`. Required — see the message doc
	// for why "this integration has no stable key" has to be something somebody
	// writes down rather than something a default quietly assumes.
	IdempotencyKey *Value `protobuf:"bytes,3,opt,name=idempotency_key,json=idempotencyKey,proto3" json:"idempotency_key,omitempty"`
	// Arguments bind this source's payload to the workflow's declared inputs, one
	// expression per input, over `event`.
	//
	// Named `arguments` rather than `with` for the reason [Call.arguments] is: the
	// schema says what the field holds and the DSL says how it is spelled, and the
	// two constructs are deliberately the same construct — checked against
	// [Workflow.declared_inputs] by the same rules, in both directions.
	Arguments map[string]*Value `protobuf:"bytes,4,rep,name=arguments,proto3" json:"arguments,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	// Signal turns this source into an answer to a gate this workflow already
	// waits at, rather than a start: a verified delivery is carried to the named
	// `wait_for_signal:` of a run that already exists.
	//
	// Mutually exclusive with [arguments], and refused together with it rather
	// than resolved by precedence. The two say incompatible things about what a
	// delivery *is* — one binds a run's `inputs:` as it starts, the other
	// answers a run that is already parked — and one delivery cannot be both.
	// See [WebhookTrigger.Signal].
	Signal        *WebhookTrigger_Signal `protobuf:"bytes,5,opt,name=signal,proto3" json:"signal,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *WebhookTrigger) Reset() {
	*x = WebhookTrigger{}
	mi := &file_flowstate_v1_trigger_proto_msgTypes[3]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *WebhookTrigger) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*WebhookTrigger) ProtoMessage() {}

func (x *WebhookTrigger) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_trigger_proto_msgTypes[3]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use WebhookTrigger.ProtoReflect.Descriptor instead.
func (*WebhookTrigger) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_trigger_proto_rawDescGZIP(), []int{3}
}

func (x *WebhookTrigger) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *WebhookTrigger) GetVerify() map[string]*Value {
	if x != nil {
		return x.Verify
	}
	return nil
}

func (x *WebhookTrigger) GetIdempotencyKey() *Value {
	if x != nil {
		return x.IdempotencyKey
	}
	return nil
}

func (x *WebhookTrigger) GetArguments() map[string]*Value {
	if x != nil {
		return x.Arguments
	}
	return nil
}

func (x *WebhookTrigger) GetSignal() *WebhookTrigger_Signal {
	if x != nil {
		return x.Signal
	}
	return nil
}

// ScheduleTrigger says when a workload should run.
//
// # A projection of Temporal's ScheduleSpec, not a reimplementation
//
// Temporal has first-class Schedules, with a spec covering cron expressions,
// intervals, calendars, skips, jitter, time zones, start and end bounds, and
// policy about what to do when a firing overlaps the last one. Every field here
// is passed to that spec unchanged; nothing in Flowstate computes a next firing
// time, and nothing here is a second implementation of a cron parser at run time.
// What this message is for is giving those parameters a *declarable* spelling:
// the surface, not the mechanism.
//
// # What is here, and what is deliberately not yet
//
// Cron expressions, intervals and explicit calendars are unioned. Start/end
// bounds constrain that union, catch-up limits automatic missed-fire recovery,
// and overlap and pause-on-failure define how executions interact. Backfill is
// creation-only and therefore lives on [CreateScheduleRequest], not here.
//
// # Where the arguments are not
//
// There is deliberately no `inputs:` under a schedule. What a scheduled run is
// started with is bound at creation, from the command that creates it (see
// [CreateScheduleRequest.inputs]), because arguments are a deployment's answer
// (which cluster, which account, which region) while the cadence is the
// workload's. Putting them here would also make the same values expressible in
// two places, since an input declaration already carries a default, and a
// precedence rule between a default and a schedule's override is a rule somebody
// has to learn for no benefit.
type ScheduleTrigger struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Cron holds cron expressions, each naming a set of firing times.
	//
	// Repeated because a cadence like "every weekday at 09:00, and at noon on
	// Sunday" is two expressions and cannot be one: the union is what Temporal
	// takes. The DSL accepts a single string as well as a list, so the common case
	// reads as one line.
	//
	// Temporal accepts 5, 6 or 7 fields, the `@daily`-style shorthands, `@every
	// <interval>`, and an optional `CRON_TZ=`/`TZ=` prefix. `flow validate` checks
	// the shape of what is written here and reports a position; the cluster is the
	// authority on the rest, which is the standing rule for anything a deployment
	// decides.
	Cron []string `protobuf:"bytes,1,rep,name=cron,proto3" json:"cron,omitempty"`
	// Every fires on a fixed interval, measured from the epoch rather than from the
	// last firing, so `every: 1h` is every hour on the hour, not an hour after
	// whenever the previous run happened to end.
	//
	// Beside `cron:` rather than instead of it because the two answer different
	// questions. An interval is what somebody means by "every fifteen minutes"; a
	// cron expression is what they mean by "at 09:00 on weekdays", and expressing
	// either in the other's notation is where the mistakes live.
	Every *durationpb.Duration `protobuf:"bytes,2,opt,name=every,proto3" json:"every,omitempty"`
	// TimeZone is the IANA zone a calendar-based expression is read in, for example
	// `America/New_York`. Empty means UTC.
	//
	// Resolved by the Temporal cluster from the zone database it runs with, not
	// here: a zone name is exactly the kind of thing a deployment answers, and a
	// validator running in an editor would be answering from the author's laptop.
	// What is checked here is that it is a plausible name; whether this cluster has
	// it is the cluster's to say.
	TimeZone string `protobuf:"bytes,3,opt,name=time_zone,json=timeZone,proto3" json:"time_zone,omitempty"`
	// Jitter spreads firings over a window, so a fleet of schedules that all say
	// `0 * * * *` does not become a thundering herd on the hour.
	//
	// Each firing is delayed by a random amount between zero and this, capped by
	// the time until the next firing.
	Jitter *durationpb.Duration `protobuf:"bytes,4,opt,name=jitter,proto3" json:"jitter,omitempty"`
	// Overlap is what happens when a firing comes due with the last run still
	// going. Unset takes Temporal's default, which is to skip.
	Overlap ScheduleTrigger_Overlap `protobuf:"varint,5,opt,name=overlap,proto3,enum=flowstate.v1.ScheduleTrigger_Overlap" json:"overlap,omitempty"`
	// Calendars are unioned with cron and interval cadences: a schedule fires at
	// every time any of the three matches, so a calendar beside a cron expression
	// adds firings rather than filtering them.
	Calendars []*ScheduleTrigger_Calendar `protobuf:"bytes,6,rep,name=calendars,proto3" json:"calendars,omitempty"`
	// StartAt and EndAt bound the window in which firings may occur, inclusively
	// at both ends. Either may be written alone. A start that is not before its
	// end is refused at creation, because such a schedule can never fire and would
	// be created successfully by Temporal all the same.
	StartAt *timestamppb.Timestamp `protobuf:"bytes,7,opt,name=start_at,json=startAt,proto3" json:"start_at,omitempty"`
	EndAt   *timestamppb.Timestamp `protobuf:"bytes,8,opt,name=end_at,json=endAt,proto3" json:"end_at,omitempty"`
	// CatchupWindow limits how late a missed firing may still be taken when the
	// Temporal service returns from an outage.
	//
	// Optional here, but never absent by the time Temporal sees it: an unset
	// window is one Temporal defaults to *one year*, so a schedule that says
	// nothing would take a year of missed firings at once. The server therefore
	// substitutes its own bounded default (`v1.DefaultScheduleCatchupWindow`) for
	// an absent value, and this field is how an operator asks for a different one,
	// up to thirty days.
	CatchupWindow *durationpb.Duration `protobuf:"bytes,9,opt,name=catchup_window,json=catchupWindow,proto3" json:"catchup_window,omitempty"`
	// PauseOnFailure pauses the whole schedule after an action exhausts its
	// retries or times out, so a workload failing every hour stops at the first
	// failure rather than at the hundredth. A firing already started is not
	// recalled.
	PauseOnFailure bool `protobuf:"varint,10,opt,name=pause_on_failure,json=pauseOnFailure,proto3" json:"pause_on_failure,omitempty"`
	unknownFields  protoimpl.UnknownFields
	sizeCache      protoimpl.SizeCache
}

func (x *ScheduleTrigger) Reset() {
	*x = ScheduleTrigger{}
	mi := &file_flowstate_v1_trigger_proto_msgTypes[4]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ScheduleTrigger) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ScheduleTrigger) ProtoMessage() {}

func (x *ScheduleTrigger) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_trigger_proto_msgTypes[4]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ScheduleTrigger.ProtoReflect.Descriptor instead.
func (*ScheduleTrigger) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_trigger_proto_rawDescGZIP(), []int{4}
}

func (x *ScheduleTrigger) GetCron() []string {
	if x != nil {
		return x.Cron
	}
	return nil
}

func (x *ScheduleTrigger) GetEvery() *durationpb.Duration {
	if x != nil {
		return x.Every
	}
	return nil
}

func (x *ScheduleTrigger) GetTimeZone() string {
	if x != nil {
		return x.TimeZone
	}
	return ""
}

func (x *ScheduleTrigger) GetJitter() *durationpb.Duration {
	if x != nil {
		return x.Jitter
	}
	return nil
}

func (x *ScheduleTrigger) GetOverlap() ScheduleTrigger_Overlap {
	if x != nil {
		return x.Overlap
	}
	return ScheduleTrigger_OVERLAP_UNSPECIFIED
}

func (x *ScheduleTrigger) GetCalendars() []*ScheduleTrigger_Calendar {
	if x != nil {
		return x.Calendars
	}
	return nil
}

func (x *ScheduleTrigger) GetStartAt() *timestamppb.Timestamp {
	if x != nil {
		return x.StartAt
	}
	return nil
}

func (x *ScheduleTrigger) GetEndAt() *timestamppb.Timestamp {
	if x != nil {
		return x.EndAt
	}
	return nil
}

func (x *ScheduleTrigger) GetCatchupWindow() *durationpb.Duration {
	if x != nil {
		return x.CatchupWindow
	}
	return nil
}

func (x *ScheduleTrigger) GetPauseOnFailure() bool {
	if x != nil {
		return x.PauseOnFailure
	}
	return false
}

// ScheduleBackfill asks creation to evaluate the declared cadence over a
// historical interval, taking the actions it would have taken then, now.
//
// The interval is half-open: Temporal evaluates times *after* start_at and up
// to and including end_at, so a start written at exactly the first firing time
// wanted excludes it. Write the start a moment earlier.
//
// The server bounds both how many intervals are accepted and their total span,
// because a backfill is the one place where a single request decides how many
// executions a cluster starts at once.
type ScheduleBackfill struct {
	state         protoimpl.MessageState  `protogen:"open.v1"`
	StartAt       *timestamppb.Timestamp  `protobuf:"bytes,1,opt,name=start_at,json=startAt,proto3" json:"start_at,omitempty"`
	EndAt         *timestamppb.Timestamp  `protobuf:"bytes,2,opt,name=end_at,json=endAt,proto3" json:"end_at,omitempty"`
	Overlap       ScheduleTrigger_Overlap `protobuf:"varint,3,opt,name=overlap,proto3,enum=flowstate.v1.ScheduleTrigger_Overlap" json:"overlap,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ScheduleBackfill) Reset() {
	*x = ScheduleBackfill{}
	mi := &file_flowstate_v1_trigger_proto_msgTypes[5]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ScheduleBackfill) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ScheduleBackfill) ProtoMessage() {}

func (x *ScheduleBackfill) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_trigger_proto_msgTypes[5]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ScheduleBackfill.ProtoReflect.Descriptor instead.
func (*ScheduleBackfill) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_trigger_proto_rawDescGZIP(), []int{5}
}

func (x *ScheduleBackfill) GetStartAt() *timestamppb.Timestamp {
	if x != nil {
		return x.StartAt
	}
	return nil
}

func (x *ScheduleBackfill) GetEndAt() *timestamppb.Timestamp {
	if x != nil {
		return x.EndAt
	}
	return nil
}

func (x *ScheduleBackfill) GetOverlap() ScheduleTrigger_Overlap {
	if x != nil {
		return x.Overlap
	}
	return ScheduleTrigger_OVERLAP_UNSPECIFIED
}

// Signal is a delivery answering a declared gate instead of starting a run.
//
// # It reuses `signals:`, and is not a second authorization
//
// A delivery proves possession of the trigger's signing key, not a person,
// so it acts as the trigger: the receiver already mints that principal for a
// run start (issuer `flowstate://webhook`, subject `<workflow>/<trigger>`,
// the receiver's own namespace), and the bridge carries the same principal
// to the same [SignalPolicy] rules through the same authorization path
// `flow signal` takes. No rule kind, no principal kind and no second policy
// spelling: a `signals:` entry admits or refuses a delivery exactly as it
// admits or refuses a person.
//
// What that principal cannot say is *who clicked*. `hmac_sha256` and
// `stripe` attest a key holder, so `distinct_from_starter:` separates
// triggers rather than humans on this path, and a workflow that needs two
// distinct people either side of a gate cannot get them from a webhook
// today.
//
// # The zero case is closed here, and only here
//
// A signal name with no `signals:` entry admits any sender — the deliberate
// zero case, argued at [SignalPolicyAllows], and tolerable for `flow signal`
// behind the server's own authentication. It is not tolerable for a key
// holder on a public route, so a `signal:` naming a name with no policy that
// could admit this trigger's principal is refused when the file compiles.
// That refusal is a property of the *file*, so `flow validate` says it with
// a line and a column rather than a receiver discovering it at three in the
// morning.
type WebhookTrigger_Signal struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Name is the signal delivered, and it must be one a `wait_for_signal:` or
	// `wait_for_signals:` in this same workflow declares.
	//
	// Not a free-form routing key: a name nothing waits for is a delivery the
	// run carries forever without ever consuming, which is the same thing
	// `SignalWithStart` refuses out loud for the same reason.
	Name string `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	// Correlate is the expression, over `event`, that says which run this
	// delivery answers. It yields the run's *entity key* — the stable business
	// key [RunRequest.entity_key] and [SignalWithStartRequest.entity_key]
	// already address a run by — and the receiver composes the workflow id
	// from it exactly as those do.
	//
	// An entity key rather than a workflow id, and that is the load-bearing
	// choice. A payload is sender-shaped, neither verification scheme signs
	// headers, and a workflow id lifted out of one would let a key holder name
	// any run in the tenant, including runs their own trigger has nothing to
	// do with. An entity key is composed with the *receiver's* namespace by
	// `EntityWorkflowID`, under a grammar (lowercase letters, digits, dashes)
	// that cannot reach a run addressed any other way: a webhook-started run's
	// own id is `flowstate-webhook-<digest>`, a namespace this expression has
	// no spelling for.
	//
	// Required: there is no default correlation, because "whichever run is
	// current" is not a thing a delivery may mean.
	Correlate *Value `protobuf:"bytes,2,opt,name=correlate,proto3" json:"correlate,omitempty"`
	// Arguments are what the delivery carries into the gate, one expression
	// per name, over `event` — spelled `with:` in a Flowfile.
	//
	// Named `arguments` rather than `payload` for [WebhookTrigger.arguments]'
	// reason, and spelled `with:` for the same one: this is a *call site*, and
	// when a wait later declares what it accepts, the call-site spelling is
	// already the one triggers use. Nothing checks these names against a
	// signature today, because a wait has none — a `wait_for_signal:` accepts
	// whatever shape arrives and shapes it with its own `outputs:`. That is a
	// gap this spelling is chosen to close add-only later, not a claim the
	// shape is checked now.
	Arguments     map[string]*Value `protobuf:"bytes,3,rep,name=arguments,proto3" json:"arguments,omitempty" protobuf_key:"bytes,1,opt,name=key" protobuf_val:"bytes,2,opt,name=value"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *WebhookTrigger_Signal) Reset() {
	*x = WebhookTrigger_Signal{}
	mi := &file_flowstate_v1_trigger_proto_msgTypes[8]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *WebhookTrigger_Signal) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*WebhookTrigger_Signal) ProtoMessage() {}

func (x *WebhookTrigger_Signal) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_trigger_proto_msgTypes[8]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use WebhookTrigger_Signal.ProtoReflect.Descriptor instead.
func (*WebhookTrigger_Signal) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_trigger_proto_rawDescGZIP(), []int{3, 2}
}

func (x *WebhookTrigger_Signal) GetName() string {
	if x != nil {
		return x.Name
	}
	return ""
}

func (x *WebhookTrigger_Signal) GetCorrelate() *Value {
	if x != nil {
		return x.Correlate
	}
	return nil
}

func (x *WebhookTrigger_Signal) GetArguments() map[string]*Value {
	if x != nil {
		return x.Arguments
	}
	return nil
}

// Calendar is an explicit calendar cadence, matched field by field the way a
// cron expression is: a time matches when at least one range of every field
// matches it. A field nobody writes takes Temporal's default for that field:
// second, minute and hour default to zero, and the three date fields match
// everything. That is why a calendar with no fields at all is refused rather
// than compiled: it means 00:00:00 every day, and nobody writes that by
// meaning it.
type ScheduleTrigger_Calendar struct {
	state         protoimpl.MessageState            `protogen:"open.v1"`
	Second        []*ScheduleTrigger_Calendar_Range `protobuf:"bytes,1,rep,name=second,proto3" json:"second,omitempty"`
	Minute        []*ScheduleTrigger_Calendar_Range `protobuf:"bytes,2,rep,name=minute,proto3" json:"minute,omitempty"`
	Hour          []*ScheduleTrigger_Calendar_Range `protobuf:"bytes,3,rep,name=hour,proto3" json:"hour,omitempty"`
	DayOfMonth    []*ScheduleTrigger_Calendar_Range `protobuf:"bytes,4,rep,name=day_of_month,json=dayOfMonth,proto3" json:"day_of_month,omitempty"`
	Month         []*ScheduleTrigger_Calendar_Range `protobuf:"bytes,5,rep,name=month,proto3" json:"month,omitempty"`
	Year          []*ScheduleTrigger_Calendar_Range `protobuf:"bytes,6,rep,name=year,proto3" json:"year,omitempty"`
	DayOfWeek     []*ScheduleTrigger_Calendar_Range `protobuf:"bytes,7,rep,name=day_of_week,json=dayOfWeek,proto3" json:"day_of_week,omitempty"`
	Comment       string                            `protobuf:"bytes,8,opt,name=comment,proto3" json:"comment,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ScheduleTrigger_Calendar) Reset() {
	*x = ScheduleTrigger_Calendar{}
	mi := &file_flowstate_v1_trigger_proto_msgTypes[10]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ScheduleTrigger_Calendar) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ScheduleTrigger_Calendar) ProtoMessage() {}

func (x *ScheduleTrigger_Calendar) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_trigger_proto_msgTypes[10]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ScheduleTrigger_Calendar.ProtoReflect.Descriptor instead.
func (*ScheduleTrigger_Calendar) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_trigger_proto_rawDescGZIP(), []int{4, 0}
}

func (x *ScheduleTrigger_Calendar) GetSecond() []*ScheduleTrigger_Calendar_Range {
	if x != nil {
		return x.Second
	}
	return nil
}

func (x *ScheduleTrigger_Calendar) GetMinute() []*ScheduleTrigger_Calendar_Range {
	if x != nil {
		return x.Minute
	}
	return nil
}

func (x *ScheduleTrigger_Calendar) GetHour() []*ScheduleTrigger_Calendar_Range {
	if x != nil {
		return x.Hour
	}
	return nil
}

func (x *ScheduleTrigger_Calendar) GetDayOfMonth() []*ScheduleTrigger_Calendar_Range {
	if x != nil {
		return x.DayOfMonth
	}
	return nil
}

func (x *ScheduleTrigger_Calendar) GetMonth() []*ScheduleTrigger_Calendar_Range {
	if x != nil {
		return x.Month
	}
	return nil
}

func (x *ScheduleTrigger_Calendar) GetYear() []*ScheduleTrigger_Calendar_Range {
	if x != nil {
		return x.Year
	}
	return nil
}

func (x *ScheduleTrigger_Calendar) GetDayOfWeek() []*ScheduleTrigger_Calendar_Range {
	if x != nil {
		return x.DayOfWeek
	}
	return nil
}

func (x *ScheduleTrigger_Calendar) GetComment() string {
	if x != nil {
		return x.Comment
	}
	return ""
}

// Range is a low-to-high span of one field, written in a Flowfile as a whole
// number, a list of them, or `{start: 9, end: 17, step: 2}`.
type ScheduleTrigger_Calendar_Range struct {
	state protoimpl.MessageState `protogen:"open.v1"`
	// Start is the first value the range matches, and the only required one.
	Start int32 `protobuf:"varint,1,opt,name=start,proto3" json:"start,omitempty"`
	// End is the last value, inclusive. Zero means no end was written, which
	// matches the start alone. The schema cannot distinguish an unwritten end
	// from one written as zero, and Temporal reads both the same way. An end
	// below the start is refused rather than passed on, since Temporal would
	// silently narrow it to the start.
	End int32 `protobuf:"varint,2,opt,name=end,proto3" json:"end,omitempty"`
	// Step is how far apart the matched values are. Zero means no step was
	// written, which takes every value in the range.
	Step          int32 `protobuf:"varint,3,opt,name=step,proto3" json:"step,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}

func (x *ScheduleTrigger_Calendar_Range) Reset() {
	*x = ScheduleTrigger_Calendar_Range{}
	mi := &file_flowstate_v1_trigger_proto_msgTypes[11]
	ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
	ms.StoreMessageInfo(mi)
}

func (x *ScheduleTrigger_Calendar_Range) String() string {
	return protoimpl.X.MessageStringOf(x)
}

func (*ScheduleTrigger_Calendar_Range) ProtoMessage() {}

func (x *ScheduleTrigger_Calendar_Range) ProtoReflect() protoreflect.Message {
	mi := &file_flowstate_v1_trigger_proto_msgTypes[11]
	if x != nil {
		ms := protoimpl.X.MessageStateOf(protoimpl.Pointer(x))
		if ms.LoadMessageInfo() == nil {
			ms.StoreMessageInfo(mi)
		}
		return ms
	}
	return mi.MessageOf(x)
}

// Deprecated: Use ScheduleTrigger_Calendar_Range.ProtoReflect.Descriptor instead.
func (*ScheduleTrigger_Calendar_Range) Descriptor() ([]byte, []int) {
	return file_flowstate_v1_trigger_proto_rawDescGZIP(), []int{4, 0, 0}
}

func (x *ScheduleTrigger_Calendar_Range) GetStart() int32 {
	if x != nil {
		return x.Start
	}
	return 0
}

func (x *ScheduleTrigger_Calendar_Range) GetEnd() int32 {
	if x != nil {
		return x.End
	}
	return 0
}

func (x *ScheduleTrigger_Calendar_Range) GetStep() int32 {
	if x != nil {
		return x.Step
	}
	return 0
}

var File_flowstate_v1_trigger_proto protoreflect.FileDescriptor

const file_flowstate_v1_trigger_proto_rawDesc = "" +
	"\n" +
	"\x1aflowstate/v1/trigger.proto\x12\fflowstate.v1\x1a\x1bbuf/validate/validate.proto\x1a\x18flowstate/v1/value.proto\x1a\x1fgoogle/api/field_behavior.proto\x1a\x1egoogle/protobuf/duration.proto\x1a\x1fgoogle/protobuf/timestamp.proto\"\xce\x01\n" +
	"\bTriggers\x12?\n" +
	"\bschedule\x18\x01 \x01(\v2\x1d.flowstate.v1.ScheduleTriggerB\x04\xe2A\x01\x01R\bschedule\x12F\n" +
	"\bwebhooks\x18\x02 \x03(\v2\x1c.flowstate.v1.WebhookTriggerB\f\xe2A\x01\x01\xbaH\x05\x92\x01\x02\x10 R\bwebhooks\x129\n" +
	"\x06manual\x18\x03 \x01(\v2\x1b.flowstate.v1.ManualTriggerB\x04\xe2A\x01\x01R\x06manual\"\xb1\x01\n" +
	"\rManualTrigger\x12\x1c\n" +
	"\x06denied\x18\x01 \x01(\bB\x04\xe2A\x01\x01R\x06denied\x12+\n" +
	"\x0erequire_reason\x18\x02 \x01(\bB\x04\xe2A\x01\x01R\rrequireReason\x12U\n" +
	"\x12allowed_principals\x18\x03 \x03(\tB&\xe2A\x01\x01\xbaH\x1f\x92\x01\x1c\x10@\x18\x01\"\x16r\x14\x10\x01\x18\xc0\x022\r^[^#]+#[^#]+$R\x11allowedPrincipals\"\x9d\x01\n" +
	"\x0eTriggerContext\x12\x1b\n" +
	"\x04kind\x18\x01 \x01(\tB\a\xbaH\x04r\x02\x18 R\x04kind\x12\x1b\n" +
	"\x04name\x18\x02 \x01(\tB\a\xbaH\x04r\x02\x18@R\x04name\x12&\n" +
	"\tprincipal\x18\x03 \x01(\tB\b\xbaH\x05r\x03\x18\x80\x02R\tprincipal\x12)\n" +
	"\vdelivery_id\x18\x04 \x01(\tB\b\xbaH\x05r\x03\x18\x80\x01R\n" +
	"deliveryId\"\xfb\x06\n" +
	"\x0eWebhookTrigger\x12;\n" +
	"\x04name\x18\x01 \x01(\tB'\xe2A\x01\x02\xbaH r\x1e\x10\x01\x18@2\x18^[A-Za-z][A-Za-z0-9_-]*$R\x04name\x12X\n" +
	"\x06verify\x18\x02 \x03(\v2(.flowstate.v1.WebhookTrigger.VerifyEntryB\x16\xe2A\x01\x02\xbaH\x0f\x9a\x01\f\b\x01\x10\b\"\x06r\x04\x10\x01\x18@R\x06verify\x12B\n" +
	"\x0fidempotency_key\x18\x03 \x01(\v2\x13.flowstate.v1.ValueB\x04\xe2A\x01\x02R\x0eidempotencyKey\x12_\n" +
	"\targuments\x18\x04 \x03(\v2+.flowstate.v1.WebhookTrigger.ArgumentsEntryB\x14\xe2A\x01\x01\xbaH\r\x9a\x01\n" +
	"\x10@\"\x06r\x04\x10\x01\x18@R\targuments\x12A\n" +
	"\x06signal\x18\x05 \x01(\v2#.flowstate.v1.WebhookTrigger.SignalB\x04\xe2A\x01\x01R\x06signal\x1aN\n" +
	"\vVerifyEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\x1aQ\n" +
	"\x0eArgumentsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\x1a\xc6\x02\n" +
	"\x06Signal\x12B\n" +
	"\x04name\x18\x01 \x01(\tB.\xe2A\x01\x02\xbaH'\xc8\x01\x01r\"\x10\x01\x18\x80\x012\x1b^[A-Za-z0-9][A-Za-z0-9-_]*$R\x04name\x12=\n" +
	"\tcorrelate\x18\x02 \x01(\v2\x13.flowstate.v1.ValueB\n" +
	"\xe2A\x01\x02\xbaH\x03\xc8\x01\x01R\tcorrelate\x12f\n" +
	"\targuments\x18\x03 \x03(\v22.flowstate.v1.WebhookTrigger.Signal.ArgumentsEntryB\x14\xe2A\x01\x01\xbaH\r\x9a\x01\n" +
	"\x10@\"\x06r\x04\x10\x01\x18@R\targuments\x1aQ\n" +
	"\x0eArgumentsEntry\x12\x10\n" +
	"\x03key\x18\x01 \x01(\tR\x03key\x12)\n" +
	"\x05value\x18\x02 \x01(\v2\x13.flowstate.v1.ValueR\x05value:\x028\x01\"\xb0\v\n" +
	"\x0fScheduleTrigger\x12)\n" +
	"\x04cron\x18\x01 \x03(\tB\x15\xe2A\x01\x01\xbaH\x0e\x92\x01\v\x10 \"\ar\x05\x10\x01\x18\x80\x02R\x04cron\x12F\n" +
	"\x05every\x18\x02 \x01(\v2\x19.google.protobuf.DurationB\x15\xe2A\x01\x01\xbaH\x0e\xaa\x01\v\"\x05\b\x80\xe7\x84\x0f2\x02\b\x01R\x05every\x12(\n" +
	"\ttime_zone\x18\x03 \x01(\tB\v\xe2A\x01\x01\xbaH\x04r\x02\x18@R\btimeZone\x12E\n" +
	"\x06jitter\x18\x04 \x01(\v2\x19.google.protobuf.DurationB\x12\xe2A\x01\x01\xbaH\v\xaa\x01\b\"\x04\b\x80\xa3\x052\x00R\x06jitter\x12M\n" +
	"\aoverlap\x18\x05 \x01(\x0e2%.flowstate.v1.ScheduleTrigger.OverlapB\f\xe2A\x01\x01\xbaH\x05\x82\x01\x02\x10\x01R\aoverlap\x12N\n" +
	"\tcalendars\x18\x06 \x03(\v2&.flowstate.v1.ScheduleTrigger.CalendarB\b\xbaH\x05\x92\x01\x02\x10 R\tcalendars\x12;\n" +
	"\bstart_at\x18\a \x01(\v2\x1a.google.protobuf.TimestampB\x04\xe2A\x01\x01R\astartAt\x127\n" +
	"\x06end_at\x18\b \x01(\v2\x1a.google.protobuf.TimestampB\x04\xe2A\x01\x01R\x05endAt\x12W\n" +
	"\x0ecatchup_window\x18\t \x01(\v2\x19.google.protobuf.DurationB\x15\xe2A\x01\x01\xbaH\x0e\xaa\x01\v\"\x05\b\x80\x9a\x9e\x012\x02\b<R\rcatchupWindow\x12.\n" +
	"\x10pause_on_failure\x18\n" +
	" \x01(\bB\x04\xe2A\x01\x01R\x0epauseOnFailure\x1a\xe5\x04\n" +
	"\bCalendar\x12D\n" +
	"\x06second\x18\x01 \x03(\v2,.flowstate.v1.ScheduleTrigger.Calendar.RangeR\x06second\x12D\n" +
	"\x06minute\x18\x02 \x03(\v2,.flowstate.v1.ScheduleTrigger.Calendar.RangeR\x06minute\x12@\n" +
	"\x04hour\x18\x03 \x03(\v2,.flowstate.v1.ScheduleTrigger.Calendar.RangeR\x04hour\x12N\n" +
	"\fday_of_month\x18\x04 \x03(\v2,.flowstate.v1.ScheduleTrigger.Calendar.RangeR\n" +
	"dayOfMonth\x12B\n" +
	"\x05month\x18\x05 \x03(\v2,.flowstate.v1.ScheduleTrigger.Calendar.RangeR\x05month\x12@\n" +
	"\x04year\x18\x06 \x03(\v2,.flowstate.v1.ScheduleTrigger.Calendar.RangeR\x04year\x12L\n" +
	"\vday_of_week\x18\a \x03(\v2,.flowstate.v1.ScheduleTrigger.Calendar.RangeR\tdayOfWeek\x12\"\n" +
	"\acomment\x18\b \x01(\tB\b\xbaH\x05r\x03\x18\x80\x02R\acomment\x1aC\n" +
	"\x05Range\x12\x14\n" +
	"\x05start\x18\x01 \x01(\x05R\x05start\x12\x10\n" +
	"\x03end\x18\x02 \x01(\x05R\x03end\x12\x12\n" +
	"\x04step\x18\x03 \x01(\x05R\x04step\"\xb2\x01\n" +
	"\aOverlap\x12\x17\n" +
	"\x13OVERLAP_UNSPECIFIED\x10\x00\x12\x10\n" +
	"\fOVERLAP_SKIP\x10\x01\x12\x16\n" +
	"\x12OVERLAP_BUFFER_ONE\x10\x02\x12\x16\n" +
	"\x12OVERLAP_BUFFER_ALL\x10\x03\x12\x18\n" +
	"\x14OVERLAP_CANCEL_OTHER\x10\x04\x12\x1b\n" +
	"\x17OVERLAP_TERMINATE_OTHER\x10\x05\x12\x15\n" +
	"\x11OVERLAP_ALLOW_ALL\x10\x06\"\xd7\x01\n" +
	"\x10ScheduleBackfill\x12=\n" +
	"\bstart_at\x18\x01 \x01(\v2\x1a.google.protobuf.TimestampB\x06\xbaH\x03\xc8\x01\x01R\astartAt\x129\n" +
	"\x06end_at\x18\x02 \x01(\v2\x1a.google.protobuf.TimestampB\x06\xbaH\x03\xc8\x01\x01R\x05endAt\x12I\n" +
	"\aoverlap\x18\x03 \x01(\x0e2%.flowstate.v1.ScheduleTrigger.OverlapB\b\xbaH\x05\x82\x01\x02\x10\x01R\aoverlapB\xab\x01\n" +
	"\x10com.flowstate.v1B\fTriggerProtoP\x01Z8github.com/picatz/flowstate/pkg/flowstate/v1;flowstatev1\xa2\x02\x03FXX\xaa\x02\fFlowstate.V1\xca\x02\fFlowstate\\V1\xe2\x02\x18Flowstate\\V1\\GPBMetadata\xea\x02\rFlowstate::V1b\x06proto3"

var (
	file_flowstate_v1_trigger_proto_rawDescOnce sync.Once
	file_flowstate_v1_trigger_proto_rawDescData []byte
)

func file_flowstate_v1_trigger_proto_rawDescGZIP() []byte {
	file_flowstate_v1_trigger_proto_rawDescOnce.Do(func() {
		file_flowstate_v1_trigger_proto_rawDescData = protoimpl.X.CompressGZIP(unsafe.Slice(unsafe.StringData(file_flowstate_v1_trigger_proto_rawDesc), len(file_flowstate_v1_trigger_proto_rawDesc)))
	})
	return file_flowstate_v1_trigger_proto_rawDescData
}

var file_flowstate_v1_trigger_proto_enumTypes = make([]protoimpl.EnumInfo, 1)
var file_flowstate_v1_trigger_proto_msgTypes = make([]protoimpl.MessageInfo, 12)
var file_flowstate_v1_trigger_proto_goTypes = []any{
	(ScheduleTrigger_Overlap)(0),           // 0: flowstate.v1.ScheduleTrigger.Overlap
	(*Triggers)(nil),                       // 1: flowstate.v1.Triggers
	(*ManualTrigger)(nil),                  // 2: flowstate.v1.ManualTrigger
	(*TriggerContext)(nil),                 // 3: flowstate.v1.TriggerContext
	(*WebhookTrigger)(nil),                 // 4: flowstate.v1.WebhookTrigger
	(*ScheduleTrigger)(nil),                // 5: flowstate.v1.ScheduleTrigger
	(*ScheduleBackfill)(nil),               // 6: flowstate.v1.ScheduleBackfill
	nil,                                    // 7: flowstate.v1.WebhookTrigger.VerifyEntry
	nil,                                    // 8: flowstate.v1.WebhookTrigger.ArgumentsEntry
	(*WebhookTrigger_Signal)(nil),          // 9: flowstate.v1.WebhookTrigger.Signal
	nil,                                    // 10: flowstate.v1.WebhookTrigger.Signal.ArgumentsEntry
	(*ScheduleTrigger_Calendar)(nil),       // 11: flowstate.v1.ScheduleTrigger.Calendar
	(*ScheduleTrigger_Calendar_Range)(nil), // 12: flowstate.v1.ScheduleTrigger.Calendar.Range
	(*Value)(nil),                          // 13: flowstate.v1.Value
	(*durationpb.Duration)(nil),            // 14: google.protobuf.Duration
	(*timestamppb.Timestamp)(nil),          // 15: google.protobuf.Timestamp
}
var file_flowstate_v1_trigger_proto_depIdxs = []int32{
	5,  // 0: flowstate.v1.Triggers.schedule:type_name -> flowstate.v1.ScheduleTrigger
	4,  // 1: flowstate.v1.Triggers.webhooks:type_name -> flowstate.v1.WebhookTrigger
	2,  // 2: flowstate.v1.Triggers.manual:type_name -> flowstate.v1.ManualTrigger
	7,  // 3: flowstate.v1.WebhookTrigger.verify:type_name -> flowstate.v1.WebhookTrigger.VerifyEntry
	13, // 4: flowstate.v1.WebhookTrigger.idempotency_key:type_name -> flowstate.v1.Value
	8,  // 5: flowstate.v1.WebhookTrigger.arguments:type_name -> flowstate.v1.WebhookTrigger.ArgumentsEntry
	9,  // 6: flowstate.v1.WebhookTrigger.signal:type_name -> flowstate.v1.WebhookTrigger.Signal
	14, // 7: flowstate.v1.ScheduleTrigger.every:type_name -> google.protobuf.Duration
	14, // 8: flowstate.v1.ScheduleTrigger.jitter:type_name -> google.protobuf.Duration
	0,  // 9: flowstate.v1.ScheduleTrigger.overlap:type_name -> flowstate.v1.ScheduleTrigger.Overlap
	11, // 10: flowstate.v1.ScheduleTrigger.calendars:type_name -> flowstate.v1.ScheduleTrigger.Calendar
	15, // 11: flowstate.v1.ScheduleTrigger.start_at:type_name -> google.protobuf.Timestamp
	15, // 12: flowstate.v1.ScheduleTrigger.end_at:type_name -> google.protobuf.Timestamp
	14, // 13: flowstate.v1.ScheduleTrigger.catchup_window:type_name -> google.protobuf.Duration
	15, // 14: flowstate.v1.ScheduleBackfill.start_at:type_name -> google.protobuf.Timestamp
	15, // 15: flowstate.v1.ScheduleBackfill.end_at:type_name -> google.protobuf.Timestamp
	0,  // 16: flowstate.v1.ScheduleBackfill.overlap:type_name -> flowstate.v1.ScheduleTrigger.Overlap
	13, // 17: flowstate.v1.WebhookTrigger.VerifyEntry.value:type_name -> flowstate.v1.Value
	13, // 18: flowstate.v1.WebhookTrigger.ArgumentsEntry.value:type_name -> flowstate.v1.Value
	13, // 19: flowstate.v1.WebhookTrigger.Signal.correlate:type_name -> flowstate.v1.Value
	10, // 20: flowstate.v1.WebhookTrigger.Signal.arguments:type_name -> flowstate.v1.WebhookTrigger.Signal.ArgumentsEntry
	13, // 21: flowstate.v1.WebhookTrigger.Signal.ArgumentsEntry.value:type_name -> flowstate.v1.Value
	12, // 22: flowstate.v1.ScheduleTrigger.Calendar.second:type_name -> flowstate.v1.ScheduleTrigger.Calendar.Range
	12, // 23: flowstate.v1.ScheduleTrigger.Calendar.minute:type_name -> flowstate.v1.ScheduleTrigger.Calendar.Range
	12, // 24: flowstate.v1.ScheduleTrigger.Calendar.hour:type_name -> flowstate.v1.ScheduleTrigger.Calendar.Range
	12, // 25: flowstate.v1.ScheduleTrigger.Calendar.day_of_month:type_name -> flowstate.v1.ScheduleTrigger.Calendar.Range
	12, // 26: flowstate.v1.ScheduleTrigger.Calendar.month:type_name -> flowstate.v1.ScheduleTrigger.Calendar.Range
	12, // 27: flowstate.v1.ScheduleTrigger.Calendar.year:type_name -> flowstate.v1.ScheduleTrigger.Calendar.Range
	12, // 28: flowstate.v1.ScheduleTrigger.Calendar.day_of_week:type_name -> flowstate.v1.ScheduleTrigger.Calendar.Range
	29, // [29:29] is the sub-list for method output_type
	29, // [29:29] is the sub-list for method input_type
	29, // [29:29] is the sub-list for extension type_name
	29, // [29:29] is the sub-list for extension extendee
	0,  // [0:29] is the sub-list for field type_name
}

func init() { file_flowstate_v1_trigger_proto_init() }
func file_flowstate_v1_trigger_proto_init() {
	if File_flowstate_v1_trigger_proto != nil {
		return
	}
	file_flowstate_v1_value_proto_init()
	type x struct{}
	out := protoimpl.TypeBuilder{
		File: protoimpl.DescBuilder{
			GoPackagePath: reflect.TypeOf(x{}).PkgPath(),
			RawDescriptor: unsafe.Slice(unsafe.StringData(file_flowstate_v1_trigger_proto_rawDesc), len(file_flowstate_v1_trigger_proto_rawDesc)),
			NumEnums:      1,
			NumMessages:   12,
			NumExtensions: 0,
			NumServices:   0,
		},
		GoTypes:           file_flowstate_v1_trigger_proto_goTypes,
		DependencyIndexes: file_flowstate_v1_trigger_proto_depIdxs,
		EnumInfos:         file_flowstate_v1_trigger_proto_enumTypes,
		MessageInfos:      file_flowstate_v1_trigger_proto_msgTypes,
	}.Build()
	File_flowstate_v1_trigger_proto = out.File
	file_flowstate_v1_trigger_proto_goTypes = nil
	file_flowstate_v1_trigger_proto_depIdxs = nil
}
