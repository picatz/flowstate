---
description: Compatibility alias for cross-driver conformance work
---

Use the `both-drivers` skill. Put observable behavior in shared conformance
cases, confirm both local and Temporal call sites execute them, and run the
bounded shared suite before broader verification.
