---
description: Compatibility alias for a full Flowstate verification rehearsal
---

Use the `flowstate-verify` skill in full CI-parity mode. Run `make check`, stop at
the first actionable failure, and report exactly which checks ran and which did
not.
