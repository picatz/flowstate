issuers:
  - name: github-actions
    issuer: https://token.actions.githubusercontent.com
    audiences: [flowstate]
    algorithms: [RS256]

secrets:
  allow:
    - 'secret.scheme == "op" && secret.name == "github#token"'
